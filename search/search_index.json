{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Notes","text":"<p>Learning notes and references across software engineering, ML, and AI topics.</p> Directory Description data/ NumPy and Pandas \u2014 arrays, vectorised operations, DataFrames finance/ Trading, exchanges, and market data APIs git/ Git workflows, commands, and CI/CD python/ Python language, tooling, and type system tools/ Language-agnostic tools and notation"},{"location":"data/","title":"Data","text":"<p>NumPy and Pandas \u2014 arrays, vectorised operations, DataFrames.</p> Directory Description numpy/ NumPy \u2014 arrays, broadcasting, vectorised operations pandas/ Pandas \u2014 DataFrame transforms, chaining, indexing"},{"location":"data/numpy/","title":"NumPy","text":"File Type Description broadcasting.md note Broadcasting rules \u2014 shape alignment, stretching, vectorised ops dtypes.md note dtype system \u2014 numeric types, casting, overflow, memory layout"},{"location":"data/numpy/broadcasting/","title":"Broadcasting","text":"<p>NumPy applies element-wise operations to arrays of different shapes by virtually stretching smaller arrays to match \u2014 no data is copied, no Python loop is written.</p>"},{"location":"data/numpy/broadcasting/#the-three-rules-right-to-left-comparison","title":"The three rules (right-to-left comparison)","text":"<ol> <li>Pad left with 1s \u2014 if arrays differ in rank, prepend 1s to the shorter shape.</li> <li>Stretch size-1 dims \u2014 a dimension of size 1 expands to match the other array's size in that dim.</li> <li>Error on mismatch \u2014 two dimensions that are neither equal nor 1 raise <code>ValueError</code>.</li> </ol> <p>Output shape = element-wise max of the aligned shapes.</p>"},{"location":"data/numpy/broadcasting/#shape-traces","title":"Shape traces","text":"<pre><code># scalar + 1-D\n(3,) + ()  \u2192  pad  \u2192  (3,) + (1,)  \u2192  stretch  \u2192  (3,) + (3,)  =  (3,)\n\n# 2-D matrix \u2212 column vector\n(2, 3) - (2, 1)  \u2192  stretch col  \u2192  (2, 3) - (2, 3)  =  (2, 3)\n\n# outer product via row \u00d7 column\n(1, 3) * (3, 1)  \u2192  stretch both  \u2192  (3, 3) * (3, 3)  =  (3, 3)\n\n# 3-D general case\n(4, 1, 3) + (5, 3)  \u2192  pad  \u2192  (4, 1, 3) + (1, 5, 3)  \u2192  (4, 5, 3)\n</code></pre>"},{"location":"data/numpy/broadcasting/#common-patterns","title":"Common patterns","text":"<pre><code># subtract column mean from every row\nA - A.mean(axis=1, keepdims=True)     # (M, N) - (M, 1)  \u2192  (M, N)\n\n# normalise asset prices to start at 1  \u2014 prices shape (T, N)\nprices / prices[0]                    # (T, N) / (N,)  \u2192  (T, N)\n\n# daily returns for all assets at once\nreturns = np.diff(prices, axis=0) / prices[:-1]   # (T-1, N)\n\n# outer sum of two 1-D arrays\nb[:, np.newaxis] + a                  # (M, 1) + (N,)  \u2192  (M, N)\n</code></pre>"},{"location":"data/numpy/broadcasting/#npnewaxis","title":"<code>np.newaxis</code>","text":"<p>Insert a size-1 axis explicitly to control which dims align:</p> <pre><code>a = np.array([1, 2, 3])   # (3,)\nb = np.array([10, 20])    # (2,)\nb[:, np.newaxis] + a      # (2, 1) + (1, 3)  \u2192  (2, 3)  \u2014 outer sum\n</code></pre> <p><code>np.newaxis</code> is an alias for <code>None</code>; both work identically.</p>"},{"location":"data/numpy/broadcasting/#why-its-fast","title":"Why it's fast","text":"<p>Python loops box every number and dispatch through the interpreter on each iteration. NumPy sends the whole operation to a tight C loop over contiguous typed memory. Typical speedup: 100\u2013500\u00d7 for arithmetic on arrays of \u2265 10 k elements. Broadcasting adds no memory overhead \u2014 a stride of 0 simulates repetition without allocation.</p>"},{"location":"data/numpy/broadcasting/#gotchas","title":"Gotchas","text":"<p>keepdims is almost always required after a reduction</p> <p><code>A.mean(axis=1)</code> returns shape <code>(M,)</code> \u2014 this aligns on the column axis when broadcast back, not the row axis. Use <code>keepdims=True</code> to get <code>(M, 1)</code>, which broadcasts correctly along columns.</p> <ul> <li><code>keepdims=True</code> is essential when reducing and then broadcasting back: <code>A.mean(axis=1)</code> gives shape <code>(M,)</code> which aligns on the wrong axis; <code>keepdims=True</code> gives <code>(M, 1)</code> which broadcasts correctly.</li> <li>Two arrays that look compatible may silently produce the wrong shape \u2014 always check <code>.shape</code> on intermediate results when debugging.</li> </ul>"},{"location":"data/numpy/dtypes/","title":"dtypes","text":"<p>A dtype is the contract that makes NumPy arrays fast: every element occupies exactly the same number of bytes, laid out contiguously in memory, with no per-element Python overhead. The CPU can stride through the block predictably and operate on multiple elements per clock cycle.</p>"},{"location":"data/numpy/dtypes/#common-dtypes","title":"Common dtypes","text":"Family Types Default Signed int <code>int8</code> <code>int16</code> <code>int32</code> <code>int64</code> <code>int64</code> Unsigned int <code>uint8</code> <code>uint16</code> <code>uint32</code> <code>uint64</code> \u2014 Float <code>float16</code> <code>float32</code> <code>float64</code> <code>float64</code> Complex <code>complex64</code> <code>complex128</code> \u2014 Bool <code>bool</code> \u2014 Object <code>object</code> fallback for mixed/string"},{"location":"data/numpy/dtypes/#specifying-and-inspecting","title":"Specifying and inspecting","text":"<pre><code>a = np.array([1, 2, 3], dtype=np.float32)\na.dtype            # dtype('float32')\na.dtype.itemsize   # 4 bytes\na.nbytes           # 12 bytes total\n\nnp.zeros((100, 100), dtype=np.uint8)\nnp.array([1, 2, 3], dtype='f4')   # shorthand: 'f4'=float32, 'i2'=int16, 'u4'=uint32\n</code></pre>"},{"location":"data/numpy/dtypes/#type-inference-from-literals","title":"Type inference from literals","text":"<pre><code>np.array([1, 2, 3])        # int64\nnp.array([1.0, 2.0])       # float64\nnp.array([1, 2, 3.0])      # float64  (mixed \u2192 upcast)\nnp.array([True, False])    # bool\nnp.array([1, 'a'])         # object  (heterogeneous \u2192 fallback, loses all speed)\n</code></pre>"},{"location":"data/numpy/dtypes/#casting","title":"Casting","text":"<pre><code>a = np.array([1.7, 2.9])\na.astype(np.int32)                      # [1, 2] \u2014 truncates, no rounding\na.astype('float32')                     # halves memory\na.astype(np.int32, casting='safe')      # raises TypeError \u2014 float\u2192int not safe\n</code></pre> <p><code>casting</code> options: <code>'safe'</code>, <code>'same_kind'</code>, <code>'unsafe'</code> (default, no check).</p>"},{"location":"data/numpy/dtypes/#precision-limits","title":"Precision limits","text":"<pre><code>np.finfo(np.float32)   # ~7 decimal digits, max ~3.4e38\nnp.finfo(np.float64)   # ~15 decimal digits, max ~1.8e308\nnp.iinfo(np.int8)      # min=-128, max=127\n</code></pre>"},{"location":"data/numpy/dtypes/#gotchas","title":"Gotchas","text":"<p>object dtype silently kills all performance</p> <p>When NumPy falls back to <code>object</code> dtype (mixed types, strings without explicit dtype), every element becomes a Python heap object. Vectorised C loops are replaced by per-element Python dispatch \u2014 the same speed as a hand-written loop. Always inspect <code>.dtype</code> after loading data from CSV or JSON.</p> <p>Silent integer overflow \u2014 no error, wraps around: <pre><code>np.int8(127) + np.int8(1)   # -128\n</code></pre></p> <p>No NaN in integer arrays \u2014 NaN is a float concept: <pre><code>np.array([1, 2, np.nan])   # silently becomes float64\n</code></pre></p> <p><code>np.int_</code> is platform-dependent \u2014 maps to C <code>long</code> (64-bit on Linux/macOS, 32-bit on some Windows). Use explicit <code>np.int32</code> / <code>np.int64</code> for reproducibility.</p> <p><code>object</code> dtype is a trap \u2014 stores Python object pointers; all vectorisation is lost. Avoid unless data is genuinely heterogeneous.</p>"},{"location":"data/numpy/dtypes/#memory-layout-and-broadcasting","title":"Memory layout and broadcasting","text":"<p>The fixed-width contiguous layout is what makes broadcasting zero-copy: a stride of 0 simulates repetition without allocating a new array.</p>"},{"location":"data/pandas/","title":"Pandas","text":"File Type Description chaining.md note Method chaining \u2014 <code>.assign</code>, <code>.pipe</code>, <code>.query</code>, full operation catalog datetimes.md note Timestamp, DatetimeIndex, .dt accessor, tz handling, resampling dtypes.md note dtype system \u2014 nullable types, categoricals, StringDtype, pd.NA indexing.md note Views vs copies, SettingWithCopyWarning, <code>.loc</code> rules, Copy-on-Write"},{"location":"data/pandas/chaining/","title":"Method Chaining","text":""},{"location":"data/pandas/chaining/#method-chaining_1","title":"Method chaining","text":"<p>Instead of reassigning <code>df</code> repeatedly, chain operations that each return a new DataFrame:</p> <pre><code>result = (\n    raw\n    .query('price &gt; 0')\n    .assign(\n        log_price=lambda d: np.log(d['price']),\n        returns=lambda d: d['price'].pct_change(),\n    )\n    .dropna()\n    .reset_index(drop=True)\n)\n</code></pre> <p><code>raw</code> is never mutated. Each step is independently readable and commentable.</p>"},{"location":"data/pandas/chaining/#queryexpr-filter-rows","title":"<code>.query(expr)</code> \u2014 filter rows","text":"<pre><code>df.query('age &gt; 18 and city == \"London\"')\ndf.query('price &gt; @threshold')   # @ references a Python variable\n</code></pre>"},{"location":"data/pandas/chaining/#assignkwargs-add-or-overwrite-columns","title":"<code>.assign(**kwargs)</code> \u2014 add or overwrite columns","text":"<pre><code>df.assign(\n    income_k=lambda d: d['income'] / 1_000,\n    is_high_earner=lambda d: d['income_k'] &gt; 50,   # can reference columns assigned above\n)\n</code></pre> <p>Lambdas receive the DataFrame as it is at that step. Columns added earlier in the same call are visible to later ones (dict ordering, Python 3.7+). Assign <code>None</code> to drop a column.</p>"},{"location":"data/pandas/chaining/#pipefunc-args-kwargs-apply-a-named-function-mid-chain","title":"<code>.pipe(func, *args, **kwargs)</code> \u2014 apply a named function mid-chain","text":"<pre><code>def trim_outliers(df, col, n_std=3):\n    lo, hi = df[col].mean() - n_std*df[col].std(), df[col].mean() + n_std*df[col].std()\n    return df[df[col].between(lo, hi)]\n\nresult = (\n    raw\n    .assign(returns=lambda d: d['close'].pct_change())\n    .pipe(trim_outliers, col='returns', n_std=2.5)\n    .dropna()\n)\n</code></pre> <p><code>df.pipe(f, ...)</code> calls <code>f(df, ...)</code> \u2014 a fluency adapter for functions that don't natively chain. Debug mid-chain: <code>.pipe(lambda d: (print(d.shape), d)[1])</code>.</p>"},{"location":"data/pandas/chaining/#common-chain-operations-catalog","title":"Common chain operations \u2014 catalog","text":"# Method What it does 1 <code>.query(expr)</code> Filter rows 2 <code>.assign(**kwargs)</code> Add or transform columns 3 <code>.astype(dict)</code> Cast dtypes 4 <code>.rename(columns=...)</code> Rename columns 5 <code>.drop(columns=[...])</code> / <code>.filter(...)</code> Remove or select columns 6 <code>.sort_values(by=...)</code> Sort rows 7 <code>.dropna()</code> / <code>.fillna(...)</code> Handle missing values 8 <code>.assign(c=lambda d: d['c'].str...)</code> String operations on a column 9 <code>.set_index()</code> / <code>.reset_index(drop=True)</code> Index management 10 <code>.groupby().agg(...)</code> Aggregate by group"},{"location":"data/pandas/chaining/#1-filter-rows-query","title":"1. Filter rows \u2014 <code>.query()</code>","text":"<pre><code>df.query('price &gt; 0 and volume &gt; 1_000')\ndf.query('status == \"active\"')\ndf.query('date &gt; @cutoff')      # @ = Python variable\n</code></pre> <p>assign() lambdas see columns added earlier in the same call</p> <p>Within one <code>.assign()</code> call, <code>lambda d: ...</code> receives the DataFrame as it exists at that point \u2014 including columns defined by earlier keyword arguments in the same call. This lets you build derived columns in sequence without chaining multiple <code>.assign()</code> calls.</p>"},{"location":"data/pandas/chaining/#2-add-transform-columns-assign","title":"2. Add / transform columns \u2014 <code>.assign()</code>","text":"<p>See the section above. Key: lambdas see columns added earlier in the same call.</p>"},{"location":"data/pandas/chaining/#3-cast-dtype-astype","title":"3. Cast dtype \u2014 <code>.astype()</code>","text":"<pre><code>df.astype({'price': 'float32', 'volume': 'float32', 'trades': 'int32'})\n# or inline via assign:\ndf.assign(price=lambda d: d['price'].astype('float32'))\n</code></pre> <p>Use <code>.astype(dict)</code> for bulk casts; <code>.assign()</code> when interleaved with other column ops.</p>"},{"location":"data/pandas/chaining/#4-rename-columns-rename","title":"4. Rename columns \u2014 <code>.rename()</code>","text":"<pre><code>df.rename(columns={'open_time': 'date', 'quote_vol': 'quote_volume'})\ndf.rename(columns=str.lower)                        # apply function to all names\ndf.rename(columns=lambda c: c.replace(' ', '_'))\n</code></pre>"},{"location":"data/pandas/chaining/#5-select-or-drop-columns","title":"5. Select or drop columns","text":"<pre><code>df.drop(columns=['ignore', 'close_time'])\ndf.filter(items=['open', 'high', 'low', 'close'])   # select by name\ndf.filter(like='taker')                             # names containing 'taker'\ndf.filter(regex=r'^vol')                            # names matching regex\n</code></pre> <p>Prefer <code>.filter()</code> over <code>df[['a','b']]</code> in a chain \u2014 keeps the fluent style.</p>"},{"location":"data/pandas/chaining/#6-sort-sort_values","title":"6. Sort \u2014 <code>.sort_values()</code>","text":"<pre><code>df.sort_values('date')\ndf.sort_values('volume', ascending=False)\ndf.sort_values(['date', 'symbol'])\n</code></pre>"},{"location":"data/pandas/chaining/#7-handle-missing-dropna-fillna","title":"7. Handle missing \u2014 <code>.dropna()</code> / <code>.fillna()</code>","text":"<pre><code>df.dropna(subset=['open', 'close'])     # drop rows where these cols are null\ndf.fillna({'volume': 0})\ndf.ffill()                              # forward-fill (time-series gaps)\n</code></pre> <p><code>.dropna()</code> usually follows <code>.assign()</code> calls that produce NaN (e.g. <code>.pct_change()</code>).</p>"},{"location":"data/pandas/chaining/#8-string-operations-assign-str","title":"8. String operations \u2014 <code>.assign()</code> + <code>.str</code>","text":"<pre><code>df.assign(\n    symbol=lambda d: d['symbol'].str.upper(),\n    name=lambda d: d['name'].str.strip().str.lower(),\n    base=lambda d: d['symbol'].str[:-4],    # 'BTCUSDT' \u2192 'BTC'\n)\n</code></pre> <p>Chain <code>.str</code> methods: <code>.str.strip().str.lower().str.replace('-', '_')</code>. Always vectorised \u2014 no <code>.apply()</code> loop needed.</p>"},{"location":"data/pandas/chaining/#9-index-management-set_index-reset_index","title":"9. Index management \u2014 <code>.set_index()</code> / <code>.reset_index()</code>","text":"<pre><code>df.set_index('date')            # promote column to index (enables resample)\ndf.reset_index(drop=True)       # clean 0\u2026n-1 index after filtering/sorting\ndf.reset_index()                # move index back to a column\n</code></pre>"},{"location":"data/pandas/chaining/#10-groupby-aggregation-groupbyagg","title":"10. Groupby aggregation \u2014 <code>.groupby().agg()</code>","text":"<pre><code>(df\n .groupby('symbol')\n .agg(\n     avg_close=('close', 'mean'),\n     total_volume=('volume', 'sum'),\n     n=('close', 'count'),\n )\n .sort_values('total_volume', ascending=False)\n .reset_index()\n)\n</code></pre> <p>Named aggregations (<code>output=('source', 'func')</code>) name columns directly. The result of <code>.agg()</code> re-enters the chain as a normal DataFrame.</p>"},{"location":"data/pandas/chaining/#full-pipeline-example","title":"Full pipeline example","text":"<pre><code>result = (\n    raw\n    .query('volume &gt; 0 and close &gt; 0')\n    .astype({'open': 'float32', 'high': 'float32',\n             'low': 'float32', 'close': 'float32'})\n    .assign(\n        returns=lambda d: d['close'].pct_change(),\n        buy_ratio=lambda d: d['taker_buy_vol'] / d['volume'],\n        symbol=lambda d: d['symbol'].str.upper(),\n    )\n    .rename(columns={'quote_vol': 'quote_volume'})\n    .drop(columns=['ignore'])\n    .dropna(subset=['returns'])\n    .sort_values('date')\n    .reset_index(drop=True)\n)\n</code></pre>"},{"location":"data/pandas/datetimes/","title":"Datetimes","text":""},{"location":"data/pandas/datetimes/#the-stack","title":"The stack","text":"<pre><code>Python      datetime.datetime    microsecond precision, tz-aware or naive\nNumPy       np.datetime64[ns]    nanosecond precision, no timezone\nPandas      pd.Timestamp         nanosecond, tz-aware or naive\n            pd.DatetimeIndex     array of Timestamps (column or index)\n</code></pre> <p><code>pd.Timestamp</code> is both a subclass of <code>datetime.datetime</code> and a wrapper around <code>np.datetime64[ns]</code>. It inherits all <code>datetime</code> methods while storing data as a 64-bit integer (nanoseconds since Unix epoch).</p> <p>Range limit: nanosecond int64 restricts representable dates to ~1678\u20132262 CE. Dates outside this raise <code>OutOfBoundsDatetime</code>.</p>"},{"location":"data/pandas/datetimes/#pdtimestamp-scalar","title":"<code>pd.Timestamp</code> \u2014 scalar","text":"<pre><code>ts = pd.Timestamp('2024-01-15 09:30:00')\nts.year, ts.month, ts.day    # 2024, 1, 15\nts.value                     # int64 nanoseconds since epoch\n\nisinstance(ts, datetime.datetime)  # True\n\n# timezone\nts_utc = pd.Timestamp('2024-01-15 09:30', tz='UTC')\nts_utc.tz_convert('America/New_York')\n\n# missing datetime\npd.NaT          # like NaN for datetimes; pd.isna(pd.NaT) == True\n</code></pre>"},{"location":"data/pandas/datetimes/#pdto_datetime-parsing","title":"<code>pd.to_datetime()</code> \u2014 parsing","text":"<pre><code>pd.to_datetime('2024-01-15')                          # from string\npd.to_datetime(df['date_str'])                        # column of strings\npd.to_datetime(df['ts_ms'], unit='ms', utc=True)      # Unix ms (Binance format)\npd.to_datetime(df['date_str'], format='%Y-%m-%d')     # explicit format = faster\npd.to_datetime(df['date_str'], errors='coerce')       # bad values \u2192 NaT\n</code></pre> <p>Always pass <code>utc=True</code> when source is Unix timestamps \u2014 produces <code>datetime64[ns, UTC]</code> and keeps the whole column tz-aware.</p>"},{"location":"data/pandas/datetimes/#datetime-column-dtype","title":"Datetime column dtype","text":"<pre><code>df['date'].dtype         # dtype('&lt;M8[ns]')  \u2014 i.e. datetime64[ns]\ndf['date_utc'].dtype     # datetime64[ns, UTC]  \u2014 tz-aware variant\ndf['date'].iloc[0]       # pd.Timestamp(...)\n</code></pre>"},{"location":"data/pandas/datetimes/#dt-accessor-vectorised-operations","title":"<code>.dt</code> accessor \u2014 vectorised operations","text":"<pre><code>s = df['date']            # datetime64 Series\n\ns.dt.year                 # s.dt.month, .day, .hour, .minute, .second\ns.dt.dayofweek            # 0=Mon \u2026 6=Sun\ns.dt.day_name()           # 'Monday', \u2026\ns.dt.date                 # Python date objects (drops time component)\n\ns.dt.tz_localize('UTC')           # naive \u2192 aware (attach tz)\ns.dt.tz_convert('America/New_York')  # aware \u2192 different tz\n\ns.dt.floor('1h')          # round down\ns.dt.ceil('1h')\ns.dt.normalize()          # set time to midnight\n</code></pre>"},{"location":"data/pandas/datetimes/#naive-vs-aware-the-one-rule","title":"Naive vs aware \u2014 the one rule","text":"<p>Naive and aware timestamps cannot be mixed</p> <p>Comparing or combining a naive <code>datetime64</code> with a tz-aware one raises <code>TypeError</code> at every layer (Python, NumPy, pandas). The fix is to commit to UTC from the moment data is loaded \u2014 pass <code>utc=True</code> to <code>pd.to_datetime()</code> and never produce naive datetimes for time-series data.</p> <p>Naive and aware timestamps cannot be compared or combined \u2014 this holds at every layer (Python, NumPy, pandas). Pick one convention (UTC throughout is simplest) and apply it consistently from the moment data is loaded.</p>"},{"location":"data/pandas/datetimes/#conversions","title":"Conversions","text":"<pre><code>ts.to_pydatetime()                   # pd.Timestamp \u2192 datetime.datetime\npd.Timestamp(datetime_obj)           # datetime.datetime \u2192 pd.Timestamp\nts.to_datetime64()                   # pd.Timestamp \u2192 np.datetime64[ns]\npd.Timestamp(np.datetime64('...'))   # np.datetime64 \u2192 pd.Timestamp\n</code></pre>"},{"location":"data/pandas/datetimes/#resampling-datetimeindex-as-time-axis","title":"Resampling \u2014 DatetimeIndex as time axis","text":"<pre><code>(df\n .set_index('date')\n .resample('1D').agg({'close': 'last', 'volume': 'sum'})\n)\n</code></pre> <p>Common aliases: <code>'1min'</code>, <code>'1h'</code>, <code>'1D'</code>, <code>'1W'</code>, <code>'1ME'</code> (month-end), <code>'1QE'</code> (quarter-end).</p>"},{"location":"data/pandas/datetimes/#quick-reference","title":"Quick reference","text":"Need Code Parse strings <code>pd.to_datetime(s)</code> Parse Unix ms (Binance) <code>pd.to_datetime(s, unit='ms', utc=True)</code> Extract year/month/day <code>s.dt.year</code> / <code>.dt.month</code> / <code>.dt.day</code> Attach timezone <code>s.dt.tz_localize('UTC')</code> Convert timezone <code>s.dt.tz_convert('America/New_York')</code> Pass to Python lib <code>ts.to_pydatetime()</code> Missing value <code>pd.NaT</code> Time-based groupby <code>.resample('1D')</code> on DatetimeIndex"},{"location":"data/pandas/dtypes/","title":"dtypes","text":"<p>Pandas wraps NumPy dtypes for numeric columns, then adds extension types that fix NumPy's practical gaps: no nullable integers, slow strings, and no categorical type.</p>"},{"location":"data/pandas/dtypes/#inspecting","title":"Inspecting","text":"<pre><code>df.dtypes                    # Series: column \u2192 dtype\ndf.dtypes.value_counts()     # how many columns of each type\ndf.memory_usage(deep=True)   # actual bytes; deep=True measures object columns correctly\n</code></pre>"},{"location":"data/pandas/dtypes/#the-string-problem-object-dtype","title":"The string problem: <code>object</code> dtype","text":"<p>Without explicit configuration, pandas stores string columns as NumPy <code>object</code> arrays \u2014 each cell is a Python <code>str</code> on the heap. No contiguous memory, no vectorisation, large memory footprint.</p> <pre><code>df['name'].dtype    # dtype('O')  \u2190 object\n\n# Fix: use StringDtype (pandas 1.0+)\ndf['name'] = df['name'].astype('string')\ndf['name'].dtype    # StringDtype()\n</code></pre> <p><code>StringDtype</code> is backed by a more efficient Arrow or NumPy structure and propagates <code>pd.NA</code> for missing values (not <code>np.nan</code>).</p>"},{"location":"data/pandas/dtypes/#nullable-integer-types","title":"Nullable integer types","text":"<p>Plain NumPy <code>int64</code> cannot hold <code>NaN</code>. Any integer column with a missing value silently becomes <code>float64</code> in old pandas. The nullable extension integers fix this \u2014 capital I:</p> <pre><code>pd.array([1, 2, None], dtype='Int64')   # Int64 \u2014 nullable, missing = pd.NA\npd.array([1, 2, None], dtype='int64')   # float64 \u2014 lowercase = NumPy, None\u2192NaN\n</code></pre> <p>Capital-I types: <code>Int8</code>, <code>Int16</code>, <code>Int32</code>, <code>Int64</code>, <code>UInt8</code>, \u2026, <code>BooleanDtype</code>.</p>"},{"location":"data/pandas/dtypes/#pdna-vs-npnan-vs-none","title":"<code>pd.NA</code> vs <code>np.nan</code> vs <code>None</code>","text":"<p>Three missing-value sentinels that don't compare equal</p> <p><code>None</code>, <code>np.nan</code>, and <code>pd.NA</code> are distinct objects with different types and boolean semantics. Mixing them in the same column (e.g. after a join) produces silent inconsistencies in <code>.isna()</code> and boolean indexing. The modern pandas convention is <code>pd.NA</code> for extension types (<code>Int64</code>, <code>StringDtype</code>, <code>BooleanDtype</code>) and <code>np.nan</code> only for plain <code>float64</code> columns.</p> Value Type Used in <code>None</code> Python object <code>object</code> columns, Python containers <code>np.nan</code> float64 NumPy, float columns, old pandas <code>pd.NA</code> NAType Nullable extension types (Int64, StringDtype, BooleanDtype) <p>They don't compare equal and behave differently in boolean contexts. Don't mix them across columns.</p>"},{"location":"data/pandas/dtypes/#categoricaldtype","title":"<code>CategoricalDtype</code>","text":"<p>For low-cardinality string or integer columns (country, status, grade): stores integer codes + a small lookup table instead of full strings per row.</p> <pre><code>df['status'] = df['status'].astype('category')\n\n# Ordered categorical \u2014 enables comparisons\nfrom pandas import CategoricalDtype\nsize_type = CategoricalDtype(['S', 'M', 'L', 'XL'], ordered=True)\ndf['size'] = df['size'].astype(size_type)\ndf[df['size'] &gt; 'M']   # works because ordered=True\n</code></pre> <p>Benefits: lower memory, faster <code>groupby</code> and <code>value_counts</code> (operate on integer codes).</p>"},{"location":"data/pandas/dtypes/#dtype-fix-up-pattern-after-loading","title":"Dtype fix-up pattern (after loading)","text":"<pre><code># Downcast numerics\ndf['price'] = df['price'].astype('float32')   # if float64 precision not needed\ndf['count'] = df['count'].astype('int32')\n\n# Fix string columns\nfor col in df.select_dtypes('object').columns:\n    df[col] = df[col].astype('string')\n\n# Fix low-cardinality columns\nfor col in ['country', 'status', 'tier']:\n    df[col] = df[col].astype('category')\n</code></pre>"},{"location":"data/pandas/dtypes/#interop-with-numpy","title":"Interop with NumPy","text":"<p>NumPy-backed columns (standard int/float/bool) share memory; <code>.to_numpy()</code> returns a view:</p> <pre><code>arr = df['price'].to_numpy()   # no copy for NumPy-backed columns\n</code></pre> <p>Extension type columns (Int64, StringDtype, Categorical) are not backed by plain NumPy arrays. <code>.to_numpy()</code> allocates a new array, often <code>dtype=object</code>. Be explicit:</p> <pre><code>df['count'].to_numpy(dtype=float, na_value=np.nan)   # converts pd.NA \u2192 np.nan\n</code></pre>"},{"location":"data/pandas/dtypes/#gotchas","title":"Gotchas","text":"<ul> <li><code>object</code> dtype silently defeats all pandas/NumPy performance \u2014 always check after loading CSV or joining.</li> <li><code>float32</code> has only ~7 significant digits; if you're accumulating sums or doing statistical calculations, stick with <code>float64</code>.</li> <li>Mixing <code>pd.NA</code> and <code>np.nan</code> in the same column produces inconsistent <code>.isna()</code> behaviour in some older pandas versions.</li> </ul>"},{"location":"data/pandas/indexing/","title":"Pandas \u2014 Indexing, Views and Copies","text":""},{"location":"data/pandas/indexing/#settingwithcopywarning","title":"SettingWithCopyWarning","text":"<p>Triggered by chained indexing used for assignment: two <code>[]</code> operations where the first may return a copy.</p> <pre><code>df[df['age'] &gt; 18]['name'] = 'adult'   # WARNING \u2014 assignment likely lost\n</code></pre> <p>Step 1 (<code>df[df['age'] &gt; 18]</code>) returns a new object (view or copy \u2014 not guaranteed). Step 2 writes to that object. If it's a copy, <code>df</code> is unchanged.</p> <p>Why view vs copy is unpredictable: NumPy returns a view for contiguous slices, a copy for boolean masks and fancy integer indexing. Pandas can't tell you which you got.</p>"},{"location":"data/pandas/indexing/#the-fix-loc-for-any-write","title":"The fix: <code>.loc</code> for any write","text":"<p><code>.loc[row_mask, col]</code> is a single operation directly on the original \u2014 no intermediate object.</p> <pre><code>df.loc[df['age'] &gt; 18, 'name'] = 'adult'   # correct\n</code></pre> <p>Reading can chain; writing must use .loc on the original</p> <p><code>df[mask]['col'] = v</code> is two separate operations. The first may return a copy, and the write lands on that copy \u2014 <code>df</code> is silently unchanged. <code>.loc[mask, 'col'] = v</code> is one operation and always modifies <code>df</code> directly. This applies under both old pandas and Copy-on-Write semantics.</p>"},{"location":"data/pandas/indexing/#explicit-copy-for-an-independent-subset","title":"Explicit <code>.copy()</code> for an independent subset","text":"<pre><code>subset = df[df['age'] &gt; 18].copy()   # explicit intent: separate object\nsubset['name'] = 'adult'             # safe, no warning\n</code></pre>"},{"location":"data/pandas/indexing/#decision-table","title":"Decision table","text":"Intent Pattern Correct? Read from subset <code>df[mask]['col'].mean()</code> Yes Modify original <code>df.loc[mask, 'col'] = v</code> Yes Work with a separate copy <code>sub = df[mask].copy(); sub['col'] = v</code> Yes Modify original (wrong way) <code>df[mask]['col'] = v</code> No \u2014 warning"},{"location":"data/pandas/indexing/#pandas-20-copy-on-write-cow","title":"Pandas 2.0+ Copy-on-Write (CoW)","text":"<p>CoW (default in pandas 3.0) makes every indexing result a copy \u2014 view-mutation is gone, the warning disappears. Code that wrote to a slice expecting to modify the original silently breaks. Fix: <code>.loc</code> on the original, which is correct under both old and CoW semantics.</p>"},{"location":"finance/","title":"Finance","text":"<p>Domain-specific notes on trading, exchanges, and market data APIs.</p> File Type Description binance.md note Binance exchange mechanics, top coins, klines API column reference market-data-apis.md ref Free public APIs for FX rates and stock/crypto OHLCV"},{"location":"finance/binance/","title":"Binance","text":"<p>World's largest crypto exchange by volume. A centralised exchange (CEX): Binance holds custody of funds; trades update an internal database, not a blockchain. For data purposes only the exchange layer matters \u2014 the ecosystem (BNB Chain, NFTs, staking) is irrelevant.</p> <p>See market-data-apis.md for the quick-reference curl snippet.</p>"},{"location":"finance/binance/#market-mechanics","title":"Market mechanics","text":"<p>Order book: every market has a sorted list of open bids (buy orders) and asks (sell orders). Market orders fill immediately against the best available price; limit orders rest in the book.</p> <p>Maker vs taker: maker places a limit order that adds liquidity (lower fee ~0.02\u20130.1%); taker hits the book immediately (higher fee ~0.05\u20130.1%). The klines API exposes taker-buy volume separately, which serves as a proxy for buying aggression.</p> <p>Spot vs Perpetual Futures:</p> Spot Perpetual (PERP) You own Actual crypto A contract Leverage 1\u00d7 Up to 125\u00d7 Expiry None None Price anchor N/A Funding rate every 8 h API host <code>api.binance.com</code> <code>fapi.binance.com</code> <p>Most of Binance's volume sits in perpetual futures, not spot.</p> <p>Symbol format: <code>BASE + QUOTE</code> \u2014 e.g. <code>BTCUSDT</code> = Bitcoin priced in USDT. Most liquid pairs are <code>*USDT</code>.</p>"},{"location":"finance/binance/#five-most-traded-cryptos-spot-volume-20232025","title":"Five most-traded cryptos (spot volume, 2023\u20132025)","text":"# Symbol Name Notes 1 BTC Bitcoin Dominant; institutional flows 2 ETH Ethereum Smart contracts platform; DeFi hub 3 BNB Binance Coin Native token; used for fee discounts 4 SOL Solana High-throughput L1; large DeFi/NFT ecosystem 5 XRP Ripple Cross-border payments; large retail following <p>USDT appears everywhere as the quote currency, not as a traded base asset.</p>"},{"location":"finance/binance/#klines-ohlcv-api-column-reference","title":"Klines (OHLCV) API \u2014 column reference","text":"<pre><code>GET https://api.binance.com/api/v3/klines\n    ?symbol=BTCUSDT&amp;interval=1d&amp;limit=500\n</code></pre> <p>Returns an array of arrays. Each inner array has 12 elements (prices/volumes are strings \u2014 cast to float):</p> Index Field Type Description 0 <code>open_time</code> int Open timestamp (Unix ms) 1 <code>open</code> str Open price 2 <code>high</code> str High price 3 <code>low</code> str Low price 4 <code>close</code> str Close price 5 <code>volume</code> str Base asset volume (e.g. BTC count) 6 <code>close_time</code> int Close timestamp (Unix ms) 7 <code>quote_asset_volume</code> str Quote asset volume (e.g. total USDT value) 8 <code>number_of_trades</code> int Trade count 9 <code>taker_buy_base_asset_volume</code> str Base volume bought by takers 10 <code>taker_buy_quote_asset_volume</code> str Quote value of taker buys 11 <code>ignore</code> str Always <code>\"0\"</code> \u2014 discard <p>Buy ratio as a signal for buying aggression</p> <p><code>taker_buy_base_asset_volume / volume</code> (columns 9 \u00f7 5) gives the fraction of volume initiated by buyers. Values consistently above 0.5 indicate more aggressive buying; below 0.5 indicates selling pressure. This is a commonly used proxy in crypto market microstructure analysis.</p> <p>Key distinctions:</p> <ul> <li><code>volume</code> (5) = how many BTC changed hands. <code>quote_asset_volume</code> (7) = total USDT value \u2014 use this for cross-market comparisons.</li> <li><code>taker_buy_base_asset_volume</code> (9) \u00f7 <code>volume</code> (5) = buy ratio. Above 0.5 \u2192 more buying aggression.</li> <li><code>open_time</code> is the candle start (midnight UTC for 1d); <code>close_time</code> = open_time + interval \u2212 1 ms.</li> </ul> <p>Valid intervals: <code>1m 3m 5m 15m 30m 1h 2h 4h 6h 8h 12h 1d 3d 1w 1M</code> Max <code>limit</code>: 1000 per request. Paginate with <code>startTime</code> / <code>endTime</code> (Unix ms) for longer histories.</p>"},{"location":"finance/binance/#canonical-pandas-loader","title":"Canonical pandas loader","text":"<pre><code>import requests, pandas as pd\n\ndef get_klines(symbol: str, interval: str = \"1d\", limit: int = 500) -&gt; pd.DataFrame:\n    r = requests.get(\n        \"https://api.binance.com/api/v3/klines\",\n        params={\"symbol\": symbol, \"interval\": interval, \"limit\": limit},\n    )\n    r.raise_for_status()\n    cols = [\n        \"open_time\", \"open\", \"high\", \"low\", \"close\", \"volume\",\n        \"close_time\", \"quote_vol\", \"trades\",\n        \"taker_buy_vol\", \"taker_buy_quote_vol\", \"ignore\",\n    ]\n    df = pd.DataFrame(r.json(), columns=cols)\n    df[\"open_time\"] = pd.to_datetime(df[\"open_time\"], unit=\"ms\", utc=True)  # see data/pandas/datetimes.md\n    df.set_index(\"open_time\", inplace=True)\n    df.drop(columns=[\"close_time\", \"ignore\"], inplace=True)\n    float_cols = [\"open\",\"high\",\"low\",\"close\",\"volume\",\n                  \"quote_vol\",\"taker_buy_vol\",\"taker_buy_quote_vol\"]\n    df[float_cols] = df[float_cols].astype(float)\n    df[\"trades\"] = df[\"trades\"].astype(int)\n    return df\n</code></pre>"},{"location":"finance/binance/#other-useful-endpoints-no-key-required","title":"Other useful endpoints (no key required)","text":"<pre><code>GET /api/v3/ticker/price?symbol=BTCUSDT      # latest price\nGET /api/v3/ticker/24hr?symbol=BTCUSDT       # 24 h rolling stats\nGET /api/v3/depth?symbol=BTCUSDT&amp;limit=20    # order book\nGET /api/v3/exchangeInfo                     # all symbols + trading rules\nGET https://fapi.binance.com/fapi/v1/klines  # futures klines (same schema)\n</code></pre>"},{"location":"finance/binance/#rate-limits","title":"Rate limits","text":"<ul> <li>Klines: 1\u20132 weight per call. Budget: 1200 weight/min.</li> <li>Check <code>X-MBX-USED-WEIGHT-1M</code> response header.</li> <li>HTTP 429 = rate-limited; HTTP 418 = IP banned.</li> </ul>"},{"location":"finance/binance/#caveats","title":"Caveats","text":"<ul> <li>Crypto only \u2014 no equities, FX, or commodities.</li> <li>Data starts mid-2017 (Binance launch). For older BTC history use Kraken/Bitstamp.</li> <li>Self-reported volume \u2014 wash trading is not independently audited.</li> </ul>"},{"location":"finance/market-data-apis/","title":"Free Public Market Data APIs","text":""},{"location":"finance/market-data-apis/#quick-reference","title":"Quick reference","text":"Use case API Key required? EUR-based FX, daily Frankfurter No Broad FX coverage, daily fawazahmed0/currency-api No Stock OHLCV yfinance (Yahoo) No (unofficial) Stock OHLCV, official Alpha Vantage Free signup Crypto OHLCV, high-volume Binance klines No Crypto OHLCV, broad coin list CoinGecko No (rate limited)"},{"location":"finance/market-data-apis/#fx-rates","title":"FX rates","text":""},{"location":"finance/market-data-apis/#frankfurter-ecb-daily-rates-no-key","title":"Frankfurter \u2014 ECB daily rates, no key","text":"<pre><code>curl \"https://api.frankfurter.app/latest?from=EUR&amp;to=USD,GBP\"\ncurl \"https://api.frankfurter.app/2024-01-01..2024-03-31?from=USD&amp;to=EUR\"\n</code></pre> <p>~30 ECB-published currencies. Weekday updates only. No exotic EM, no crypto.</p>"},{"location":"finance/market-data-apis/#fawazahmed0currency-api-170-currencies-cdn-hosted","title":"fawazahmed0/currency-api \u2014 ~170 currencies, CDN-hosted","text":"<pre><code>https://cdn.jsdelivr.net/npm/@fawazahmed0/currency-api@latest/v1/currencies/usd.json\nhttps://cdn.jsdelivr.net/npm/@fawazahmed0/currency-api@2024-03-15/v1/currencies/usd.json\n</code></pre> <p>Mid-rates only, daily. No key. Includes some crypto pairs.</p>"},{"location":"finance/market-data-apis/#stock-ohlcv","title":"Stock OHLCV","text":""},{"location":"finance/market-data-apis/#yfinance-de-facto-standard","title":"yfinance (de facto standard)","text":"<pre><code>import yfinance as yf\n\ndf = yf.Ticker(\"AAPL\").history(period=\"3mo\")           # daily\ndf = yf.Ticker(\"AAPL\").history(period=\"5d\", interval=\"1h\")  # intraday\ndata = yf.download([\"AAPL\", \"SPY\"], start=\"2024-01-01\")     # multiple tickers\n</code></pre> <p>Returns a pandas DataFrame: <code>Open High Low Close Volume</code>. No key, but unofficial (reverse-engineered Yahoo Finance). Intraday limited to ~60 days.</p>"},{"location":"finance/market-data-apis/#alpha-vantage-free-key-25-reqday","title":"Alpha Vantage \u2014 free key, 25 req/day","text":"<pre><code>curl \"https://www.alphavantage.co/query?function=TIME_SERIES_DAILY_ADJUSTED&amp;symbol=IBM&amp;outputsize=compact&amp;apikey=KEY\"\n</code></pre> <p>Register free at alphavantage.co. <code>outputsize=compact</code> \u2192 last 100 days.</p>"},{"location":"finance/market-data-apis/#crypto-ohlcv","title":"Crypto OHLCV","text":""},{"location":"finance/market-data-apis/#binance-klines-no-key-best-rate-limits","title":"Binance klines \u2014 no key, best rate limits","text":"<p>See binance.md for full column reference, pagination, and market mechanics.</p> <pre><code>curl \"https://api.binance.com/api/v3/klines?symbol=BTCUSDT&amp;interval=1d&amp;limit=90\"\n</code></pre> <p>Intervals: <code>1m 5m 15m 1h 4h 1d 1w</code>. Response is array of arrays: <code>[open_time_ms, open, high, low, close, volume, ...]</code></p> <pre><code>import requests, pandas as pd\n\nr = requests.get(\"https://api.binance.com/api/v3/klines\",\n                 params={\"symbol\": \"BTCUSDT\", \"interval\": \"1d\", \"limit\": 90})\ncols = [\"open_time\",\"open\",\"high\",\"low\",\"close\",\"volume\",\n        \"close_time\",\"quote_vol\",\"trades\",\"_\",\"__\",\"___\"]\ndf = pd.DataFrame(r.json(), columns=cols)\ndf[\"open_time\"] = pd.to_datetime(df[\"open_time\"], unit=\"ms\")\ndf[[\"open\",\"high\",\"low\",\"close\",\"volume\"]] = df[[\"open\",\"high\",\"low\",\"close\",\"volume\"]].astype(float)\n</code></pre>"},{"location":"finance/market-data-apis/#coingecko-10000-coins-rate-limited","title":"CoinGecko \u2014 10,000+ coins, rate-limited","text":"<pre><code>curl \"https://api.coingecko.com/api/v3/coins/bitcoin/ohlc?vs_currency=usd&amp;days=30\"\ncurl \"https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum&amp;vs_currencies=usd\"\n</code></pre> <p>Coin IDs are slugs (<code>bitcoin</code>, <code>ethereum</code>). Free tier: ~10\u201330 req/min \u2014 add <code>time.sleep(2)</code> in loops.</p>"},{"location":"finance/market-data-apis/#gotchas","title":"Gotchas","text":"<p>Timestamp units differ across every API</p> <p>Binance returns Unix milliseconds, Kraken returns Unix seconds, Yahoo Finance returns pandas <code>Timestamps</code>, and CoinGecko mixes formats. Always check and convert explicitly \u2014 passing milliseconds where seconds are expected silently produces dates in the year 2554.</p> <ul> <li>Timestamps: Binance = Unix ms, Kraken = Unix s, Yahoo = pandas Timestamps \u2014 parse carefully.</li> <li>Intraday data is usually restricted to recent history on free tiers.</li> <li>Survivorship bias: delisted tickers are inconsistent across free APIs.</li> <li>Store raw responses \u2014 free-tier quotas run out and replaying calls is wasteful.</li> </ul>"},{"location":"git/","title":"Git","text":"File Type Description git.md ref Core workflow: init, stage, commit, push, pull tags-releases.md note Tags (lightweight vs annotated), semver, GitHub releases, release workflow github-actions.md note CI/CD with GitHub Actions: workflows, jobs, triggers, matrix, caching"},{"location":"git/git/","title":"Git \u2014 Basic Usage","text":""},{"location":"git/git/#what-is-git","title":"What is Git?","text":"<p>Git is a distributed version control system used to track changes in code and collaborate safely.</p>"},{"location":"git/git/#typical-workflow","title":"Typical Workflow","text":"<pre><code># Create or enter a project\nmkdir my_project &amp;&amp; cd my_project\n\n# Initialize a git repository\ngit init\n\n# Check current status\ngit status\n\n# Add files to staging\ngit add file.py\ngit add .\n\n# Save a snapshot (commit)\ngit commit -m \"Initial commit\"\n\n# View commit history\ngit log --oneline\n\n# Connect to remote repository\ngit remote add origin &lt;repo-url&gt;\n\n# Upload code\ngit push -u origin main\n\n# Download latest changes\ngit pull\n</code></pre>"},{"location":"git/github-actions/","title":"GitHub Actions &amp; CI","text":"<p>Continuous Integration (CI): automatically run tests, linters, and type checkers on every push. Failures surface immediately while context is fresh.</p> <p>GitHub Actions is GitHub's built-in CI/CD platform. Workflows are YAML files in <code>.github/workflows/</code> that GitHub runs on managed VMs called runners.</p>"},{"location":"git/github-actions/#core-hierarchy","title":"Core hierarchy","text":"<pre><code>Workflow (.yml file, triggered by events)\n\u2514\u2500\u2500 Job (runs on one runner; jobs run in parallel by default)\n    \u2514\u2500\u2500 Step (a shell command or a reusable Action; steps run sequentially)\n</code></pre> <ul> <li>Runner: a fresh VM (<code>ubuntu-latest</code>, etc.) spun up per job.</li> <li>Action: a reusable step, referenced as <code>owner/repo@version</code>.</li> </ul>"},{"location":"git/github-actions/#minimal-python-ci-workflow","title":"Minimal Python CI workflow","text":"<pre><code># .github/workflows/ci.yml\nname: CI\non: [push, pull_request]\n\njobs:\n  ci:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n\n      - uses: actions/setup-python@v5\n        with:\n          python-version: \"3.12\"\n\n      - name: Cache Poetry\n        uses: actions/cache@v4\n        with:\n          path: ~/.cache/pypoetry\n          key: ${{ runner.os }}-poetry-${{ hashFiles('poetry.lock') }}\n\n      - run: pip install poetry &amp;&amp; poetry install\n      - run: poetry run ruff check .\n      - run: poetry run mypy .\n      - run: poetry run pytest --tb=short\n</code></pre>"},{"location":"git/github-actions/#triggers-on","title":"Triggers (<code>on:</code>)","text":"Trigger When <code>push</code> Any push (filter with <code>branches:</code>, <code>paths:</code>) <code>pull_request</code> PR open, sync, reopen <code>schedule</code> Cron: <code>cron: \"0 8 * * 1\"</code> (Mon 8am UTC) <code>workflow_dispatch</code> Manual trigger from the GitHub UI <code>release</code> When a GitHub Release is published <pre><code>on:\n  push:\n    branches: [main, \"release/**\"]\n    paths: [\"src/**\", \"tests/**\"]   # skip if only docs changed\n</code></pre>"},{"location":"git/github-actions/#job-dependencies-sequencing","title":"Job dependencies (sequencing)","text":"<p>Jobs run in parallel unless <code>needs:</code> declares a dependency:</p> <pre><code>jobs:\n  lint:\n    runs-on: ubuntu-latest\n    steps: ...\n\n  test:\n    needs: lint          # waits for lint to pass\n    runs-on: ubuntu-latest\n    steps: ...\n\n  deploy:\n    needs: [lint, test]\n    runs-on: ubuntu-latest\n    steps: ...\n</code></pre>"},{"location":"git/github-actions/#secrets-and-environment-variables","title":"Secrets and environment variables","text":"<pre><code>steps:\n  - run: deploy.sh\n    env:\n      API_KEY: ${{ secrets.MY_API_KEY }}   # from repo Settings \u2192 Secrets\n      ENV: production\n</code></pre> <p><code>GITHUB_TOKEN</code> is auto-created per run with permission to push, comment on PRs, etc. \u2014 no setup required.</p>"},{"location":"git/github-actions/#matrix-builds","title":"Matrix builds","text":"<p>Fan out a job across multiple parameter combinations:</p> <pre><code>strategy:\n  matrix:\n    python-version: [\"3.10\", \"3.11\", \"3.12\"]\nsteps:\n  - uses: actions/setup-python@v5\n    with:\n      python-version: ${{ matrix.python-version }}\n</code></pre>"},{"location":"git/github-actions/#caching-dependencies","title":"Caching dependencies","text":"<p>Runners start fresh. Cache to avoid re-installing on every run:</p> <pre><code>- uses: actions/cache@v4\n  with:\n    path: ~/.cache/pypoetry/virtualenvs\n    key: ${{ runner.os }}-poetry-${{ hashFiles('poetry.lock') }}\n</code></pre> <p>Cache is invalidated when <code>poetry.lock</code> changes.</p>"},{"location":"git/github-actions/#artifacts","title":"Artifacts","text":"<p>Share files between jobs or download after a run (kept 90 days):</p> <pre><code>- uses: actions/upload-artifact@v4\n  with:\n    name: coverage-report\n    path: htmlcov/\n</code></pre>"},{"location":"git/github-actions/#useful-patterns","title":"Useful patterns","text":"<p>Workflow files are code \u2014 they only change on the next trigger</p> <p>A workflow YAML edit takes effect the next time the triggering event fires. There's no \"reload\" \u2014 if you're debugging a failing push trigger, you must push again after each fix. This also means accidental changes to CI are immediately versioned and revertable via <code>git revert</code>.</p> <ul> <li>Skip a step conditionally: <code>if: github.ref == 'refs/heads/main'</code></li> <li>Don't fail on step error: <code>continue-on-error: true</code></li> <li>Deployment gate: set <code>environment: production</code> on a job \u2192 requires manual approval in GitHub UI</li> <li>Workflow files are versioned code \u2014 changes take effect on the next trigger</li> </ul>"},{"location":"git/github-actions/#related-notes","title":"Related notes","text":"<ul> <li>git.md \u2014 branching model that CI enforces</li> <li>testing-strategy.md \u2014 what CI runs: pytest, coverage</li> <li>ruff.md \u2014 linter CI runs on every push</li> <li>mypy.md \u2014 type checker CI runs on every push</li> <li>poetry.md \u2014 dependency management; <code>poetry install</code> in CI</li> </ul>"},{"location":"git/tags-releases/","title":"Git Tags and Releases","text":""},{"location":"git/tags-releases/#tags","title":"Tags","text":"<p>A tag is a named, immutable pointer to a specific commit. Unlike branches, tags don't move. They mark a commit as meaningful \u2014 typically a version release.</p>"},{"location":"git/tags-releases/#lightweight-vs-annotated","title":"Lightweight vs annotated","text":"<pre><code>git tag v1.0.0                          # lightweight: just a pointer\ngit tag -a v1.0.0 -m \"First release\"   # annotated: full Git object with metadata\n</code></pre> <p>Prefer annotated tags \u2014 they store author, date, and message, and appear in <code>git describe</code>.</p>"},{"location":"git/tags-releases/#common-operations","title":"Common operations","text":"<pre><code>git tag                        # list all tags\ngit tag -l \"v1.*\"              # filter by pattern\ngit show v1.0.0                # show tag metadata + commit\n\ngit tag -a v0.9.0 e636bb2 -m \"Beta\"   # tag a past commit\n\ngit push origin v1.0.0         # push one tag (not automatic!)\ngit push origin --tags         # push all tags\n\ngit tag -d v1.0.0                       # delete locally\ngit push origin --delete v1.0.0         # delete on remote\n</code></pre> <p>Tags are not pushed by git push \u2014 you must push them explicitly</p> <p><code>git push</code> only sends commits and branch refs. Tags are ignored unless you add <code>--tags</code> (all tags) or name one explicitly: <code>git push origin v1.0.0</code>. Forgetting this is a common cause of \"tag exists locally but not on GitHub\" confusion.</p> <p>Tags are not pushed with <code>git push</code> \u2014 you must push them explicitly.</p>"},{"location":"git/tags-releases/#semantic-versioning","title":"Semantic versioning","text":"<p>Tags follow <code>vMAJOR.MINOR.PATCH</code> by convention:</p> Increment When <code>MAJOR</code> Breaking change <code>MINOR</code> New backward-compatible feature <code>PATCH</code> Bug fix"},{"location":"git/tags-releases/#releases","title":"Releases","text":"<p>A release is a platform layer on top of a tag (e.g. GitHub Releases). It adds: - Human-readable release notes / changelog - Downloadable build artifacts (binaries, wheels, etc.) - A <code>release</code> event that CI/CD pipelines can react to</p> <p>The tag is the Git-native concept; the release is the presentation layer for users and automation.</p> <pre><code>gh release create v1.0.0 --generate-notes          # auto-generate notes from commits\ngh release create v1.0.0 ./dist/app --title \"v1.0.0\"  # attach a build artifact\n</code></pre>"},{"location":"git/tags-releases/#typical-release-workflow","title":"Typical release workflow","text":"<pre><code># 1. Tag the release commit\ngit tag -a v1.2.0 -m \"Release v1.2.0\"\n\n# 2. Push the tag\ngit push origin v1.2.0\n\n# 3. Create the GitHub release\ngh release create v1.2.0 --generate-notes\n</code></pre>"},{"location":"python/","title":"Python","text":"Directory Description language/ Core Python: data model, iterators, generators, exceptions, stdlib tooling/ Environment, packaging, linting, type checking, testing"},{"location":"python/language/","title":"Python \u2014 Language","text":"Directory Description functional/ Iteration and functional patterns: comprehensions, generators, itertools, functools, operator objects/ Object model and type system: data model, OOP, hash, exceptions runtime/ Execution model and stdlib utilities: concurrency, imports, context managers, CLI, datetime"},{"location":"python/language/functional/","title":"Python \u2014 Language / Functional","text":"File Type Description comprehensions.md note List, dict, set comprehensions \u2014 when each is more readable than a loop functools.md note Decorators, <code>lru_cache</code>, <code>partial</code>, <code>functools.wraps</code> iterators-generators.md note Iterables, generators, <code>yield</code>, <code>itertools</code> itertools/ \u2014 <code>accumulate</code>, <code>chain</code>, <code>islice</code>, <code>product</code>, <code>combinations</code>, <code>groupby</code> lazy-evaluation.md note Lazy vs eager: memory tradeoffs, sinks, when each wins, NumPy comparison operator.md note attrgetter, itemgetter, methodcaller \u2014 faster and clearer than lambdas in sort keys string-formatting.md ref String formatting: f-strings, str.format(), Template, format spec mini-language unpacking.md note <code>*x</code> and <code>**x</code> operators: protocols, contexts (calls, literals, assignment)"},{"location":"python/language/functional/comprehensions/","title":"Comprehensions","text":"<p>Comprehensions build a new collection by mapping and/or filtering an iterable in a single expression. Use them when the intent is transformation or filtering; use a loop when the body has side effects, accumulates multiple things, or is too complex to read in one line.</p>"},{"location":"python/language/functional/comprehensions/#list-comprehensions","title":"List comprehensions","text":"<pre><code>squares = [x**2 for x in range(10)]\n</code></pre> <p>Filter (if at the end) \u2014 excludes items: <pre><code>positives = [x for x in data if x &gt; 0]\n</code></pre></p> <p>Ternary (if/else in the expression) \u2014 transforms all items: <pre><code>clamped = [x if x &gt; 0 else 0 for x in data]\n</code></pre></p> <p>Mixing both is at the readability limit: <pre><code>result = [x if x &gt; 0 else 0 for x in data if x is not None]\n</code></pre></p>"},{"location":"python/language/functional/comprehensions/#nested-comprehensions","title":"Nested comprehensions","text":"<p>Two <code>for</code> clauses, read left-to-right (outer loop first):</p> <pre><code># Flatten a 2D list\nflat = [x for row in matrix for x in row]\n\n# Cartesian product\npairs = [(x, y) for x in [1, 2] for y in ['a', 'b']]\n</code></pre> <p>One level of nesting is usually fine; two or more levels \u2014 use a loop.</p> <p>A nested comprehension of comprehensions produces a 2D list (different from flattening): <pre><code>transposed = [[row[i] for row in matrix] for i in range(len(matrix[0]))]\n# but prefer: list(zip(*matrix))\n</code></pre></p>"},{"location":"python/language/functional/comprehensions/#dict-comprehensions","title":"Dict comprehensions","text":"<pre><code>word_lengths = {w: len(w) for w in words}          # from iterable\nuppercased   = {k: v.upper() for k, v in d.items()} # transform dict\nactive       = {k: v for k, v in d.items() if v['active']}  # filter dict\ninverted     = {v: k for k, v in d.items()}          # invert (unique values only)\n</code></pre> <p>Use a loop when the value needs multiple steps or key collisions need explicit handling.</p>"},{"location":"python/language/functional/comprehensions/#set-comprehensions","title":"Set comprehensions","text":"<pre><code>unique_lengths = {len(w) for w in words}\ninitials       = {name[0].upper() for name in names if name}\n</code></pre> <p>Identical to list comprehensions but deduplicate automatically. Prefer over <code>set(... for ...)</code> for consistency with dict syntax.</p>"},{"location":"python/language/functional/comprehensions/#when-to-use-a-loop-instead","title":"When to use a loop instead","text":"Situation Reason Side effects (print, write, mutate) Comprehensions imply no side effects Multiple accumulations One comprehension = one output Multi-step value computation Can't use intermediate variables Per-item error handling <code>try/except</code> can't go inside &gt; 2 <code>for</code> clauses Loop order becomes confusing <code>break</code> / <code>continue</code> flow control Not expressible in a comprehension"},{"location":"python/language/functional/comprehensions/#performance-note","title":"Performance note","text":"<p>List comprehensions are faster than <code>for</code> + <code>.append()</code> (the append method is resolved once, not per iteration), but readability is the primary reason to prefer them.</p> <p>Use a generator expression when you only need one pass</p> <p><code>[x**2 for x in range(1_000_000)]</code> allocates ~8 MB immediately. <code>sum(x**2 for x in range(1_000_000))</code> uses O(1) memory \u2014 the generator feeds values one at a time into <code>sum</code>, which never builds the list. Use <code>[...]</code> only when you need the list itself (indexing, multiple passes, <code>len</code>).</p> <p>For single-pass consumption, use a generator expression instead to avoid materialising the full list: <pre><code>total = sum(x**2 for x in range(1_000_000))\n</code></pre></p> <p>See iterators-generators.md for more on generators.</p>"},{"location":"python/language/functional/functools/","title":"Decorators and <code>functools</code>","text":""},{"location":"python/language/functional/functools/#decorators","title":"Decorators","text":"<p>A function that takes a function as input and returns a new function.</p>"},{"location":"python/language/functional/functools/#no-arguments-2-levels-of-nesting","title":"No arguments \u2014 2 levels of nesting","text":"<pre><code>import functools\n\ndef my_decorator(func):\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        ...\n        return func(*args, **kwargs)\n    return wrapper\n</code></pre> <p><code>@functools.wraps</code> preserves the metadata of the original function (name, docstring) instead of exposing the wrapper.</p>"},{"location":"python/language/functional/functools/#with-arguments-3-levels-of-nesting","title":"With arguments \u2014 3 levels of nesting","text":"<pre><code>def my_decorator(x_dec):                # 1. receives decorator arguments\n    def decorator(func):                # 2. receives the function being decorated\n        def wrapper(*args, **kwargs):   # 3. receives the function call arguments\n            ...\n            return func(*args, **kwargs)\n        return wrapper\n    return decorator\n</code></pre>"},{"location":"python/language/functional/functools/#memoization-with-lru_cache","title":"Memoization with <code>@lru_cache</code>","text":"<p>Memoisation caches the result of a function call keyed on its arguments. On a cache hit, the function body is never executed \u2014 the stored result is returned immediately.</p> <pre><code>from functools import lru_cache\n\n@lru_cache(maxsize=128)\ndef fib(n: int) -&gt; int:\n    return n if n &lt; 2 else fib(n - 1) + fib(n - 2)\n</code></pre> <p>LRU = Least Recently Used: when <code>maxsize</code> entries are stored, the least recently accessed entry is evicted to make room.</p>"},{"location":"python/language/functional/functools/#the-hashability-constraint","title":"The hashability constraint","text":"<p>The cache key is built from arguments exactly like a <code>dict</code> key \u2014 every argument must be hashable. Passing a <code>list</code>, <code>dict</code>, or <code>set</code> raises <code>TypeError</code> at call time.</p> <pre><code>@lru_cache(maxsize=128)\ndef process(data: tuple[float, ...]) -&gt; float: ...  # tuple \u2713\n# process([1.0, 2.0])  \u2192 TypeError: unhashable type: 'list'\nprocess((1.0, 2.0))    # \u2713\n</code></pre> <p>Use <code>tuple</code> instead of <code>list</code>, <code>frozenset</code> instead of <code>set</code>, <code>frozen=True</code> dataclasses instead of mutable ones.</p> <p>Only cache pure functions \u2014 impure caches silently return stale results</p> <p><code>@lru_cache</code> never re-calls the function for a seen input. If the result depends on a DB, file, clock, or any external state, the first result is frozen forever. There's no automatic invalidation; you'd have to call <code>.cache_clear()</code> manually. See the decision guide below.</p>"},{"location":"python/language/functional/functools/#the-determinism-constraint","title":"The determinism constraint","text":"<p>The cache never re-calls the function for a previously seen input. If the output can change for the same input \u2014 reads from a DB, file, clock, or RNG \u2014 the cached result silently goes stale.</p> <pre><code>@lru_cache(maxsize=None)\ndef get_price(symbol: str) -&gt; float:\n    return db.query(...)   # BAD: price moves, cache doesn't\n</code></pre> <p>Memoisation is only correct for pure functions: same inputs \u2192 same output, always, with no side effects.</p>"},{"location":"python/language/functional/functools/#cache-unbounded-variant","title":"<code>@cache</code> \u2014 unbounded variant","text":"<p><code>@functools.cache</code> (Python 3.9+) is <code>lru_cache(maxsize=None)</code> with no eviction and slightly less overhead. Safe for small, well-bounded input spaces; risky in long-running processes with unbounded inputs.</p> <pre><code>from functools import cache\n\n@cache\ndef fib(n: int) -&gt; int:\n    return n if n &lt; 2 else fib(n - 1) + fib(n - 2)\n</code></pre>"},{"location":"python/language/functional/functools/#introspection-and-invalidation","title":"Introspection and invalidation","text":"<pre><code>fib.cache_info()   # CacheInfo(hits=34, misses=10, maxsize=128, currsize=10)\nfib.cache_clear()  # wipe entire cache (no per-key invalidation in stdlib)\n</code></pre> <p>@lru_cache on instance methods leaks memory</p> <p>Because <code>self</code> is part of the cache key, the module-level cache dict holds a strong reference to every instance ever passed to the method. The instance can never be garbage collected. Use <code>@functools.cached_property</code> instead for computed attributes with no arguments \u2014 it stores the result on the instance itself and is collected with it.</p>"},{"location":"python/language/functional/functools/#pitfall-caching-instance-methods","title":"Pitfall: caching instance methods","text":"<p><code>self</code> is part of the cache key, so the module-level cache dict holds a strong reference to <code>self</code>, preventing garbage collection of instances.</p> <p>For a computed attribute with no arguments, use <code>@functools.cached_property</code> \u2014 it stores the result on the instance itself:</p> <pre><code>from functools import cached_property\n\nclass Pricer:\n    @cached_property\n    def fair_value(self) -&gt; float:\n        return heavy_computation(self.data)   # computed once, stored on self\n</code></pre>"},{"location":"python/language/functional/functools/#decision-guide","title":"Decision guide","text":"Situation Choice Pure function, unbounded inputs <code>@lru_cache(maxsize=N)</code> Pure function, small/fixed inputs <code>@cache</code> Computed instance attribute (no args) <code>@cached_property</code> DB / file / network read Do not cache at this layer Per-key expiry or invalidation needed <code>cachetools</code> library"},{"location":"python/language/functional/functools/#partial-application","title":"Partial Application","text":"<p><code>functools.partial</code> fixes some arguments of a callable, returning a new callable for the rest:</p> <pre><code>from functools import partial\n\nsquare = partial(pow, exp=2)\nsquare(3)  # 9\n</code></pre>"},{"location":"python/language/functional/iterators-generators/","title":"Iterators and Generators","text":"<p>Iteration is fundamental in data processing, especially to fetch data lazily (one at a time as needed; the opposite is eagerly). See lazy-evaluation.md for the full tradeoff analysis.</p>"},{"location":"python/language/functional/iterators-generators/#iterables-and-iterators","title":"Iterables and Iterators","text":"<ul> <li>Iterable: object that implements <code>__iter__</code>, which returns an iterator; if not available it falls back to <code>__getitem__</code>.</li> <li>Iterator: object that defines <code>__next__</code> (returning a value or <code>raise StopIteration</code>) and <code>__iter__</code> (returning self).</li> <li>Iterable objects used in:</li> <li><code>for</code> loops</li> <li>list, dict and set comprehensions (<code>[2*s for s in x]</code>)</li> <li>unpacking assignments (<code>*x</code>) \u2014 see unpacking.md for the full <code>*</code>/<code>**</code> operator reference</li> </ul>"},{"location":"python/language/functional/iterators-generators/#generators","title":"Generators","text":"<p>A generator is an iterator you write with yield instead of next</p> <p>A generator function suspends at each <code>yield</code>, preserving local state between calls. This lets you express complex iteration logic as sequential code without building a full collection in memory. The compiler converts the function body into a state machine automatically.</p> <ul> <li>A generator function is a function with the <code>yield</code> keyword (<code>return</code> not needed).</li> <li>It returns a generator object, which is an iterator built by the compiler.</li> <li>Alternatively, use a generator expression: <code>(2*s for s in x)</code>.</li> <li>Generators can be used to implement iterators.</li> <li><code>yield from</code> delegates to a subgenerator.</li> <li>Generators are a secret weapon for large datasets.</li> </ul> <pre><code>def evens(n):\n    for i in range(n):\n        if i % 2 == 0:\n            yield i\n\nlist(evens(10))  # [0, 2, 4, 6, 8]\n</code></pre>"},{"location":"python/language/functional/iterators-generators/#generators-in-the-standard-library","title":"Generators in the Standard Library","text":"<p>Avoid reinventing the wheel \u2014 use <code>itertools</code>.</p> <ul> <li>Filtering: <code>compress</code>, <code>dropwhile</code>, <code>filter</code>, <code>islice</code>, <code>takewhile</code></li> <li>Mapping: <code>accumulate</code>, <code>enumerate</code>, <code>map</code></li> <li>Merging: <code>chain</code>, <code>chain.from_iterable</code>, <code>product</code>, <code>zip</code>, <code>zip_longest</code></li> <li>Multiple outputs: <code>combinations</code>, <code>count</code>, <code>pairwise</code>, <code>cycle</code>, <code>permutations</code>, <code>repeat</code></li> <li>Rearranging: <code>[groupby](itertools/groupby.md)</code>, <code>reversed</code>, <code>tee</code></li> <li>Reducing: <code>all</code>, <code>any</code>, <code>max</code>, <code>min</code>, <code>reduce</code>, <code>sum</code></li> </ul>"},{"location":"python/language/functional/lazy-evaluation/","title":"Lazy vs Eager Evaluation","text":"<p>Eager: compute everything now, store the full result in memory. Lazy: produce one element at a time, on demand; O(1) memory regardless of input size.</p> <pre><code>eager = [x * 2 for x in range(1_000_000)]   # allocates ~8 MB immediately\nlazy  = (x * 2 for x in range(1_000_000))   # 104 bytes \u2014 a state machine\n</code></pre>"},{"location":"python/language/functional/lazy-evaluation/#pythons-lazy-built-ins","title":"Python's lazy built-ins","text":"<p><code>range</code>, <code>map</code>, <code>filter</code>, <code>zip</code>, <code>enumerate</code>, <code>reversed</code>, <code>dict.keys/values/items</code>, generator expressions, and all of <code>itertools</code> are lazy. List/dict/set comprehensions are eager.</p> <p>A generator can only be iterated once \u2014 after that it's exhausted</p> <p>Once consumed, a generator yields nothing. Calling <code>list(gen)</code> twice returns an empty list the second time. If you need multiple passes (sorting, two separate loops), materialise to a list first with <code>data = list(gen)</code>.</p>"},{"location":"python/language/functional/lazy-evaluation/#forcing-evaluation-sinks","title":"Forcing evaluation (\"sinks\")","text":"<p>A lazy iterator does nothing until consumed:</p> <pre><code>gen = (x * 2 for x in range(10))\n\nlist(gen)   # materialise to list\nsum(gen)    # fold to scalar \u2014 never builds a list; O(1) memory\nmax(gen)    # same\nnext(gen)   # pull one element\n</code></pre> <p><code>sum()</code> and <code>max()</code> are the best sinks when you only need a scalar \u2014 they stream through the iterator without ever allocating the intermediate collection.</p>"},{"location":"python/language/functional/lazy-evaluation/#when-lazy-wins","title":"When lazy wins","text":"<p>Large data, single pass \u2014 don't load what you don't need:</p> <pre><code>total = sum(\n    float(line.split(\",\")[3])\n    for line in open(\"trades.csv\")\n    if line.startswith(\"2024\")\n)\n</code></pre> <p>Short-circuiting \u2014 stops as soon as the answer is known:</p> <pre><code>found = any(is_breached(pos) for pos in portfolio)   # stops at first True\nfirst_loss = next(t for t in trades if t.pnl &lt; 0)   # pulls one element\n</code></pre> <p>With an eager list comprehension both would evaluate every element first.</p> <p>Infinite sequences \u2014 impossible to materialise:</p> <pre><code>from itertools import count, islice\nfirst_10_evens = list(islice(count(0, 2), 10))\n</code></pre> <p>Pipeline composition \u2014 chain lazy stages with no intermediate allocations:</p> <pre><code>from itertools import islice\n\nresult = list(islice(\n    (float(row[\"pnl\"]) for row in load_trades(\"trades.csv\") if row[\"desk\"] == \"eq\"),\n    500,\n))\n</code></pre> <p>Each stage hands one element at a time to the next. Memory = sum of stage state, not data size.</p> <p>sum() and max() are the best sinks \u2014 they stream without allocating</p> <p><code>list(gen)</code> and <code>sum(gen)</code> both consume the generator, but <code>list</code> allocates O(n) memory first. <code>sum(gen)</code> and <code>max(gen)</code> maintain O(1) state (a running total or current max), streaming through without ever building the intermediate collection. Prefer them when you only need a scalar result.</p>"},{"location":"python/language/functional/lazy-evaluation/#when-eager-wins","title":"When eager wins","text":"Need Why lazy fails Use Multiple passes iterator exhausted after one <code>list()</code> first Random access (<code>data[i]</code>) iterators don't index <code>list()</code> <code>len()</code> generators have no length <code>list()</code> or count separately Sorting must consume fully anyway <code>sorted()</code> Numerical computation Python-loop overhead NumPy / Pandas (vectorised) <p>NumPy and Pandas are always eager \u2014 they allocate upfront and run vectorised C operations. That tradeoff (memory for speed) is correct when the data fits in RAM and you're doing numerical work.</p> <pre><code># NumPy \u2014 eager, but vectorised and much faster\nnav = np.cumprod(1 + returns)\n\n# Pure-Python lazy \u2014 O(1) memory, but a Python loop per element\nfrom itertools import accumulate\nimport operator\nnav = list(accumulate((1 + r for r in returns), operator.mul))\n</code></pre>"},{"location":"python/language/functional/lazy-evaluation/#generator-function-vs-generator-expression","title":"Generator function vs generator expression","text":"<p>Both are lazy. Use a function when logic is multi-step or stateful:</p> <pre><code># Expression \u2014 fine for simple transforms\npos_returns = (r for r in daily_returns if r &gt; 0)\n\n# Function \u2014 needed when local state spans yields\ndef period_returns(prices):\n    prev = None\n    for p in prices:\n        if prev is not None:\n            yield (p - prev) / prev\n        prev = p\n</code></pre>"},{"location":"python/language/functional/lazy-evaluation/#summary","title":"Summary","text":"Lazy Eager Memory O(1) O(n) Multiple passes No (exhausted) Yes Random access No Yes Short-circuit Yes No Infinite sequences Yes No Numeric speed Slow (Python loop) Fast (vectorised)"},{"location":"python/language/functional/lazy-evaluation/#related","title":"Related","text":"<ul> <li>iterators-generators.md \u2014 iterator protocol, <code>yield</code>, generator functions</li> <li>itertools/core.md \u2014 lazy combinatorics: <code>chain</code>, <code>islice</code>, <code>product</code></li> <li>itertools/accumulate.md \u2014 lazy running totals; vs <code>numpy.cumsum</code></li> </ul>"},{"location":"python/language/functional/operator/","title":"<code>operator</code> \u2014 attrgetter, itemgetter, methodcaller","text":"<p>The <code>operator</code> module provides C-implemented callables for attribute, item, and method access. They replace common lambdas used as sort keys, <code>map</code> arguments, and <code>groupby</code> key functions.</p> <p>Picklability is the decisive advantage for multiprocessing</p> <p>Lambda functions cannot be pickled, which means they can't be passed across process boundaries in <code>ProcessPoolExecutor</code> or <code>multiprocessing.Pool</code>. <code>itemgetter</code>, <code>attrgetter</code>, and <code>methodcaller</code> are all picklable, making them the only correct choice when using parallel processes.</p> <p>Three reasons to prefer them over lambdas: - Faster \u2014 no Python stack frame overhead (~20\u201340% on CPython for large sorts) - Declarative \u2014 state what is accessed, not how - Picklable \u2014 lambdas cannot be pickled; these can (required for <code>multiprocessing</code>)</p>"},{"location":"python/language/functional/operator/#itemgetter-subscript-access","title":"<code>itemgetter</code> \u2014 subscript access","text":"<pre><code>from operator import itemgetter\n\nitemgetter(key)          # obj[key]\nitemgetter(k1, k2, ...)  # (obj[k1], obj[k2], ...) \u2014 tuple\n</code></pre> <p>Works on tuples, lists, dicts, numpy arrays \u2014 anything subscriptable.</p> <pre><code>rows = [('Alice', 30), ('Bob', 25)]\nsorted(rows, key=itemgetter(1))                         # by age\n\nsorted(data, key=itemgetter('dept', 'salary'))          # composite key\n# vs lambda r: (r['dept'], r['salary'])\n</code></pre> <p>Best use: same key for sort + groupby \u2014 name it once, pass it twice: <pre><code>from operator import itemgetter\nfrom itertools import groupby\n\nby_desk = itemgetter('desk')\ntrades.sort(key=by_desk)\nfor desk, group in groupby(trades, key=by_desk):\n    print(desk, sum(t['pnl'] for t in group))\n</code></pre></p> <p>See itertools/groupby.md for the sort-first pattern.</p>"},{"location":"python/language/functional/operator/#attrgetter-object-attribute-access","title":"<code>attrgetter</code> \u2014 object attribute access","text":"<pre><code>from operator import attrgetter\n\nattrgetter('salary')              # obj.salary\nattrgetter('department.name')     # obj.department.name  \u2014 dotted path\nattrgetter('dept', 'salary')      # (obj.dept, obj.salary)\n</code></pre> <p>Dotted paths are the killer feature \u2014 no clean lambda equivalent for deep nesting:</p> <pre><code>employees.sort(key=attrgetter('salary'))\nemployees.sort(key=attrgetter('department.headcount'))  # nested object\nemployees.sort(key=attrgetter('department', 'salary'))  # composite\n</code></pre>"},{"location":"python/language/functional/operator/#methodcaller-method-call-with-fixed-arguments","title":"<code>methodcaller</code> \u2014 method call with fixed arguments","text":"<pre><code>from operator import methodcaller\n\nmethodcaller('upper')                 # obj.upper()\nmethodcaller('strftime', '%Y-%m')     # obj.strftime('%Y-%m')\nmethodcaller('returns', period='1Y')  # obj.returns(period='1Y')\n</code></pre> <p>Main use: <code>map</code> or sort key where all objects get the same call with the same args:</p> <pre><code>from datetime import date\ndates = [date(2024, 3, 15), date(2024, 1, 1)]\nlist(map(methodcaller('strftime', '%Y-%m'), dates))\n# vs lambda d: d.strftime('%Y-%m')\n\nportfolios.sort(key=methodcaller('returns', period='1Y'))\n# vs lambda p: p.returns(period='1Y')\n</code></pre> <p>For zero-argument methods on built-in types, <code>str.upper</code> is cleaner than <code>methodcaller('upper')</code>.</p>"},{"location":"python/language/functional/operator/#when-lambdas-are-still-right","title":"When lambdas are still right","text":"<p>Use a lambda when the body involves computation, not just access:</p> <pre><code>sorted(positions, key=lambda p: p.price * p.quantity)  # arithmetic\nsorted(data, key=lambda r: -r['score'])                 # negation\nsorted(items, key=lambda x: x.value or 0)              # fallback\n</code></pre> <p>Rule: if the lambda body is only attribute/item/method access (possibly nested or multi-key), use the operator equivalent.</p>"},{"location":"python/language/functional/operator/#summary","title":"Summary","text":"Accesses Multi-key Dotted path <code>itemgetter</code> <code>obj[key]</code> \u2713 \u2717 <code>attrgetter</code> <code>obj.attr</code> \u2713 \u2713 (<code>'a.b.c'</code>) <code>methodcaller</code> <code>obj.method(*args)</code> \u2717 \u2717 <p>All three: faster than lambda, picklable, work with <code>sorted</code>, <code>min</code>, <code>max</code>, <code>map</code>, <code>groupby</code>.</p> <p>See functools.md for higher-order function patterns that combine well with these.</p>"},{"location":"python/language/functional/string-formatting/","title":"String Formatting in Python","text":"<p>Four ways to embed values into strings. f-strings are the modern default.</p> Situation Use Normal case f-string Template stored in a variable / config <code>str.format()</code> Logging calls <code>%</code>-style (deferred evaluation) Template from untrusted user input <code>string.Template</code>"},{"location":"python/language/functional/string-formatting/#f-strings-python-36","title":"f-strings (Python 3.6+)","text":"<p>Prefix a string literal with <code>f</code>. Any <code>{expression}</code> is evaluated at runtime:</p> <pre><code>name, age = \"Alice\", 30\nf\"Hello, {name}! You are {age}.\"     # arbitrary expressions: attr, index, calls, ternary\nf\"{price * 1.2:.2f}\"                 # format spec after colon\nf\"{'yes' if flag else 'no'}\"\n</code></pre>"},{"location":"python/language/functional/string-formatting/#debug-shorthand","title":"Debug shorthand","text":"<p><code>f\"{expr=}\"</code> prints the source text and the value \u2014 useful for quick inspection:</p> <pre><code>x = 42\nf\"{x=}\"           # 'x=42'\nf\"{x * 2 + 1=}\"   # 'x * 2 + 1=85'\nf\"{x=:.2f}\"       # 'x=42.00'\n</code></pre>"},{"location":"python/language/functional/string-formatting/#dynamic-spec","title":"Dynamic spec","text":"<p>The spec field can itself be an expression:</p> <pre><code>f\"{value:{width}.{precision}f}\"   # width and precision from variables\n</code></pre>"},{"location":"python/language/functional/string-formatting/#raw-f-strings","title":"Raw f-strings","text":"<pre><code>rf\"C:\\users\\{path}\\file.txt\"   # backslashes literal; braces still interpolate\n</code></pre>"},{"location":"python/language/functional/string-formatting/#strformat","title":"str.format()","text":"<p>Useful when the template is a variable (can't do that with an f-string literal):</p> <pre><code>\"{} is {}\".format(\"Alice\", 30)          # positional\n\"{0} and {0}\".format(\"echo\")            # reuse by index\n\"{name}\".format(name=\"Alice\")           # keyword\n\nTEMPLATE = \"Dear {name}, order #{id} shipped.\"\nTEMPLATE.format(**order_dict)           # fill from dict\n</code></pre> <p>Format specs work identically to f-strings: <code>\"{:.2f}\".format(3.14)</code>.</p>"},{"location":"python/language/functional/string-formatting/#-formatting-legacy","title":"%-formatting \u2014 legacy","text":"<p>Use %-style in logging calls \u2014 the string is only built if the message is emitted</p> <p><code>log.debug(f\"parsed {n} records\")</code> always builds the string, even if DEBUG is disabled. <code>log.debug(\"parsed %d records\", n)</code> skips string construction entirely when the level is filtered out. In hot paths or tight loops, this difference matters.</p> <p>Avoid in new code. Still conventional in <code>logging</code> because formatting is deferred until the message is actually emitted:</p> <pre><code>logger.debug(\"processing %s items\", len(items))   # string not built if DEBUG is off\n</code></pre>"},{"location":"python/language/functional/string-formatting/#stringtemplate-for-untrusted-input","title":"string.Template \u2014 for untrusted input","text":"<p>No expressions, no arbitrary Python \u2014 safe when the template comes from user input or config:</p> <pre><code>from string import Template\nTemplate(\"Hello, $name!\").substitute(name=\"Alice\")\nTemplate(\"Hi $name\").safe_substitute()   # leaves unknown $vars intact instead of raising\n</code></pre>"},{"location":"python/language/functional/string-formatting/#string-specific-format-specs","title":"String-specific format specs","text":"<pre><code>{value:[fill][align][.precision]}\n</code></pre> <p>Strings default to left-aligned; numbers default to right-aligned.</p> <pre><code>f\"{'text':&lt;20}\"      # 'text                '  \u2014 left-aligned (explicit)\nf\"{'text':&gt;20}\"      # '                text'  \u2014 right-aligned\nf\"{'text':^20}\"      # '        text        '  \u2014 centred\nf\"{'text':*^20}\"     # '********text********'  \u2014 fill char before align\n</code></pre> <p>Truncation \u2014 <code>.precision</code> on a string means max character count:</p> <pre><code>f\"{'hello world':.5}\"      # 'hello'\nf\"{'hello world':&lt;20.5}\"   # 'hello               '  \u2014 truncate then pad\n</code></pre> <p>Fixed-width table columns that never overflow:</p> <pre><code>for name, value in rows:\n    print(f\"{name:&lt;20.20} {value:&gt;10.2f}\")\n</code></pre>"},{"location":"python/language/functional/string-formatting/#number-format-specs","title":"Number format specs","text":"<p>See the type codes, precision, sign, and separator options in the section below \u2014 they apply inside any <code>{}</code>, whether an f-string, <code>.format()</code>, or <code>format()</code> call.</p>"},{"location":"python/language/functional/string-formatting/#type-codes","title":"Type codes","text":"Code Output Example <code>f</code> Fixed-point <code>f\"{3.14:.2f}\"</code> \u2192 <code>'3.14'</code> <code>e</code>/<code>E</code> Scientific <code>f\"{12345:.2e}\"</code> \u2192 <code>'1.23e+04'</code> <code>g</code> Fixed or scientific, shorter <code>f\"{3.14159:.4g}\"</code> \u2192 <code>'3.142'</code> <code>%</code> Percent (\u00d7100) <code>f\"{0.173:.1%}\"</code> \u2192 <code>'17.3%'</code> <code>d</code> Integer decimal <code>f\"{42:d}\"</code> \u2192 <code>'42'</code> <code>b</code>/<code>x</code>/<code>X</code>/<code>o</code> Binary / hex / HEX / octal <code>f\"{255:x}\"</code> \u2192 <code>'ff'</code>"},{"location":"python/language/functional/string-formatting/#precision","title":"Precision","text":"<p><code>.precision</code> = decimal places for <code>f</code>/<code>e</code>/<code>%</code>; significant figures for <code>g</code>:</p> <pre><code>f\"{3.14159:.2f}\"   # '3.14'\nf\"{3.14159:.4g}\"   # '3.142'\n</code></pre>"},{"location":"python/language/functional/string-formatting/#width-alignment-padding","title":"Width, alignment, padding","text":"<pre><code>f\"{3.14:10.2f}\"    # '      3.14'  \u2014 right-aligned in 10 (default for numbers)\nf\"{3.14:&lt;10.2f}\"   # '3.14      '  \u2014 left-aligned\nf\"{3.14:^10.2f}\"   # '   3.14   '  \u2014 centred\nf\"{3.14:010.2f}\"   # '0000003.14'  \u2014 zero-padded\n</code></pre>"},{"location":"python/language/functional/string-formatting/#sign","title":"Sign","text":"<pre><code>f\"{3.14:+.2f}\"    # '+3.14'\nf\"{3.14: .2f}\"    # ' 3.14'  \u2014 space for positive\n</code></pre>"},{"location":"python/language/functional/string-formatting/#separators","title":"Separators","text":"<pre><code>f\"{1234567:,.2f}\"   # '1,234,567.00'\nf\"{1234567:_.2f}\"   # '1_234_567.00'\n</code></pre>"},{"location":"python/language/functional/string-formatting/#alternate-form","title":"Alternate form","text":"<pre><code>f\"{255:#x}\"    # '0xff'\nf\"{255:#010x}\" # '0x000000ff'\nf\"{10:#b}\"     # '0b1010'\n</code></pre>"},{"location":"python/language/functional/string-formatting/#custom-format","title":"Custom format","text":"<p>Any class can define how it responds to a format spec:</p> <pre><code>class Money:\n    def __format__(self, spec):\n        if spec == \"short\":\n            return f\"${self.amount:,.0f}\"\n        return f\"${self.amount:{spec}}\"\n\nf\"{Money(1234.5):.2f}\"    # '$1234.50'\nf\"{Money(1234.5):short}\"  # '$1,235'\n</code></pre>"},{"location":"python/language/functional/unpacking/","title":"Unpacking Operators: <code>*x</code> and <code>**x</code>","text":"<p><code>*</code> and <code>**</code> appear in expression and assignment contexts with different meanings. This note covers expression context (right-hand side) \u2014 what protocol <code>x</code> must satisfy.</p>"},{"location":"python/language/functional/unpacking/#x-iterable-unpacking","title":"<code>*x</code> \u2014 iterable unpacking","text":"<p>Python calls <code>iter(x)</code> and consumes all items. <code>x</code> must implement: - <code>__iter__</code> \u2192 returns an iterator, or - <code>__getitem__</code> with integer keys starting at 0 (legacy sequence fallback)</p>"},{"location":"python/language/functional/unpacking/#contexts","title":"Contexts","text":"<p>Function calls \u2014 items become positional args: <pre><code>f(*[1, 2, 3])          # f(1, 2, 3)\nf(*args, key=val)      # mix with keyword args\n</code></pre></p> <p>Collection literals \u2014 splice into another collection: <pre><code>[*a, *b]       # list  \u2014 concatenate\n(*a, *b)       # tuple\n{*a, *b}       # set (order not preserved)\n[*a, 0, *b]    # interleave literals freely\n</code></pre></p> <p>Multiple <code>*</code> unpacks are allowed in a single call or literal (Python 3.5+).</p>"},{"location":"python/language/functional/unpacking/#custom-class","title":"Custom class","text":"<pre><code>class MySeq:\n    def __iter__(self):\n        yield 1; yield 2; yield 3\n\nprint([*MySeq()])   # [1, 2, 3]\n</code></pre>"},{"location":"python/language/functional/unpacking/#x-mapping-unpacking","title":"<code>**x</code> \u2014 mapping unpacking","text":"<p>Python calls <code>x.keys()</code> then <code>x[key]</code> for each key. <code>x</code> must implement: - <code>keys()</code> \u2014 returns an iterable of keys - <code>__getitem__</code> \u2014 lookup by key</p> <p><code>__iter__</code> is not required. A class with only <code>keys()</code> and <code>__getitem__</code> works with <code>**</code> but not <code>*</code>.</p>"},{"location":"python/language/functional/unpacking/#contexts_1","title":"Contexts","text":"<p>Function calls \u2014 items become keyword args (keys must be strings): <pre><code>f(**{\"a\": 1, \"b\": 2})         # f(a=1, b=2)\nf(**d, extra=3)                # mix with literal keyword\n</code></pre></p> <p>Dict literals \u2014 merge/overlay dicts: <pre><code>{**d1, **d2}           # merge; rightmost wins on conflict\n{**d1, \"key\": 99}      # literal value overrides d1[\"key\"]\n</code></pre></p> <p>Pre-3.9 idiom. From Python 3.9+, <code>d1 | d2</code> is cleaner for merging two dicts into a new one.</p>"},{"location":"python/language/functional/unpacking/#custom-class_1","title":"Custom class","text":"<pre><code>class MyMapping:\n    def keys(self):         return [\"x\", \"y\"]\n    def __getitem__(self, k): return {\"x\": 10, \"y\": 20}[k]\n\nprint({**MyMapping()})  # {\"x\": 10, \"y\": 20}\n</code></pre>"},{"location":"python/language/functional/unpacking/#starred-assignment-lhs-different-syntax-same-symbol","title":"Starred assignment (LHS) \u2014 different syntax, same symbol","text":"<p>When <code>*</code> appears on the left of an assignment it captures a variable-length slice \u2014 no custom protocol needed:</p> <pre><code>first, *rest = [1, 2, 3, 4]   # rest = [2, 3, 4]\n*init, last  = [1, 2, 3, 4]   # init = [1, 2, 3]\na, *mid, z   = range(5)        # mid  = [1, 2, 3]\n</code></pre> <p>Only one <code>*</code> per assignment target.</p>"},{"location":"python/language/functional/unpacking/#mixing-in-function-calls","title":"Mixing in function calls","text":"<pre><code>def f(a, b, c=0, d=0): ...\nf(*[1, 2], **{\"c\": 3, \"d\": 4})   # f(1, 2, c=3, d=4)\n</code></pre> <p>* and ** use different protocols \u2014 a class can support one but not the other</p> <p><code>*x</code> requires <code>__iter__</code> (iteration). <code>**x</code> requires <code>keys()</code> + <code>__getitem__</code> (mapping). A class with only <code>keys()</code> and <code>__getitem__</code> works with <code>**</code> in function calls but raises <code>TypeError</code> with <code>*</code>. This is why dicts support both, but a custom mapping that lacks <code>__iter__</code> only works with <code>**</code>.</p>"},{"location":"python/language/functional/unpacking/#protocol-summary","title":"Protocol summary","text":"Operator Requires Does NOT require <code>*x</code> <code>__iter__</code> (or <code>__getitem__(int)</code>) <code>keys()</code> <code>**x</code> <code>keys()</code> + <code>__getitem__</code> <code>__iter__</code> <p>See iterators-generators.md for the full iteration protocol and data-model.md for dunder methods.</p>"},{"location":"python/language/functional/itertools/","title":"Python \u2014 Language / Functional / itertools","text":"File Type Description accumulate.md note Running totals and compound products; lazy numpy.cumsum; NAV series pattern core.md note chain, islice, product, combinations \u2014 lazy combinatorics and slicing groupby.md note Consecutive grouping, key function, sort-first pattern, shared-iterator gotcha"},{"location":"python/language/functional/itertools/accumulate/","title":"<code>itertools.accumulate</code> \u2014 running totals","text":"<p><code>accumulate(iterable, func=operator.add, *, initial=None)</code> yields cumulative results of a binary function applied left-to-right. Lazy equivalent of <code>numpy.cumsum</code> / <code>numpy.cumprod</code>.</p>"},{"location":"python/language/functional/itertools/accumulate/#default-running-sum","title":"Default: running sum","text":"<pre><code>from itertools import accumulate\n\nlist(accumulate([1, 2, 3, 4, 5]))\n# [1, 3, 6, 10, 15]\n</code></pre>"},{"location":"python/language/functional/itertools/accumulate/#with-operatormul-compound-returns","title":"With <code>operator.mul</code>: compound returns","text":"<pre><code>import operator\n\ndaily_gross = [1.01, 0.99, 1.02]\nlist(accumulate(daily_gross, operator.mul))\n# [1.01, 0.9999, 1.01990...]\n</code></pre>"},{"location":"python/language/functional/itertools/accumulate/#initial-38-prepend-a-starting-value","title":"<code>initial</code> (3.8+): prepend a starting value","text":"<p>Output length becomes <code>len(input) + 1</code>:</p> <pre><code>list(accumulate([1, 2, 3], operator.add, initial=10))\n# [10, 11, 13, 16]\n</code></pre>"},{"location":"python/language/functional/itertools/accumulate/#gross-return-nav-series-pattern","title":"Gross return / NAV series pattern","text":"<pre><code>from itertools import accumulate\nimport operator\n\ndaily_net = [0.01, -0.01, 0.02, 0.005]\nnav = list(accumulate(\n    (1 + r for r in daily_net),\n    operator.mul,\n    initial=1.0,\n))\n# [1.0, 1.01, 0.9999, 1.01990..., 1.02500...]\n</code></pre> <p>Equivalent to <code>numpy.cumprod(1 + returns)</code> prepended with 1.0, but lazy and composable.</p>"},{"location":"python/language/functional/itertools/accumulate/#running-max-drawdown-tracking","title":"Running max / drawdown tracking","text":"<pre><code>prices = [100, 102, 101, 105, 98, 107]\n\npeaks = list(accumulate(prices, max))\n# [100, 102, 102, 105, 105, 107]\n\ndrawdowns = [(p - pk) / pk for p, pk in zip(prices, peaks)]\n</code></pre>"},{"location":"python/language/functional/itertools/accumulate/#accumulate-vs-reduce","title":"<code>accumulate</code> vs <code>reduce</code>","text":"<p>accumulate keeps every intermediate value; reduce collapses to one</p> <p>Use <code>accumulate</code> when the path matters (NAV series, drawdown history, running P&amp;L). Use <code>reduce</code> when only the final result matters (total, product of all). <code>accumulate</code> is lazy; <code>reduce</code> consumes everything before returning.</p> <code>accumulate</code> <code>functools.reduce</code> Returns iterator of all intermediate values single final value Use for time series, NAV paths, running stats scalar fold / aggregate Lazy yes no"},{"location":"python/language/functional/itertools/accumulate/#related","title":"Related","text":"<ul> <li>core.md \u2014 chain, islice, product, combinations</li> <li>groupby.md \u2014 consecutive grouping</li> <li>../iterators-generators.md \u2014 iterator protocol</li> </ul>"},{"location":"python/language/functional/itertools/core/","title":"<code>itertools</code> \u2014 chain, islice, product, combinations","text":"<p>All four operate on iterators \u2014 they are lazy and never materialise the full sequence in memory. This matters when your input is a tick stream, a 5000-ticker universe, or a cross of many scenario axes.</p> <p>See groupby.md for <code>groupby</code> and accumulate.md for running totals.</p>"},{"location":"python/language/functional/itertools/core/#chain-concatenate-iterables-into-one-stream","title":"<code>chain</code> \u2014 concatenate iterables into one stream","text":"<pre><code>from itertools import chain\n\nchain(iter_a, iter_b, iter_c)       # one after another\nchain.from_iterable(list_of_iters)  # flatten one level\n</code></pre> <ul> <li>Use <code>chain.from_iterable</code> when the outer iterable is itself a generator (can't unpack with <code>*</code>).</li> </ul> <p>Financial examples: <pre><code># Merge trade logs from multiple desks\nall_trades = chain(equity_trades, fx_trades, rates_trades)\ntotal_pnl = sum(t.pnl for t in all_trades)\n\n# Flatten per-account transaction lists\ntransactions = chain.from_iterable(account_txns.values())\n\n# Prepend a header row to a report generator\nrows = chain([header], generate_report_rows())\n</code></pre></p>"},{"location":"python/language/functional/itertools/core/#islice-slice-a-lazy-iterator","title":"<code>islice</code> \u2014 slice a lazy iterator","text":"<pre><code>from itertools import islice\n\nislice(iterable, stop)\nislice(iterable, start, stop)\nislice(iterable, start, stop, step)\n</code></pre> <p>Like list slicing but for iterators, which don't support <code>[start:stop]</code>. Consumes only what it needs; the rest is untouched.</p> <ul> <li>Does not support negative indices \u2014 use <code>collections.deque(it, maxlen=N)</code> for last-N.</li> </ul> <p>Financial examples: <pre><code># Top-20 holdings from a sorted generator\ntop20 = list(islice(rank_by_momentum(universe), 20))\n\n# Skip a 21-day warm-up period\nreturns_ex_warmup = islice(daily_returns, 21, None)\n\n# Page through large API results\ndef paginate(gen, page_size=100):\n    while chunk := list(islice(gen, page_size)):\n        process(chunk)\n</code></pre></p>"},{"location":"python/language/functional/itertools/core/#product-cartesian-product-scenario-grids","title":"<code>product</code> \u2014 Cartesian product (scenario grids)","text":"<pre><code>from itertools import product\n\nproduct(A, B)        # all (a, b) pairs \u2014 equivalent to nested for loops\nproduct(A, repeat=k) # all k-tuples drawn from A\n</code></pre> <p><code>product(A, B, C)</code> is exactly <code>[(a,b,c) for a in A for b in B for c in C]</code>, but lazy.</p> <p>Financial examples: <pre><code>rate_shocks   = [-0.01, 0.00, +0.01, +0.02]\nspread_shocks = [-0.005, 0.00, +0.005]\nvol_shocks    = [0.8, 1.0, 1.2]\n\n# All 4 \u00d7 3 \u00d7 3 = 36 stress scenarios\nfor dr, ds, dv in product(rate_shocks, spread_shocks, vol_shocks):\n    price_scenario(portfolio, dr, ds, dv)\n\n# Grid search over model hyperparameters\nfor lookback, threshold in product(range(10, 60, 5), [0.01, 0.02, 0.05]):\n    backtest(lookback, threshold)\n</code></pre></p>"},{"location":"python/language/functional/itertools/core/#combinations-unordered-subsets-without-repetition","title":"<code>combinations</code> \u2014 unordered subsets without repetition","text":"<pre><code>from itertools import combinations, combinations_with_replacement\n\ncombinations(pool, r)                  # C(n, r) subsets, no repeats\ncombinations_with_replacement(pool, r) # allows repeated elements\n</code></pre> <p><code>combinations('ABCD', 2)</code> \u2192 <code>AB AC AD BC BD CD</code> \u2014 6 pairs, not 12.</p> <p>Financial examples: <pre><code># All pairs for a correlation matrix (avoid computing both (A,B) and (B,A))\nfor t1, t2 in combinations(tickers, 2):\n    corr_matrix[t1][t2] = correlation(returns[t1], returns[t2])\n\n# All 3-stock baskets from a universe of 20 \u2014 C(20,3) = 1140\nfor basket in combinations(universe, 3):\n    backtest_basket(basket)\n</code></pre></p>"},{"location":"python/language/functional/itertools/core/#product-vs-combinations","title":"<code>product</code> vs <code>combinations</code>","text":"<code>product</code> <code>combinations</code> Draws from multiple independent pools one shared pool Repeats yes (across pools) no Order matters yes no Use for scenario grids, hyperparameter search portfolio subsets, pair selection"},{"location":"python/language/functional/itertools/core/#check-size-before-iterating","title":"Check size before iterating","text":"<p>Combinatorial explosion: always compute size before enumerating</p> <p><code>combinations(universe, 5)</code> over 500 assets produces 255 billion pairs. Even with lazy iteration, processing each one takes time \u2014 this will not finish. Use <code>math.comb(n, r)</code> to check the count before writing the loop, and switch to random sampling for large spaces.</p> <pre><code>from math import comb\n\nprint(comb(500, 5))   # 255,244,687,600 \u2014 do NOT enumerate\nprint(4 * 3 * 3)      # 36 \u2014 fine\n</code></pre> <p>If the count is large, switch to random sampling or Monte Carlo instead.</p>"},{"location":"python/language/functional/itertools/core/#composing-them","title":"Composing them","text":"<p>All functions consume and return iterators, so they compose directly:</p> <pre><code># Top-10 most correlated pairs from all combinations\ntop_pairs = list(\n    islice(\n        sorted(\n            ((corr(r[a], r[b]), a, b) for a, b in combinations(tickers, 2)),\n            reverse=True,\n        ),\n        10,\n    )\n)\n</code></pre> <p>See ../iterators-generators.md for the iterator protocol underlying all of these.</p>"},{"location":"python/language/functional/itertools/groupby/","title":"<code>itertools.groupby</code>","text":"<p>Groups consecutive elements of an iterable that share the same key. Like SQL <code>GROUP BY</code>, but only groups adjacent elements \u2014 input must be sorted by the key first.</p>"},{"location":"python/language/functional/itertools/groupby/#basic-usage","title":"Basic usage","text":"<pre><code>from itertools import groupby\n\ndata = [1, 1, 2, 2, 2, 3, 1, 1]\nfor key, group in groupby(data):\n    print(key, list(group))\n# 1 [1, 1]\n# 2 [2, 2, 2]\n# 3 [3]\n# 1 [1, 1]   \u2190 new group; 1 appeared again non-consecutively\n</code></pre> <p>Each iteration yields <code>(key, group)</code> where <code>group</code> is a lazy iterator.</p>"},{"location":"python/language/functional/itertools/groupby/#key-function","title":"Key function","text":"<pre><code>words = [\"apple\", \"ant\", \"banana\", \"bee\"]\nwords.sort(key=lambda w: w[0])  # sort first!\n\nfor letter, group in groupby(words, key=lambda w: w[0]):\n    print(letter, list(group))\n# a ['apple', 'ant']\n# b ['banana', 'bee']\n</code></pre> <p>Two rules that must both hold: sort first, consume each group immediately</p> <p><code>groupby</code> only groups adjacent equal elements \u2014 unsorted input produces multiple groups for the same key. And the inner <code>group</code> iterator shares state with the outer loop: advancing past a key exhausts the previous group. Always sort by the key first, and always materialise each group with <code>list(g)</code> before the next iteration.</p>"},{"location":"python/language/functional/itertools/groupby/#critical-gotcha-consume-each-group-before-advancing","title":"Critical gotcha: consume each group before advancing","text":"<p>The <code>group</code> iterator shares state with the outer iterator. Advancing to the next key exhausts the current group.</p> <pre><code># WRONG \u2014 all groups are empty when accessed later\ngroups = {k: g for k, g in groupby([1, 1, 2, 2, 3])}\n\n# RIGHT \u2014 materialise each group immediately\ngroups = {k: list(g) for k, g in groupby([1, 1, 2, 2, 3])}\n# {1: [1, 1], 2: [2, 2], 3: [3]}\n</code></pre>"},{"location":"python/language/functional/itertools/groupby/#canonical-sort-groupby-pattern","title":"Canonical sort \u2192 groupby pattern","text":"<pre><code>records = [\n    {\"name\": \"alice\", \"dept\": \"eng\"},\n    {\"name\": \"bob\",   \"dept\": \"hr\"},\n    {\"name\": \"carol\", \"dept\": \"eng\"},\n]\n\nkey = itemgetter(\"dept\")  # or lambda r: r[\"dept\"] \u2014 see ../operator.md\nfor dept, group in groupby(sorted(records, key=key), key=key):\n    print(dept, [r[\"name\"] for r in group])\n# eng ['alice', 'carol']\n# hr  ['bob']\n</code></pre>"},{"location":"python/language/functional/itertools/groupby/#when-to-use-groupby-vs-alternatives","title":"When to use <code>groupby</code> vs alternatives","text":"Situation Use Already-sorted / streaming data <code>groupby</code> Unsorted data, need counts or aggregations <code>collections.Counter</code> / <code>defaultdict</code> DataFrames <code>pandas.DataFrame.groupby</code> <p>See also: core.md for <code>chain</code>, <code>islice</code>, <code>product</code>, and <code>combinations</code>; ../iterators-generators.md for the iterator protocol.</p>"},{"location":"python/language/objects/","title":"Python \u2014 Language / Objects","text":"File Type Description data-model.md note Dunder methods, pythonic objects, <code>@dataclass</code> exceptions.md note Exception hierarchy, EAFP, raising/chaining, custom exceptions, best practices hash.md note <code>__hash__</code>: hash contract, <code>__eq__</code> coupling, mutability, dataclasses oop.md note Inheritance, MRO, ABCs, Protocols, structural vs nominal typing repository-di.md note Repository pattern + dependency injection: Protocol interface, in-memory fake, testable services sets.md note <code>set</code> and <code>frozenset</code>: creation, mutation, set operations, O(1) membership, gotchas subscriptable.md note <code>__getitem__</code>, <code>__class_getitem__</code>, generic aliases, pre-3.9 annotation fixes typing.md note <code>typing</code> module: <code>Literal</code>, <code>TypeAlias</code>, <code>overload</code> \u2014 restrict and narrow types warnings.md note <code>warnings.warn()</code>, stacklevel, filters, converting exceptions to warnings"},{"location":"python/language/objects/data-model/","title":"The Data Model and Pythonic Objects","text":""},{"location":"python/language/objects/data-model/#the-data-model","title":"The Data Model","text":"<ul> <li>The key API of the Python language is the Data Model.</li> <li>It is a class framework in which classes have special methods (dunder methods).</li> <li>The advantage of dunder methods is uniformity and the ability to apply built-in functions to them.</li> <li>Typical examples: <code>__init__</code>, <code>__len__</code>, <code>__getitem__</code>, <code>__repr__</code>, <code>__hash__</code> and arithmetic operators.</li> <li>Emulating sequences is one of the most common uses.</li> </ul>"},{"location":"python/language/objects/data-model/#pythonic-objects","title":"Pythonic Objects","text":"<ul> <li>String/bytes representation: <code>__repr__</code>, <code>__str__</code>, <code>__format__</code>, <code>__bytes__</code></li> <li>Use <code>__eq__</code> and <code>__hash__</code> to support equality testing and use in sets/dicts.</li> <li><code>__call__</code> makes an object callable (i.e., <code>obj(x)</code>).</li> <li>Decorators <code>@classmethod</code> (e.g. alternative constructor) and <code>@staticmethod</code> (not very useful).</li> <li>Implement <code>__format__</code> that parses <code>format_spec</code> to use:</li> <li><code>format(obj, format_spec)</code></li> <li><code>'1 BRL = {rate:0.2f} USD'.format(rate=brl)</code></li> <li><code>f'1 USD = {1 / brl:0.2f} BRL'</code></li> <li>Make objects immutable by making attributes private with <code>self.__x</code>, then define a getter with <code>@property</code>.</li> <li>Declare the class attribute <code>__slots__</code> to save memory.</li> <li>Define the class attribute <code>typecode</code>, which an instance can override.</li> </ul>"},{"location":"python/language/objects/data-model/#dataclass","title":"<code>@dataclass</code>","text":"<p>Use frozen=True to get hashability and immutability for free</p> <p><code>@dataclass</code> alone generates <code>__eq__</code> but sets <code>__hash__ = None</code>, making instances unhashable (can't be used in sets or as dict keys). <code>@dataclass(frozen=True)</code> also generates <code>__hash__</code> and prevents attribute mutation \u2014 the right default for value objects like <code>Trade</code>, <code>Point</code>, or <code>Price</code>.</p> <ul> <li><code>@dataclass</code> is a decorator from the <code>dataclasses</code> module.</li> <li>It automatically generates boilerplate methods like <code>__init__</code> and <code>__repr__</code>.</li> <li>Mainly used for classes that store data.</li> <li>Fields are defined using type hints.</li> <li><code>@dataclass(frozen=True)</code> makes instances immutable and adds <code>__hash__</code>.</li> <li><code>__post_init__</code>: validation runs after <code>__init__</code>.</li> </ul> <pre><code>from dataclasses import dataclass\n\n@dataclass\nclass Trade:\n    symbol: str\n    price: float\n</code></pre> <p>See also: oop.md for inheritance, ABCs, and Protocols.</p>"},{"location":"python/language/objects/exceptions/","title":"Exceptions","text":"<p>Python uses exceptions for all error conditions \u2014 no error codes, no <code>(value, err)</code> returns. The happy path stays clean; failures can't be silently ignored.</p>"},{"location":"python/language/objects/exceptions/#eafp-vs-lbyl","title":"EAFP vs LBYL","text":"<p>Python favours EAFP (Easier to Ask Forgiveness than Permission): try the operation, handle failure if it occurs.</p> <pre><code># LBYL \u2014 check first (more Java/C)\nif key in d:\n    value = d[key]\n\n# EAFP \u2014 Pythonic\ntry:\n    value = d[key]\nexcept KeyError:\n    value = default\n</code></pre> <p>EAFP avoids race conditions and is faster when success is the common case.</p>"},{"location":"python/language/objects/exceptions/#exception-hierarchy","title":"Exception hierarchy","text":"<pre><code>BaseException\n\u251c\u2500\u2500 SystemExit          \u2190 sys.exit(); do not catch\n\u251c\u2500\u2500 KeyboardInterrupt   \u2190 Ctrl+C; do not catch (or re-raise)\n\u251c\u2500\u2500 GeneratorExit       \u2190 generator .close(); do not catch\n\u2514\u2500\u2500 Exception           \u2190 everything else; your catch boundary\n    \u251c\u2500\u2500 ValueError      right type, wrong value\n    \u251c\u2500\u2500 TypeError       wrong type\n    \u251c\u2500\u2500 AttributeError  obj.attr missing\n    \u251c\u2500\u2500 KeyError        dict key missing\n    \u251c\u2500\u2500 IndexError      list index out of range\n    \u251c\u2500\u2500 FileNotFoundError (subclass of OSError)\n    \u2514\u2500\u2500 RuntimeError    general runtime failure\n</code></pre> <p>Never catch BaseException or bare except \u2014 you'll swallow Ctrl+C and sys.exit()</p> <p><code>except Exception</code> is the correct catch-all boundary. <code>except BaseException</code> and bare <code>except:</code> also catch <code>KeyboardInterrupt</code> and <code>SystemExit</code>, which prevents the user from stopping the program and breaks <code>sys.exit()</code> calls.</p> <p>Never catch <code>BaseException</code> or bare <code>except:</code> \u2014 you'll swallow <code>Ctrl+C</code> and <code>sys.exit()</code>.</p>"},{"location":"python/language/objects/exceptions/#the-four-clauses","title":"The four clauses","text":"<pre><code>try:\n    result = risky_operation()\nexcept ValueError as e:\n    handle(e)\nexcept (TypeError, KeyError):\n    handle_either()\nelse:\n    # runs only if NO exception was raised\n    process(result)\nfinally:\n    # ALWAYS runs \u2014 use for cleanup\n    cleanup()\n</code></pre> <p>Use the else clause to separate the success path from error handling</p> <p>Code in <code>else</code> runs only when no exception was raised \u2014 this is subtly different from code after the <code>try</code> block, which runs regardless. It prevents accidentally catching exceptions raised by your result processing code, and makes the intent (\"this is the happy path\") explicit.</p> <p><code>else</code> is underused: it clearly marks \"success path\" code without accidentally including it inside the error-catching scope.</p>"},{"location":"python/language/objects/exceptions/#raising-exceptions","title":"Raising exceptions","text":"<pre><code>raise ValueError(\"quantity must be positive\")\n\n# re-raise the current exception with original traceback intact\nexcept ValueError:\n    log()\n    raise\n</code></pre>"},{"location":"python/language/objects/exceptions/#exception-chaining","title":"Exception chaining","text":"<p>Link exceptions to preserve cause when translating between layers:</p> <pre><code>try:\n    data = json.loads(raw)\nexcept json.JSONDecodeError as e:\n    raise ValueError(\"invalid config file\") from e   # e becomes __cause__\n</code></pre> <p>The traceback shows both exceptions. Use <code>from None</code> to suppress the original:</p> <pre><code>raise ValueError(\"invalid config\") from None\n</code></pre>"},{"location":"python/language/objects/exceptions/#custom-exceptions","title":"Custom exceptions","text":"<pre><code>class AppError(Exception):\n    \"\"\"Base for all application errors.\"\"\"\n\nclass TradeError(AppError):\n    \"\"\"Errors in trade processing.\"\"\"\n\nclass InvalidQuantityError(TradeError):\n    def __init__(self, quantity: float):\n        self.quantity = quantity\n        super().__init__(f\"invalid quantity: {quantity}\")\n</code></pre> <p>A hierarchy lets callers catch at the right level (<code>AppError</code> vs <code>TradeError</code> vs <code>InvalidQuantityError</code>) without catching unrelated library exceptions.</p>"},{"location":"python/language/objects/exceptions/#best-practices","title":"Best practices","text":"<ul> <li>Be specific \u2014 catch the narrowest type you can actually handle.</li> <li>Don't swallow silently \u2014 at minimum log, then re-raise.</li> <li>Raise at the right level \u2014 low-level code raises specific exceptions; high-level code translates them with <code>raise X from Y</code>.</li> <li>Use <code>finally</code> for cleanup, or better: use a context manager.</li> </ul> <pre><code># BAD \u2014 bug disappears forever\nexcept Exception:\n    pass\n\n# GOOD\nexcept Exception:\n    logger.exception(\"process failed\")\n    raise\n</code></pre>"},{"location":"python/language/objects/exceptions/#exception-attributes","title":"Exception attributes","text":"Attribute Contents <code>e.args</code> Tuple of constructor arguments <code>str(e)</code> Human-readable message <code>e.__cause__</code> Explicitly chained (<code>raise X from Y</code>) <code>e.__context__</code> Implicitly chained (exception during exception handling) <code>e.__traceback__</code> Full traceback object"},{"location":"python/language/objects/exceptions/#exceptions-and-context-managers","title":"Exceptions and context managers","text":"<p><code>__exit__(exc_type, exc_val, exc_tb)</code> returning <code>True</code> suppresses the exception. Use <code>contextlib.suppress</code> for this pattern:</p> <pre><code>from contextlib import suppress\n\nwith suppress(FileNotFoundError):\n    os.remove(\"maybe_exists.txt\")\n</code></pre> <p>See context-managers.md for the full context manager protocol.</p>"},{"location":"python/language/objects/exceptions/#exceptiongroup-python-311","title":"ExceptionGroup (Python 3.11+)","text":"<p>For concurrent tasks where multiple exceptions can occur simultaneously:</p> <pre><code>try:\n    async with asyncio.TaskGroup() as tg:\n        tg.create_task(task_a())\n        tg.create_task(task_b())\nexcept* ValueError as eg:    # except* filters by type\n    for exc in eg.exceptions:\n        print(exc)\n</code></pre>"},{"location":"python/language/objects/exceptions/#related-notes","title":"Related notes","text":"<ul> <li>context-managers.md \u2014 context managers (<code>__exit__</code> protocol, <code>@contextmanager</code>)</li> <li>testing-strategy.md \u2014 <code>pytest.raises</code> to assert exceptions in tests</li> <li>pydantic.md \u2014 <code>ValidationError</code> is the canonical custom exception pattern at application boundaries</li> <li>warnings.md \u2014 <code>warnings.warn()</code>: downgrade a caught exception to a non-fatal advisory</li> </ul>"},{"location":"python/language/objects/hash/","title":"<code>__hash__</code> \u2014 Making Objects Hashable","text":""},{"location":"python/language/objects/hash/#what-and-why","title":"What and why","text":"<p><code>__hash__</code> returns an <code>int</code> that lets an object live in a <code>set</code> or be used as a <code>dict</code> key. Hash-based containers map this integer to a bucket for O(1) lookup; <code>__eq__</code> then resolves collisions within the bucket.</p> <p>The hash contract: if <code>a == b</code> then <code>hash(a) == hash(b)</code>. Violating it silently corrupts sets and dicts.</p> <p>Defining eq alone makes your class unhashable</p> <p>Python sets <code>__hash__ = None</code> automatically when you define <code>__eq__</code> without <code>__hash__</code>. The reason: two objects that compare equal must have the same hash \u2014 if you define equality by value but keep the default identity-based hash, equal objects could end up in different dict buckets, silently corrupting lookups. Python makes this impossible by removing hashability until you explicitly define both.</p>"},{"location":"python/language/objects/hash/#the-__eq__-__hash__-coupling","title":"The <code>__eq__</code> / <code>__hash__</code> coupling","text":"<p>Python enforces the contract by managing <code>__hash__</code> automatically:</p> What you define What Python does Neither Both inherited from <code>object</code> (id-based) <code>__eq__</code> only <code>__hash__ = None</code> \u2192 unhashable <code>__hash__</code> only Works, unusual Both Your definitions win <p>Defining <code>__eq__</code> alone kills hashability because two equal-but-differently-hashed objects would be treated as distinct dict keys \u2014 silent data corruption.</p>"},{"location":"python/language/objects/hash/#implementing-__hash__","title":"Implementing <code>__hash__</code>","text":"<p>Hash the same fields used in <code>__eq__</code> as a tuple:</p> <pre><code>class Point:\n    def __init__(self, x, y):\n        self.x, self.y = x, y\n\n    def __eq__(self, other):\n        return isinstance(other, Point) and (self.x, self.y) == (other.x, other.y)\n\n    def __hash__(self):\n        return hash((self.x, self.y))\n</code></pre> <p><code>hash()</code> on a tuple combines field hashes correctly.</p>"},{"location":"python/language/objects/hash/#mutable-objects-and-hash-corruption","title":"Mutable objects and hash corruption","text":"<p>Mutating state after insertion makes the object unreachable:</p> <pre><code>b = Bad(1)\ns = {b}\nb.x = 99\nprint(b in s)   # False \u2014 b is \"lost\" in the wrong bucket\n</code></pre> <p>This is why <code>list</code>, <code>dict</code>, and <code>set</code> are unhashable by design. Only hash immutable or effectively frozen objects.</p>"},{"location":"python/language/objects/hash/#dataclass","title":"<code>@dataclass</code>","text":"<pre><code>@dataclass\nclass Trade:               # __eq__ generated, __hash__ = None \u2192 unhashable\n    symbol: str\n    price: float\n\n@dataclass(frozen=True)\nclass ImmutableTrade:      # __eq__ + __hash__ both generated \u2192 hashable\n    symbol: str\n    price: float\n</code></pre> <p><code>unsafe_hash=True</code> forces hash generation for a mutable dataclass \u2014 only safe if you guarantee no mutation after hashing.</p>"},{"location":"python/language/objects/hash/#restoring-hashability-in-subclasses","title":"Restoring hashability in subclasses","text":"<p>Defining <code>__eq__</code> in a subclass silently sets <code>__hash__ = None</code>. Re-inherit explicitly:</p> <pre><code>class Sub(Point):\n    def __eq__(self, other): ...\n    __hash__ = Point.__hash__   # restore parent hash\n</code></pre>"},{"location":"python/language/objects/hash/#hash-randomization-pythonhashseed","title":"Hash randomization (<code>PYTHONHASHSEED</code>)","text":"<p>Since Python 3.3, hash values are salted per session to prevent hash-flooding DoS. Never serialize <code>hash()</code> output or compare it across processes. Disable with <code>PYTHONHASHSEED=0</code> for debugging.</p>"},{"location":"python/language/objects/hash/#default-object__hash__","title":"Default <code>object.__hash__</code>","text":"<p>Returns a value derived from <code>id(obj)</code> (roughly memory address). Two distinct objects never collide, and equality is identity.</p> <p>See also: data-model.md for the broader data model and <code>@dataclass</code> overview; functools.md for <code>@lru_cache</code>, which requires hashable arguments as cache keys.</p>"},{"location":"python/language/objects/oop/","title":"Classes: Inheritance and Interfaces","text":""},{"location":"python/language/objects/oop/#inheritance","title":"Inheritance","text":"<ul> <li>Composition (x has y) is preferred over inheritance (x is y).</li> <li>Multiple inheritance available (<code>class U(A, B)</code>). The MRO (method resolution order) determines which method is called. Check <code>cls.__mro__</code>.</li> <li>The MRO follows the C3 linearisation algorithm.</li> <li>Use <code>super()</code> to call the next class in the MRO.</li> <li>Subclassing built-in types has pitfalls \u2014 consider before doing.</li> <li>Some say only ABCs (abstract base classes) should be subclassed.</li> </ul>"},{"location":"python/language/objects/oop/#abstract-base-classes-abcs","title":"Abstract Base Classes (ABCs)","text":"<pre><code>from abc import ABC, abstractmethod\n\nclass Instrument(ABC):\n\n    @property\n    @abstractmethod\n    def symbol(self) -&gt; str: ...\n\n    @abstractmethod\n    def price(self) -&gt; Decimal: ...\n</code></pre> <ul> <li>ABCs use nominal typing: a class must explicitly inherit from the base class.</li> <li>Subclasses that don't implement all abstract methods cannot be instantiated.</li> <li>Better for frameworks, plugins, and strict class designs.</li> <li>ABCs are better when the base class provides shared implementation.</li> </ul>"},{"location":"python/language/objects/oop/#protocols","title":"Protocols","text":"<ul> <li><code>Protocol</code> defines an interface based on object structure, from <code>typing</code>: <code>from typing import Protocol</code>.</li> <li>A class satisfies a protocol if it has the required methods/attributes \u2014 no explicit inheritance needed.</li> <li>This is called structural typing, matching Python's duck typing idea.</li> <li>Protocols are mainly useful for static type checkers like <code>[mypy](../../tooling/mypy.md)</code>.</li> <li>By default, protocols do not enforce behaviour at runtime.</li> <li><code>@runtime_checkable</code> allows limited <code>isinstance()</code> checks.</li> </ul>"},{"location":"python/language/objects/oop/#protocols-vs-abcs","title":"Protocols vs ABCs","text":"ABC Protocol Typing Nominal Structural Requires inheritance Yes No Runtime enforcement Yes Optional (<code>@runtime_checkable</code>) Best for Frameworks, shared implementation Flexible duck typing <ul> <li>ABC: \"you belong to this family.\"</li> <li>Protocol: \"you behave in the required way.\"</li> <li>In modern Python, protocols are often preferred for lightweight interfaces.</li> </ul>"},{"location":"python/language/objects/oop/#verifying-protocol-conformance","title":"Verifying Protocol conformance","text":"<p>Two separate questions: does a class satisfy a Protocol statically (mypy), or at runtime?</p>"},{"location":"python/language/objects/oop/#static-assignability-trick-catches-signature-mismatches","title":"Static \u2014 assignability trick (catches signature mismatches)","text":"<pre><code>def _check(x: PostgresTradeRepo) -&gt; TradeRepo:\n    return x   # mypy errors here if signatures don't match\n</code></pre> <p>Put this at module level. Zero runtime cost; mypy catches missing methods and wrong signatures.</p> <p><code>assert_type</code> (Python 3.11+ / <code>typing_extensions</code>) is an alternative that reads more clearly:</p> <pre><code>from typing import assert_type\nassert_type(PostgresTradeRepo(conn), TradeRepo)  # no-op at runtime\n</code></pre>"},{"location":"python/language/objects/oop/#runtime-isinstance-with-runtime_checkable","title":"Runtime \u2014 <code>isinstance</code> with <code>@runtime_checkable</code>","text":"<pre><code>from typing import runtime_checkable\n\n@runtime_checkable\nclass TradeRepo(Protocol):\n    def get(self, trade_id: int) -&gt; Trade: ...\n    def save(self, trade: Trade) -&gt; None: ...\n\nassert isinstance(PostgresTradeRepo(conn), TradeRepo)  # usable in pytest\n</code></pre> <p>isinstance with @runtime_checkable only checks attribute names, not signatures</p> <p>A class with <code>def get(self, wrong_arg_name): return None</code> passes an <code>isinstance</code> check against a Protocol that requires <code>get(self, trade_id: int)</code>. The names match; the signatures don't. Use the assignability trick or <code>assert_type</code> (mypy) to catch signature mismatches \u2014 <code>isinstance</code> alone is insufficient.</p> <p>Critical limitation: <code>isinstance</code> only checks that the attribute names exist \u2014 not signatures. A class with <code>def get(self, x): return \"nonsense\"</code> passes.</p> Technique Catches missing methods Catches wrong signatures Needs mypy <code>isinstance</code> (<code>@runtime_checkable</code>) Yes No No Assignability function / <code>assert_type</code> Yes Yes Yes <p>Use both: mypy for correctness, <code>isinstance</code> test to document intent and catch omissions.</p> <p>See also: data-model.md for dunder methods and <code>@dataclass</code>; repository-di.md for Protocol-based dependency injection.</p>"},{"location":"python/language/objects/repository-di/","title":"Repository Pattern + Dependency Injection","text":""},{"location":"python/language/objects/repository-di/#what-and-why","title":"What and why","text":"<p>A repository is an object that hides where data lives (DB, file, API) behind a clean interface. Dependency injection means passing that object in via constructor rather than creating it internally. Together they make business logic testable without a real database.</p>"},{"location":"python/language/objects/repository-di/#define-the-interface-as-a-protocol","title":"Define the interface as a Protocol","text":"<pre><code>from typing import Protocol\nfrom dataclasses import dataclass\n\n@dataclass\nclass Trade:\n    id: int\n    symbol: str\n    status: str  # \"OPEN\" | \"CLOSED\"\n\nclass TradeRepo(Protocol):\n    def get(self, trade_id: int) -&gt; Trade: ...\n    def save(self, trade: Trade) -&gt; None: ...\n    def list_open(self) -&gt; list[Trade]: ...\n</code></pre> <p>Use <code>Protocol</code> (structural typing), not ABC. Implementations don't need to inherit \u2014 they just need the right methods.</p>"},{"location":"python/language/objects/repository-di/#business-logic-only-knows-the-protocol","title":"Business logic \u2014 only knows the Protocol","text":"<pre><code>class TradingService:\n    def __init__(self, repo: TradeRepo) -&gt; None:  # \u2190 injection point\n        self._repo = repo\n\n    def close_trade(self, trade_id: int) -&gt; None:\n        trade = self._repo.get(trade_id)\n        trade.status = \"CLOSED\"\n        self._repo.save(trade)\n</code></pre> <p>No database import, no connection string. <code>TradingService</code> is portable and testable.</p>"},{"location":"python/language/objects/repository-di/#real-implementation","title":"Real implementation","text":"<pre><code>class PostgresTradeRepo:\n    def __init__(self, conn) -&gt; None:\n        self._conn = conn\n\n    def get(self, trade_id: int) -&gt; Trade:\n        row = self._conn.execute(\n            \"SELECT id, symbol, status FROM trades WHERE id = %s\", (trade_id,)\n        ).fetchone()\n        return Trade(*row)\n\n    def save(self, trade: Trade) -&gt; None:\n        self._conn.execute(\n            \"INSERT INTO trades (id, symbol, status) VALUES (%s, %s, %s) \"\n            \"ON CONFLICT (id) DO UPDATE SET status = EXCLUDED.status\",\n            (trade.id, trade.symbol, trade.status),\n        )\n\n    def list_open(self) -&gt; list[Trade]:\n        rows = self._conn.execute(\n            \"SELECT id, symbol, status FROM trades WHERE status = 'OPEN'\"\n        ).fetchall()\n        return [Trade(*r) for r in rows]\n</code></pre>"},{"location":"python/language/objects/repository-di/#in-memory-fake-for-tests","title":"In-memory fake (for tests)","text":"<pre><code>class InMemoryTradeRepo:\n    def __init__(self) -&gt; None:\n        self._db: dict[int, Trade] = {}\n\n    def get(self, trade_id: int) -&gt; Trade:\n        return self._db[trade_id]\n\n    def save(self, trade: Trade) -&gt; None:\n        self._db[trade.id] = trade\n\n    def list_open(self) -&gt; list[Trade]:\n        return [t for t in self._db.values() if t.status == \"OPEN\"]\n</code></pre> <p>Prefer a fake over a mock for repositories</p> <p>A fake (like <code>InMemoryTradeRepo</code>) actually implements the behaviour correctly \u2014 <code>list_open()</code> really filters by status. A mock just records calls and returns canned values. Fakes catch logic bugs in callers; mocks only catch that the right method was called with the right args. Fakes also survive refactors; mocks break when implementation details change.</p> <p>This is a fake, not a mock \u2014 it actually behaves correctly. Prefer fakes over mocks for repositories; mocks tie tests to implementation details.</p>"},{"location":"python/language/objects/repository-di/#tests-no-database-needed","title":"Tests \u2014 no database needed","text":"<pre><code>def test_close_trade():\n    repo = InMemoryTradeRepo()\n    repo.save(Trade(id=42, symbol=\"BTC\", status=\"OPEN\"))\n\n    TradingService(repo).close_trade(42)\n\n    assert repo.get(42).status == \"CLOSED\"\n</code></pre>"},{"location":"python/language/objects/repository-di/#production-wiring","title":"Production wiring","text":"<pre><code>def build_trading_service(conn) -&gt; TradingService:\n    return TradingService(PostgresTradeRepo(conn))\n</code></pre> <p>This factory is the only place that knows both <code>PostgresTradeRepo</code> and <code>TradingService</code> exist together.</p>"},{"location":"python/language/objects/repository-di/#key-rules","title":"Key rules","text":"<ul> <li>Inject at the constructor \u2014 makes the dependency visible and overridable per test; never use module-level globals.</li> <li>Keep the Protocol minimal \u2014 only add methods the business logic actually calls.</li> <li>Protocol &gt; ABC \u2014 external storage libraries can't inherit your ABC; they can satisfy your Protocol.</li> </ul>"},{"location":"python/language/objects/repository-di/#related","title":"Related","text":"<ul> <li>oop.md \u2014 Protocol vs ABC detail</li> <li>testing-patterns.md \u2014 seams, <code>tmp_path</code>, in-memory DB strategies</li> </ul>"},{"location":"python/language/objects/sets/","title":"Sets","text":"<p>An unordered collection of unique, hashable elements backed by a hash table. O(1) membership testing; no duplicates.</p> <p>{} creates a dict, not a set \u2014 use set() for an empty set</p> <p>This is the most common set gotcha. <code>{}</code> has been a dict literal since Python 1; set literals require at least one element: <code>{1}</code>. For an empty set, always write <code>set()</code>.</p>"},{"location":"python/language/objects/sets/#creation","title":"Creation","text":"<pre><code>s = {1, 2, 3}                    # literal\ns = set()                        # empty \u2014 {} alone is a dict\ns = set([1, 2, 2, 3])            # from iterable: {1, 2, 3}\ns = {x**2 for x in range(5)}    # set comprehension\n</code></pre> <p>Elements must be hashable (<code>int</code>, <code>str</code>, <code>tuple</code>, <code>frozenset</code> \u2014 not <code>list</code>, <code>dict</code>, <code>set</code>).</p>"},{"location":"python/language/objects/sets/#mutation","title":"Mutation","text":"<pre><code>s.add(4)        # no-op if already present\ns.remove(4)     # KeyError if missing\ns.discard(4)    # silent if missing\ns.update([4,5]) # add multiple from any iterable\ns.pop()         # remove and return arbitrary element\ns.clear()\n</code></pre>"},{"location":"python/language/objects/sets/#membership-and-iteration","title":"Membership and iteration","text":"<pre><code>3 in s          # O(1)\nlen(s)\nfor x in s: ... # order is undefined \u2014 do not rely on it\n</code></pre>"},{"location":"python/language/objects/sets/#set-operations","title":"Set operations","text":"<p>Operators (<code>|</code>, <code>&amp;</code>, <code>-</code>, <code>^</code>) require both operands to be sets. Methods (<code>.union()</code>, <code>.intersection()</code>, etc.) accept any iterable on the right.</p> <pre><code>a = {1, 2, 3}\nb = {2, 3, 4}\n\na | b   # union:                {1, 2, 3, 4}\na &amp; b   # intersection:         {2, 3}\na - b   # difference (a not b): {1}\na ^ b   # symmetric difference: {1, 4}  \u2014 in one but not both\n\n{1, 2} &lt;= {1, 2, 3}          # subset (&lt;=) / proper subset (&lt;)\n{1, 2, 3} &gt;= {1, 2}          # superset\n{1, 2}.isdisjoint({3, 4})    # True \u2014 no elements in common\n</code></pre> <p>In-place: <code>|=</code>, <code>&amp;=</code>, <code>-=</code>, <code>^=</code></p>"},{"location":"python/language/objects/sets/#frozenset-immutable-hashable-set","title":"<code>frozenset</code> \u2014 immutable, hashable set","text":"<pre><code>fs = frozenset({1, 2, 3})    # no add/remove/clear\nd = {frozenset({1, 2}): \"x\"} # can be a dict key or set element\n</code></pre> <p>All read operations work identically to <code>set</code>.</p>"},{"location":"python/language/objects/sets/#performance","title":"Performance","text":"Operation Avg <code>x in s</code> O(1) <code>add</code>, <code>discard</code> O(1) <code>s \\| t</code>, <code>s &amp; t</code> O(len(s)+len(t)) <code>s - t</code> O(len(s))"},{"location":"python/language/objects/sets/#common-patterns","title":"Common patterns","text":"<pre><code># Deduplication (loses order)\nunique = list(set(items))\n\n# Fast membership filtering \u2014 convert once, test many times in O(1)\nexcluded = set(excluded_list)\nfiltered = [x for x in items if x not in excluded]\n\n# Set arithmetic\nonly_in_a = set_a - set_b\nin_both   = set_a &amp; set_b\n</code></pre>"},{"location":"python/language/objects/sets/#gotchas","title":"Gotchas","text":"<ul> <li><code>{}</code> is a <code>dict</code> \u2014 use <code>set()</code> for empty set</li> <li><code>{1, 2} &lt; {1, 2}</code> is <code>False</code> \u2014 <code>&lt;</code> means strict subset</li> <li><code>{1, 2} == {2, 1}</code> is <code>True</code> \u2014 sets are unordered</li> </ul>"},{"location":"python/language/objects/sets/#related","title":"Related","text":"<ul> <li>hash.md \u2014 why elements must be hashable; the <code>__eq__</code>/<code>__hash__</code> contract for custom classes</li> <li>data-model.md \u2014 <code>__contains__</code> dunder for custom membership testing</li> </ul>"},{"location":"python/language/objects/subscriptable/","title":"Subscriptable Types","text":"<p><code>obj[key]</code> is syntactic sugar for <code>type(obj).__getitem__(obj, key)</code>. A type that doesn't define <code>__getitem__</code> raises:</p> <pre><code>TypeError: 'X' object is not subscriptable\n</code></pre>"},{"location":"python/language/objects/subscriptable/#instance-subscripting-__getitem__","title":"Instance subscripting \u2014 <code>__getitem__</code>","text":"<p>Built-ins with <code>__getitem__</code>: <code>list</code>, <code>tuple</code>, <code>str</code>, <code>bytes</code>, <code>dict</code>, <code>numpy.ndarray</code>, <code>pandas.Series</code>. Built-ins without: <code>int</code>, <code>float</code>, <code>bool</code>, <code>NoneType</code>, <code>set</code> (iterable but unordered \u2014 no indexing).</p> <pre><code>42[0]      # TypeError: 'int' object is not subscriptable\n{1, 2}[0]  # TypeError: 'set' object is not subscriptable\n</code></pre> <p>Implement it in your own class to support <code>[]</code>:</p> <pre><code>class TimeSeries:\n    def __init__(self, data): self._data = data\n    def __getitem__(self, key): return self._data[key]  # slices arrive as slice objects\n</code></pre>"},{"location":"python/language/objects/subscriptable/#type-class-subscripting-__class_getitem__","title":"Type / class subscripting \u2014 <code>__class_getitem__</code>","text":"<p>There's a second kind of subscripting: applying <code>[]</code> to a class object, used for generic type annotations:</p> <pre><code>list[int]        # subscripts the class list, not an instance\ndict[str, float]\n</code></pre> <p>This calls <code>list.__class_getitem__(int)</code>, which returns a <code>types.GenericAlias</code> \u2014 metadata for type checkers; no runtime enforcement.</p> <p><code>__class_getitem__</code> was added to built-in types in Python 3.9 (PEP 585). On 3.8 and earlier, <code>list[int]</code> raises <code>TypeError: 'type' object is not subscriptable</code>.</p>"},{"location":"python/language/objects/subscriptable/#pre-39-fixes","title":"Pre-3.9 fixes","text":"<p>Option 1 \u2014 use <code>typing</code> wrappers (capital-letter aliases that have <code>__class_getitem__</code>):</p> <pre><code>from typing import List, Dict, Tuple, Optional\n\ndef process(items: List[int]) -&gt; Dict[str, int]: ...\n</code></pre> <p>Option 2 \u2014 defer annotation evaluation (annotations stored as strings, never executed at import):</p> <pre><code>from __future__ import annotations   # top of file; works on 3.7+\n\ndef process(items: list[int]) -&gt; dict[str, int]: ...  # no runtime error\n</code></pre>"},{"location":"python/language/objects/subscriptable/#what-listint-returns","title":"What <code>list[int]</code> returns","text":"<pre><code>list[int].__origin__  # &lt;class 'list'&gt;\nlist[int].__args__    # (&lt;class 'int'&gt;,)\n</code></pre> <p>A <code>GenericAlias</code> \u2014 pure metadata. A <code>list[int]</code> variable can still hold any type at runtime; Python doesn't enforce it.</p>"},{"location":"python/language/objects/subscriptable/#generic-classes","title":"Generic classes","text":"<p>To make your own class subscriptable as a type hint, inherit from <code>Generic</code>:</p> <pre><code>from typing import Generic, TypeVar\n\nT = TypeVar(\"T\")\n\nclass Stack(Generic[T]):\n    def push(self, item: T) -&gt; None: ...\n    def pop(self) -&gt; T: ...\n\n# Stack[int] now returns a proper generic alias\n</code></pre>"},{"location":"python/language/objects/subscriptable/#common-errors-at-a-glance","title":"Common errors at a glance","text":"<p>'NoneType' object is not subscriptable \u2014 the variable you're indexing is None</p> <p>This is the most frequent subscript error in practice. Common causes: a function that forgets <code>return</code>, <code>dict.get()</code> returning <code>None</code> for a missing key, or an optional value that wasn't checked. Fix the source \u2014 add the missing <code>return</code>, use a default in <code>.get()</code>, or guard with <code>if x is not None</code>.</p> Error Cause Fix <code>'NoneType' object is not subscriptable</code> variable is <code>None</code> (missed <code>return</code>, <code>.get()</code> returned <code>None</code>) check for <code>None</code> first <code>'set' object is not subscriptable</code> sets are unordered use <code>next(iter(s))</code> or convert to list <code>'type' object is not subscriptable</code> built-in generic on Python &lt; 3.9 <code>from __future__ import annotations</code> or use <code>typing.List</code> etc. <p><code>'NoneType'</code> is by far the most common in practice.</p>"},{"location":"python/language/objects/subscriptable/#related","title":"Related","text":"<ul> <li>data-model.md \u2014 dunder methods overview; <code>__getitem__</code> in context</li> <li>../../tooling/mypy.md \u2014 type checking generic aliases statically</li> </ul>"},{"location":"python/language/objects/typing/","title":"typing module \u2014 selected constructs","text":"<p>Key types from <code>typing</code> (and <code>typing_extensions</code>) that go beyond basic annotations. Enforced by mypy and Pydantic at parse time; ignored by the Python runtime.</p>"},{"location":"python/language/objects/typing/#literal-restrict-to-specific-values","title":"<code>Literal</code> \u2014 restrict to specific values","text":"<p>Narrows a type to a fixed set of constants rather than the broad base type.</p> <pre><code>from typing import Literal\n\nSide = Literal['BUY', 'SELL']\n\ndef place_order(side: Side) -&gt; None: ...\n\nplace_order('BUY')    # ok\nplace_order('hold')   # mypy error\n</code></pre> <p>Allowed value types: <code>str</code>, <code>int</code>, <code>bool</code>, <code>bytes</code>, <code>None</code>, Enum members. Not variables or mutable types.</p>"},{"location":"python/language/objects/typing/#subtype-direction","title":"Subtype direction","text":"<p>A plain str variable fails assignment to a Literal type</p> <p><code>Literal['BUY']</code> is a subtype of <code>str</code>, but <code>str</code> is not a subtype of <code>Literal['BUY', 'SELL']</code>. This means passing a <code>str</code> variable (even one that happens to hold <code>'BUY'</code>) to a <code>Literal</code>-typed parameter is a mypy error. You must either narrow the variable first (e.g. via a branch or <code>assert</code>) or use a <code>Literal</code> annotation at the assignment site.</p> <p><code>Literal['BUY']</code> is a subtype of <code>str</code> \u2014 narrowing is always safe. <code>str</code> is not a subtype of <code>Literal['BUY']</code> \u2014 a bare <code>str</code> variable could be anything.</p> <pre><code>x: str = 'BUY'\nplace_order(x)   # mypy error: str is not assignable to Literal['BUY', 'SELL']\n</code></pre>"},{"location":"python/language/objects/typing/#type-narrowing-in-branches","title":"Type narrowing in branches","text":"<pre><code>def process(side: Literal['BUY', 'SELL']) -&gt; None:\n    if side == 'BUY':\n        ...  # checker knows: side is Literal['BUY']\n    else:\n        ...  # checker knows: side is Literal['SELL']\n</code></pre>"},{"location":"python/language/objects/typing/#overloads-different-return-types-per-literal-argument","title":"Overloads \u2014 different return types per literal argument","text":"<pre><code>from typing import overload\n\n@overload\ndef parse(raw: str, mode: Literal['int']) -&gt; int: ...\n@overload\ndef parse(raw: str, mode: Literal['float']) -&gt; float: ...\ndef parse(raw: str, mode: str) -&gt; int | float:\n    return int(raw) if mode == 'int' else float(raw)\n\nx = parse(\"42\", \"int\")     # x: int\ny = parse(\"3.14\", \"float\") # y: float\n</code></pre>"},{"location":"python/language/objects/typing/#runtime-introspection","title":"Runtime introspection","text":"<pre><code>from typing import get_args\n\nget_args(Side)  # ('BUY', 'SELL')\n\n# Manual validation without Pydantic:\nif value not in get_args(Side):\n    raise ValueError(f\"invalid side: {value!r}\")\n</code></pre>"},{"location":"python/language/objects/typing/#literal-vs-enum","title":"<code>Literal</code> vs <code>Enum</code>","text":"<code>Literal</code> <code>Enum</code> Runtime type plain <code>str</code>/<code>int</code> <code>Enum</code> instance Methods none can add Pydantic JSON natural needs <code>.value</code> Use when static valid-value set, no logic need behaviour or identity"},{"location":"python/language/objects/typing/#typealias","title":"<code>TypeAlias</code>","text":"<pre><code>from typing import TypeAlias\nSide: TypeAlias = Literal['BUY', 'SELL']  # explicit alias (Python 3.10+)\n</code></pre> <p>Python 3.12 adds <code>type Side = Literal['BUY', 'SELL']</code> as a cleaner alternative.</p>"},{"location":"python/language/objects/typing/#version","title":"Version","text":"<p>Introduced in Python 3.8 (PEP 586).</p>"},{"location":"python/language/objects/warnings/","title":"Warnings","text":"<p>Warnings are advisory \u2014 they print a message and execution continues. Use them for deprecations, non-fatal anomalies, or when you catch an exception but the program can still proceed.</p>"},{"location":"python/language/objects/warnings/#basic-api","title":"Basic API","text":"<pre><code>import warnings\n\nwarnings.warn(\"something is off\")                         # UserWarning by default\nwarnings.warn(\"use new_fn() instead\", DeprecationWarning)\n</code></pre>"},{"location":"python/language/objects/warnings/#warning-hierarchy","title":"Warning hierarchy","text":"<pre><code>Warning  (subclass of Exception)\n\u251c\u2500\u2500 UserWarning          \u2190 general user-facing messages\n\u251c\u2500\u2500 DeprecationWarning   \u2190 for developers; hidden in production by default\n\u251c\u2500\u2500 FutureWarning        \u2190 for end-users about changing behaviour\n\u251c\u2500\u2500 RuntimeWarning       \u2190 suspicious runtime behaviour\n\u2514\u2500\u2500 ResourceWarning      \u2190 unclosed files/sockets\n</code></pre> <p><code>Warning</code> is a subclass of <code>Exception</code>, so you can catch warnings with <code>except Warning</code> and raise them as exceptions.</p>"},{"location":"python/language/objects/warnings/#generating-a-warning-from-an-exception","title":"Generating a warning from an exception","text":"<p>Catch the exception, downgrade it to a warning when the program can still proceed:</p> <pre><code>def load_config(path):\n    try:\n        with open(path) as f:\n            return json.load(f)\n    except FileNotFoundError as e:\n        warnings.warn(f\"Config not found ({e}), using defaults\", UserWarning, stacklevel=2)\n        return DEFAULT_CONFIG\n</code></pre> <p>Pass the exception as the message with <code>str(e)</code>, or build a Warning instance directly:</p> <pre><code>except json.JSONDecodeError as e:\n    warnings.warn(f\"Malformed JSON at line {e.lineno}: {e.msg}\", UserWarning, stacklevel=2)\n</code></pre>"},{"location":"python/language/objects/warnings/#stacklevel-point-at-the-caller-not-your-internals","title":"stacklevel \u2014 point at the caller, not your internals","text":"<p>stacklevel=2 is almost always the right value for library code</p> <p>With the default <code>stacklevel=1</code>, the warning points to the line inside your library where <code>warn()</code> is called \u2014 not useful to the caller. <code>stacklevel=2</code> points to whoever called your function, which is where the fix should be made. Increment further if <code>warn()</code> is nested inside a helper called by your public API.</p> <p><code>stacklevel=1</code> (default) points the warning at the <code>warn()</code> call \u2014 usually wrong. Use <code>stacklevel=2</code> to point at whoever called your function:</p> <pre><code>def parse(data):\n    warnings.warn(\"bad format\", stacklevel=2)  # blame the caller of parse()\n</code></pre> <p>Increment further if <code>warn()</code> is nested inside helpers.</p>"},{"location":"python/language/objects/warnings/#warning-filters","title":"Warning filters","text":"<p>The same warning (same message + location) is shown only once by default.</p> <pre><code>warnings.filterwarnings(\"ignore\", category=DeprecationWarning)\nwarnings.filterwarnings(\"error\", category=UserWarning)   # promote to exception\nwarnings.filterwarnings(\"always\")                        # show every occurrence\n</code></pre> <p>Temporarily override in a block:</p> <pre><code>with warnings.catch_warnings():\n    warnings.simplefilter(\"ignore\", DeprecationWarning)\n    result = old_api()\n</code></pre>"},{"location":"python/language/objects/warnings/#custom-warning-class","title":"Custom warning class","text":"<pre><code>class PrecisionWarning(UserWarning):\n    \"\"\"Floating-point precision loss detected.\"\"\"\n\nwarnings.warn(\"result may lose precision\", PrecisionWarning, stacklevel=2)\n</code></pre> <p>Lets callers filter by type: <code>warnings.filterwarnings(\"error\", category=PrecisionWarning)</code>.</p>"},{"location":"python/language/objects/warnings/#in-tests-pytest","title":"In tests (pytest)","text":"<pre><code># Assert a warning is emitted:\nwith pytest.warns(DeprecationWarning, match=\"use new_fn\"):\n    old_fn()\n</code></pre> <p>Promote all warnings to exceptions in <code>pyproject.toml</code>:</p> <pre><code>[tool.pytest.ini_options]\nfilterwarnings = [\"error\"]\n</code></pre>"},{"location":"python/language/objects/warnings/#related-notes","title":"Related notes","text":"<ul> <li>exceptions.md \u2014 exception hierarchy, raising, chaining, custom exceptions</li> <li>testing-strategy.md \u2014 <code>pytest.warns</code> alongside <code>pytest.raises</code></li> </ul>"},{"location":"python/language/runtime/","title":"Python \u2014 Language / Runtime","text":"File Type Description asyncio.md note async def, await, asyncio.run(), gather(), create_task() \u2014 core mechanics cli.md ref Running Python from the terminal: <code>-c</code>, <code>-m</code>, scripts concurrency.md note GIL, threading, multiprocessing, asyncio \u2014 overview and decision guide context-managers.md note <code>with</code> statement, class-based and <code>@contextmanager</code> datetime.md note datetime from strings and integer timestamps; timezone awareness; strptime, fromisoformat, fromtimestamp entrypoint.md note <code>__name__</code>, <code>if __name__ == \"__main__\"</code> guard, <code>__main__.py</code>, async <code>main</code> with <code>asyncio.run()</code> import-system.md note Modules, packages, <code>__init__.py</code>, <code>sys.path</code>, public API logging.md note stdlib logging: pipeline, levels, dictConfig, best practices, structured logging scopes.md note LEGB rule, local/global/nonlocal, closures, late-binding gotcha, class scope threading.md note <code>ThreadPoolExecutor</code>, <code>Future</code> mechanics, <code>as_completed</code>, exception handling, shared state"},{"location":"python/language/runtime/asyncio/","title":"asyncio \u2014 Core Mechanics","text":"<p>See concurrency.md for the comparison with threads and the decision guide.</p> <p>async def defines a factory, not a running task \u2014 calling it does nothing</p> <p><code>async def fetch(url)</code> is a function that returns a coroutine object. The body doesn't run until something drives it with <code>await</code> or schedules it as a Task. This surprises people used to regular functions, where calling = executing.</p>"},{"location":"python/language/runtime/asyncio/#async-def-coroutine-function-vs-coroutine-object","title":"async def: coroutine function vs coroutine object","text":"<p><code>async def</code> defines a coroutine function. Calling it returns an inert coroutine object \u2014 the body does not run:</p> <pre><code>async def fetch(url):\n    ...\n\nc = fetch(\"https://example.com\")   # returns coroutine object, runs nothing\n</code></pre> <p>Compare to a regular function, where calling it executes immediately. The coroutine object only runs when driven by <code>await</code> or the event loop. Discarding it unrun produces a <code>RuntimeWarning</code>.</p> <p>The most common bug this causes:</p> <pre><code>result = fetch(url)         # BUG: creates a coroutine object, discards it\nresult = await fetch(url)   # CORRECT\n</code></pre>"},{"location":"python/language/runtime/asyncio/#await-suspension-and-resumption","title":"await: suspension and resumption","text":"<p><code>await expr</code> suspends the current coroutine and yields control to the event loop, which can then run other coroutines. When the awaited thing completes, the event loop resumes this coroutine.</p> <pre><code>async def process(url):\n    response = await session.get(url)   # suspend \u2014 event loop runs others\n    text = await response.text()         # suspend again\n    return text\n</code></pre> <p><code>await</code> can only appear inside <code>async def</code>. You can only <code>await</code> an awaitable: coroutine objects, <code>Task</code>, <code>Future</code>, or objects with <code>__await__</code>.</p> <p><code>await</code> is not blocking \u2014 a blocked thread holds the GIL and does nothing; an awaited coroutine suspends and frees the event loop to run other work.</p>"},{"location":"python/language/runtime/asyncio/#asynciorun-the-entry-point","title":"asyncio.run(): the entry point","text":"<p>Bridges synchronous code into the async world. Creates an event loop, runs one top-level coroutine to completion, then closes the loop:</p> <pre><code>value = asyncio.run(main())   # blocks until main() returns\n</code></pre> <p>Can only be called from synchronous code \u2014 not from inside a running event loop. One loop at a time.</p>"},{"location":"python/language/runtime/asyncio/#asynciogather-concurrent-execution","title":"asyncio.gather(): concurrent execution","text":"<p>Schedules multiple coroutines as Tasks and runs them concurrently. Returns when all complete:</p> <pre><code>results = await asyncio.gather(\n    fetch(\"https://a.com\"),\n    fetch(\"https://b.com\"),\n    fetch(\"https://c.com\"),\n)\n# results = [result_a, result_b, result_c]  \u2014 submission order, not completion order\n</code></pre> <p>What gather does: wraps each argument in a <code>Task</code> (scheduled immediately), then suspends the caller until all Tasks finish.</p> <p>Exception handling:</p> <pre><code># Default (return_exceptions=False): first exception propagates, others cancelled\ntry:\n    results = await asyncio.gather(a(), b(), c())\nexcept Exception as e:\n    ...\n\n# return_exceptions=True: all run to completion; exceptions returned as values\nresults = await asyncio.gather(a(), b(), c(), return_exceptions=True)\nfor r in results:\n    if isinstance(r, Exception):\n        print(f\"failed: {r}\")\n</code></pre>"},{"location":"python/language/runtime/asyncio/#create_task-fire-and-overlap","title":"create_task(): fire and overlap","text":"<p><code>create_task()</code> schedules a coroutine without awaiting it immediately \u2014 the task runs whenever the event loop has cycles:</p> <pre><code>task = asyncio.create_task(fetch(url))   # scheduled, starts running\nawait other_work()                        # loop runs fetch() here too\nresult = await task                       # wait if not done\n</code></pre> <p>Use <code>create_task()</code> to overlap sequential work with background tasks. Use <code>gather()</code> for a batch you want to collect all at once.</p>"},{"location":"python/language/runtime/asyncio/#event-loop-mental-model","title":"Event loop \u2014 mental model","text":"<p>Single-threaded scheduler: runs one coroutine at a time, switches at every <code>await</code>. Concurrency comes from cooperative yielding, not parallelism.</p> <p>Any blocking call freezes the entire event loop</p> <p>The event loop is single-threaded. A <code>time.sleep(5)</code> or <code>requests.get(url)</code> inside <code>async def</code> blocks the thread for the full duration \u2014 no other coroutine runs. Replace with <code>await asyncio.sleep()</code> and async-native libraries (<code>aiohttp</code>, <code>asyncpg</code>, <code>aiofiles</code>). For unavoidable sync calls, use <code>await loop.run_in_executor(None, blocking_fn)</code> to offload to a thread pool.</p> <p>Consequence: any blocking call freezes all other coroutines:</p> <pre><code># BAD \u2014 blocks the whole event loop\nasync def bad():\n    time.sleep(5)\n    data = requests.get(url)\n\n# GOOD \u2014 suspends this coroutine, others keep running\nasync def good():\n    await asyncio.sleep(5)\n    async with aiohttp.ClientSession() as s:   # see aiohttp.md\n        data = await s.get(url)\n</code></pre> <p>Use async-native libraries (<code>aiohttp</code>, <code>asyncpg</code>, <code>aiofiles</code>). Offload unavoidable sync calls with <code>await loop.run_in_executor(None, blocking_fn, arg)</code>.</p>"},{"location":"python/language/runtime/asyncio/#async-context-managers-and-iterators","title":"Async context managers and iterators","text":"<pre><code>async with resource() as r:    # awaits __aenter__ / __aexit__\n    ...\n\nasync for item in aiter():     # awaits __anext__ each step\n    ...\n</code></pre>"},{"location":"python/language/runtime/asyncio/#key-vocabulary","title":"Key vocabulary","text":"Term Meaning Coroutine function <code>async def fn()</code> \u2014 calling it returns a coroutine object Coroutine object Inert result of calling a coroutine function; must be awaited to run <code>await</code> Suspends current coroutine; yields to event loop Event loop Scheduler \u2014 runs one coroutine at a time, switches at <code>await</code> Task A scheduled coroutine that runs independently <code>asyncio.run()</code> Creates loop, runs top-level coroutine, closes loop <code>asyncio.gather()</code> Runs N coroutines concurrently, returns all results <code>asyncio.create_task()</code> Schedules a coroutine as a Task without awaiting it"},{"location":"python/language/runtime/asyncio/#related-notes","title":"Related notes","text":"<ul> <li>concurrency.md \u2014 threads vs asyncio decision guide, GIL, multiprocessing</li> <li>context-managers.md \u2014 <code>async with</code> protocol (<code>__aenter__</code>/<code>__aexit__</code>)</li> <li>../../tooling/aiohttp.md \u2014 async HTTP client built on asyncio</li> </ul>"},{"location":"python/language/runtime/cli/","title":"Python CLI","text":""},{"location":"python/language/runtime/cli/#run-a-command","title":"Run a command","text":"<pre><code>python -c 'import sys'\n</code></pre> <pre><code>python -c script.py\n</code></pre>"},{"location":"python/language/runtime/cli/#run-a-module","title":"Run a module","text":"<p>Notice without <code>.py</code>:</p> <pre><code>python -m module\n</code></pre>"},{"location":"python/language/runtime/concurrency/","title":"Concurrency in Python: GIL, Threads, Processes, asyncio","text":""},{"location":"python/language/runtime/concurrency/#foundations","title":"Foundations","text":"<p>Process: OS-level container with private memory and at least one thread. Isolated \u2014 processes can't read each other's memory.</p> <p>Thread: unit of execution within a process. Threads share memory with each other, which makes them fast to create and dangerous to use carelessly.</p> <p>Preemptive scheduling: the OS interrupts threads every few milliseconds and switches between them, giving the illusion of simultaneous execution on a single core. Threads have no say in when they're interrupted.</p> <p>Blocking I/O: when a thread asks the OS for data (network, disk), the OS parks the thread and schedules other work. The thread does nothing until the data arrives. This \"wasted\" time is what concurrency exploits.</p> <p>Concurrency vs parallelism:</p> <ul> <li>Concurrency \u2014 multiple tasks in progress at once, possibly interleaved on one core.</li> <li>Parallelism \u2014 multiple tasks literally running simultaneously on separate cores.</li> </ul> <p>I/O-bound work only needs concurrency (one core is fine while threads wait). CPU-bound work needs parallelism (multiple cores actually computing).</p> <p>Preemptive vs cooperative concurrency</p> <p>Preemptive (threads): the OS interrupts a thread at any point \u2014 between any two bytecode instructions \u2014 and switches to another. The thread has no say. Consequence: shared data can be corrupted at any instruction boundary, so you need locks.</p> <p>Cooperative (asyncio): a coroutine runs uninterrupted until it explicitly yields with <code>await</code>. The scheduler only switches at those yield points. Consequence: no preemption surprises \u2014 but a coroutine that never <code>await</code>s blocks everything.</p> <p>The practical difference: in asyncio, you can reason about atomicity between <code>await</code> points; in threaded code, no such guarantee exists without explicit locking.</p>"},{"location":"python/language/runtime/concurrency/#the-gil","title":"The GIL","text":"<p>CPython uses reference counting for garbage collection. Because reference counts are not thread-safe, CPython uses a single lock \u2014 the Global Interpreter Lock \u2014 so that only one thread executes Python bytecode at a time.</p> <p>The GIL is released during:</p> <ul> <li>Any blocking I/O call (network, disk, <code>sleep</code>) \u2014 OS takes over, Python doesn't need it.</li> <li>C extensions that explicitly release it (numpy, scipy).</li> </ul> <p>The GIL protects CPython internals, not your shared data. Concurrent access to a Python <code>list</code> or <code>dict</code> still requires explicit locking.</p>"},{"location":"python/language/runtime/concurrency/#threading-io-bound-work","title":"Threading \u2014 I/O-bound work","text":"<p>While one thread waits for I/O (GIL released), another thread runs Python code. Threads genuinely overlap during I/O waits.</p> <pre><code>from concurrent.futures import ThreadPoolExecutor\n\nwith ThreadPoolExecutor(max_workers=10) as pool:\n    futures = [pool.submit(fetch, url) for url in urls]\n    results = [f.result() for f in futures]\n</code></pre> <p>See threading.md for <code>Future</code> mechanics, <code>as_completed</code>, exception handling, and shared state.</p>"},{"location":"python/language/runtime/concurrency/#multiprocessing-cpu-bound-work","title":"Multiprocessing \u2014 CPU-bound work","text":"<p>For CPU-bound work, threads don't help: the GIL limits execution to one thread at a time regardless of how many cores you have. Multiprocessing spawns separate OS processes, each with its own interpreter and GIL \u2014 real parallelism across cores.</p> <pre><code>from concurrent.futures import ProcessPoolExecutor\n\nwith ProcessPoolExecutor(max_workers=4) as pool:\n    results = list(pool.map(heavy_fn, inputs))\n</code></pre> <p>Costs:</p> <ul> <li>Data is pickled (serialised) to pass between processes \u2014 overhead for large inputs.</li> <li>Workers must be defined at module level (lambdas can't be pickled).</li> <li>Process startup is slow (~100ms); amortise over many tasks.</li> </ul> <p>For large arrays: use <code>multiprocessing.shared_memory</code> to avoid copying.</p> <p>numpy exception: numpy/scipy release the GIL during C operations \u2014 threads can parallelise numpy work. Profile before using <code>ProcessPoolExecutor</code> for numerical code.</p>"},{"location":"python/language/runtime/concurrency/#asyncio-io-bound-high-concurrency","title":"asyncio \u2014 I/O-bound, high concurrency","text":"<p>A single-threaded event loop runs coroutines cooperatively. Instead of the OS interrupting threads, coroutines voluntarily yield at <code>await</code> points. The event loop runs another coroutine while one waits for I/O. Scales to tens of thousands of concurrent tasks with low overhead (no per-thread stack, no OS context switches).</p> <p>See asyncio.md for a deep dive into <code>async def</code>, <code>await</code>, <code>asyncio.run()</code>, and <code>asyncio.gather()</code>.</p> <p>Key primitives:</p> <pre><code>await asyncio.gather(*coros)                         # run coroutines concurrently, collect results\nasyncio.create_task(coro())                          # schedule without waiting\nawait asyncio.sleep(n)                               # non-blocking sleep\nawait loop.run_in_executor(None, blocking_fn, arg)   # offload sync call to thread pool\n</code></pre> <p>Never call blocking code (e.g. <code>requests.get</code>, <code>time.sleep</code>) inside <code>async def</code> \u2014 it freezes the entire event loop.</p>"},{"location":"python/language/runtime/concurrency/#decision-guide","title":"Decision guide","text":"Workload Tool I/O-bound, few tasks <code>ThreadPoolExecutor</code> I/O-bound, high concurrency <code>asyncio</code> + async libraries CPU-bound, pure Python <code>ProcessPoolExecutor</code> CPU-bound, numpy/scipy Threads often fine (GIL released in C layer)"},{"location":"python/language/runtime/concurrency/#threads-vs-asyncio-for-io","title":"Threads vs asyncio for I/O","text":"Threads asyncio Context switching OS (preemptive) Cooperative (<code>await</code>) Scale Hundreds Tens of thousands Existing sync code Works as-is Must use <code>run_in_executor</code> or rewrite Risk Race conditions Blocking calls freeze the loop"},{"location":"python/language/runtime/concurrency/#key-vocabulary","title":"Key vocabulary","text":"Term Meaning Coroutine <code>async def</code> function that suspends at <code>await</code> and resumes later Event loop asyncio's scheduler \u2014 runs one coroutine at a time, switches at <code>await</code> Future Placeholder for a result that will exist later (<code>concurrent.futures</code>) Task asyncio's version of a Future \u2014 a scheduled coroutine Pickle Python's serialisation format; used to pass data between processes"},{"location":"python/language/runtime/concurrency/#related-notes","title":"Related notes","text":"<ul> <li>threading.md \u2014 <code>ThreadPoolExecutor</code> API, futures, <code>as_completed</code>, shared state</li> <li>asyncio.md \u2014 full async/await deep dive</li> <li>functools.md \u2014 <code>@lru_cache</code> thread-safety note</li> <li>context-managers.md \u2014 <code>async with</code> uses <code>__aenter__</code>/<code>__aexit__</code></li> </ul>"},{"location":"python/language/runtime/context-managers/","title":"Context Managers","text":"<p>Used to do something temporarily, then reliably undo it. Big advantage: hard to leave the environment in a bad state.</p> <p>The key guarantee: cleanup runs even if an exception is raised</p> <p><code>__exit__</code> (or the <code>finally</code> block in <code>@contextmanager</code>) is called regardless of whether the <code>with</code> body raised. This makes context managers the correct pattern for any resource that needs paired setup/teardown \u2014 files, locks, DB connections, temporary state changes \u2014 where a bare <code>try/finally</code> would work but is more verbose and easier to get wrong.</p> <pre><code>with manager() as x:\n    do_work(x)\n</code></pre> <p>Equivalent to:</p> <pre><code>x = setup()\ntry:\n    do_work(x)\nfinally:\n    cleanup()\n</code></pre> <p>Common uses: file reading, database connections, threading, timing, changing settings temporarily.</p>"},{"location":"python/language/runtime/context-managers/#class-based-context-manager","title":"Class-based Context Manager","text":"<p>Requires <code>__enter__</code> and <code>__exit__</code> methods.</p> <pre><code>class Manager:\n    def __enter__(self):\n        setup()\n        return value\n\n    def __exit__(self, exc_type, exc_val, exc_tb):\n        cleanup()\n        return False  # don't suppress exceptions\n</code></pre>"},{"location":"python/language/runtime/context-managers/#generator-based-contextmanager","title":"Generator-based (<code>@contextmanager</code>)","text":"<pre><code>from contextlib import contextmanager\n\n@contextmanager\ndef manager():\n    setup_code()\n    try:\n        yield value   # becomes the result of __enter__()\n    finally:\n        cleanup_code()  # behaves like __exit__()\n</code></pre>"},{"location":"python/language/runtime/datetime/","title":"Datetime","text":"<p>Converting between strings, integer timestamps, and <code>datetime.datetime</code> objects.</p>"},{"location":"python/language/runtime/datetime/#three-approaches","title":"Three approaches","text":""},{"location":"python/language/runtime/datetime/#strptime-known-format","title":"<code>strptime</code> \u2014 known format","text":"<pre><code>from datetime import datetime\n\ndt = datetime.strptime(\"2024-06-21 14:30:00\", \"%Y-%m-%d %H:%M:%S\")\n</code></pre> <p>Supply a format string using <code>strftime</code>/<code>strptime</code> codes. Raises <code>ValueError</code> on mismatch.</p> <p>Common codes:</p> Code Meaning Example <code>%Y</code> 4-digit year <code>2024</code> <code>%m</code> zero-padded month <code>06</code> <code>%d</code> zero-padded day <code>21</code> <code>%H</code> 24h hour <code>14</code> <code>%M</code> minutes <code>30</code> <code>%S</code> seconds <code>00</code> <code>%f</code> microseconds <code>000000</code> <code>%z</code> UTC offset <code>+0000</code> <code>%I</code> 12h hour <code>02</code> <code>%p</code> AM/PM <code>PM</code>"},{"location":"python/language/runtime/datetime/#fromisoformat-iso-8601-strings-python-37","title":"<code>fromisoformat</code> \u2014 ISO 8601 strings (Python \u2265 3.7)","text":"<pre><code>dt = datetime.fromisoformat(\"2024-06-21T14:30:00\")\ndt = datetime.fromisoformat(\"2024-06-21T14:30:00+05:30\")\n</code></pre> <ul> <li>Python 3.11+ handles trailing <code>Z</code> natively.</li> <li>On 3.10 and below: <code>s.replace(\"Z\", \"+00:00\")</code> before parsing.</li> </ul>"},{"location":"python/language/runtime/datetime/#dateutilparserparse-fuzzy-unknown-format","title":"<code>dateutil.parser.parse</code> \u2014 fuzzy / unknown format","text":"<pre><code>from dateutil import parser  # pip install python-dateutil\n\ndt = parser.parse(\"June 21 2024 2:30pm\")\n</code></pre> <p>Tries many formats automatically. Convenient for user input or heterogeneous sources; can silently misparse ambiguous strings like <code>01/02/03</code>.</p>"},{"location":"python/language/runtime/datetime/#which-to-use","title":"Which to use?","text":"Situation Use Known, fixed format <code>strptime</code> ISO 8601 from an API / database <code>fromisoformat</code> Unknown / user-supplied format <code>dateutil.parser.parse</code>"},{"location":"python/language/runtime/datetime/#timezone-awareness","title":"Timezone awareness","text":"<p>A <code>datetime</code> is either naive (no <code>tzinfo</code>) or aware (has <code>tzinfo</code>). Mixing them raises <code>TypeError</code>.</p> <pre><code>from datetime import datetime, timezone\n\n# naive\ndt = datetime.strptime(\"2024-06-21 14:30\", \"%Y-%m-%d %H:%M\")\ndt.tzinfo  # None\n\n# aware via format string\ndt = datetime.strptime(\"2024-06-21 14:30 +0000\", \"%Y-%m-%d %H:%M %z\")\n\n# attach tz to a naive datetime (only safe if you know the tz)\ndt = dt.replace(tzinfo=timezone.utc)\n</code></pre>"},{"location":"python/language/runtime/datetime/#going-the-other-way-datetime-string","title":"Going the other way (datetime \u2192 string)","text":"<pre><code>dt.strftime(\"%Y-%m-%d\")   # \u2192 \"2024-06-21\"\ndt.isoformat()             # \u2192 \"2024-06-21T14:30:00\"\n</code></pre>"},{"location":"python/language/runtime/datetime/#integer-timestamps","title":"Integer timestamps","text":"<p>A Unix timestamp is seconds since 1970-01-01 00:00:00 UTC. It has no timezone \u2014 that only enters when you convert it to a datetime.</p>"},{"location":"python/language/runtime/datetime/#seconds-datetime","title":"Seconds \u2192 datetime","text":"<p>Always pass tz= to fromtimestamp \u2014 without it you get naive local time</p> <p><code>datetime.fromtimestamp(ts)</code> uses the local machine's timezone, producing a naive datetime. In CI, servers, or Docker containers, \"local\" may be UTC; on a developer's machine it may not be. Always pass <code>tz=timezone.utc</code> explicitly. <code>datetime.utcfromtimestamp()</code> is deprecated in 3.12 for the same reason \u2014 it also returns naive UTC.</p> <pre><code>from datetime import datetime, timezone\n\nts = 1719000000\n\n# Always pass tz= \u2014 without it, fromtimestamp returns naive local time\ndt = datetime.fromtimestamp(ts, tz=timezone.utc)   # 2024-06-21 20:00:00+00:00\n\n# Specific timezone\nimport zoneinfo\ndt = datetime.fromtimestamp(ts, tz=zoneinfo.ZoneInfo(\"Europe/Rome\"))\n</code></pre> <p><code>datetime.utcfromtimestamp(ts)</code> returns a naive UTC datetime and is deprecated in Python 3.12. Avoid it.</p>"},{"location":"python/language/runtime/datetime/#millisecond-timestamps-most-apis-and-financial-data","title":"Millisecond timestamps (most APIs and financial data)","text":"<pre><code>ts_ms = 1719000000000          # e.g. from Binance, JavaScript Date.now()\ndt = datetime.fromtimestamp(ts_ms / 1000, tz=timezone.utc)\n</code></pre> <p>Sanity check: if the timestamp is &gt; ~2 \u00d7 10\u00b9\u2070 it's milliseconds, not seconds</p> <p>A Unix second timestamp for 2024 is ~1.7 \u00d7 10\u2079 (10 digits). A millisecond timestamp is ~1.7 \u00d7 10\u00b9\u00b2 (13 digits). Passing milliseconds to <code>fromtimestamp()</code> without dividing by 1000 silently produces a date in the year 56,000. This is the most common financial data parsing bug.</p>"},{"location":"python/language/runtime/datetime/#datetime-integer-timestamp","title":"datetime \u2192 integer timestamp","text":"<pre><code>dt = datetime(2024, 6, 21, 20, 0, 0, tzinfo=timezone.utc)\nts     = int(dt.timestamp())          # seconds  \u2192 1719000000\nts_ms  = int(dt.timestamp() * 1000)  # milliseconds \u2192 1719000000000\n</code></pre> <p><code>timestamp()</code> on a naive datetime assumes local time \u2014 the same footgun as <code>fromtimestamp()</code>. Always use aware datetimes for round-trips.</p>"},{"location":"python/language/runtime/datetime/#resolution-summary","title":"Resolution summary","text":"Unit Example Common source seconds <code>1719000000</code> POSIX, most DBs, HTTP headers milliseconds <code>1719000000000</code> JS, Binance, most financial APIs microseconds <code>1719000000000000</code> PostgreSQL nanoseconds <code>1719000000000000000</code> Kafka, InfluxDB \u2014 <code>datetime</code> loses sub-\u00b5s precision"},{"location":"python/language/runtime/entrypoint/","title":"Python Entry Points","text":""},{"location":"python/language/runtime/entrypoint/#__name__-and-the-guard","title":"<code>__name__</code> and the guard","text":"<p>Every module gets a <code>__name__</code> attribute at import time: - <code>\"__main__\"</code> when the file is run directly (<code>python script.py</code>) - The module's dotted name (e.g. <code>\"mypackage.utils\"</code>) when imported</p> <p>The guard exploits this to let a file act as both a script and an importable library:</p> <pre><code>def main():\n    ...\n\nif __name__ == \"__main__\":\n    main()\n</code></pre> <p>Without the guard, every <code>import</code> would execute the top-level code as a side effect. Most tooling (pytest, mypy) imports files \u2014 the guard is important even for files you \"never plan to import.\"</p> <p><code>main</code> is purely conventional \u2014 Python enforces nothing about the name.</p>"},{"location":"python/language/runtime/entrypoint/#__main__py-runnable-packages","title":"<code>__main__.py</code> \u2014 runnable packages","text":"<p>Add <code>__main__.py</code> to a package to make it runnable with <code>python -m mypackage</code>:</p> <pre><code>mypackage/\n    __init__.py\n    __main__.py   \u2190 runs when: python -m mypackage\n</code></pre> <p>This is how <code>python -m pytest</code>, <code>python -m http.server</code>, etc. work.</p>"},{"location":"python/language/runtime/entrypoint/#async-main-what-changes","title":"Async <code>main</code>: what changes","text":"<p>Calling async def main() without asyncio.run() silently does nothing</p> <p><code>main()</code> returns a coroutine object. Without <code>asyncio.run()</code>, the object is created and immediately discarded \u2014 the body never executes. Python emits a <code>RuntimeWarning: coroutine 'main' was never awaited</code> but does not raise an exception. The program appears to finish successfully.</p> <p>Calling <code>async def main()</code> returns an inert coroutine object \u2014 nothing runs:</p> <pre><code>async def main():\n    ...\n\nif __name__ == \"__main__\":\n    main()   # BUG: coroutine object created, not run; RuntimeWarning\n</code></pre> <p>Use <code>asyncio.run()</code> to create an event loop and drive the coroutine to completion:</p> <pre><code>import asyncio\n\nasync def main():\n    await some_async_work()\n\nif __name__ == \"__main__\":\n    asyncio.run(main())   # creates loop \u2192 runs main() \u2192 closes loop\n</code></pre> <p><code>asyncio.run()</code> always creates a fresh loop (no reuse), handles task cleanup, and must be called from synchronous code. Calling it inside an already-running event loop raises <code>RuntimeError</code> \u2014 inside async code, just <code>await main()</code>.</p>"},{"location":"python/language/runtime/entrypoint/#patterns-at-a-glance","title":"Patterns at a glance","text":"Scenario Pattern Sync script <code>def main()</code> + <code>if __name__ == \"__main__\": main()</code> Async script <code>async def main()</code> + <code>if __name__ == \"__main__\": asyncio.run(main())</code> Runnable package <code>__main__.py</code> in package Test an async entry point <code>asyncio.run(main())</code> in the test body"},{"location":"python/language/runtime/entrypoint/#related","title":"Related","text":"<ul> <li>asyncio.md \u2014 event loop, <code>await</code>, <code>gather</code>, <code>create_task</code></li> <li>cli.md \u2014 CLI flags for running Python (<code>-c</code>, <code>-m</code>)</li> </ul>"},{"location":"python/language/runtime/import-system/","title":"The Import System","text":"<ul> <li><code>sys.path</code> lists the directories Python searches when importing a module.</li> <li>The file <code>__init__.py</code> makes a directory a package (importable namespace).</li> <li>When imported, a module generates a <code>__pycache__</code> directory with bytecode compilation cache.</li> </ul> <pre><code>import sys\nprint(sys.path)         # where Python looks for modules\n\nimport finlib\nprint(finlib.__file__)  # where the package lives\nprint(finlib.__spec__)  # full module spec\n</code></pre>"},{"location":"python/language/runtime/import-system/#packages-and-__init__py","title":"Packages and <code>__init__.py</code>","text":"<p>A package is a directory with an <code>__init__.py</code> file. Without it, <code>import finlib</code> fails even if the directory exists on <code>sys.path</code>.</p> <pre><code>finlib/\n\u251c\u2500\u2500 __init__.py        \u2190 makes finlib importable\n\u251c\u2500\u2500 models/\n\u2502   \u251c\u2500\u2500 __init__.py    \u2190 makes finlib.models a subpackage\n\u2502   \u2514\u2500\u2500 trade.py       \u2190 importable as finlib.models.trade\n\u2514\u2500\u2500 utils/\n    \u251c\u2500\u2500 __init__.py\n    \u2514\u2500\u2500 math.py\n</code></pre> <p><code>__init__.py</code> can be empty \u2014 presence alone is enough. Common uses:</p> <p>Flattening the public API (most common) \u2014 re-export symbols so callers don't need to know your internal file structure:</p> <pre><code># finlib/models/__init__.py\nfrom .trade import Trade          # \".\" = current package (relative import)\nfrom .portfolio import Portfolio\n\n# users can now write:\nfrom finlib.models import Trade   # instead of finlib.models.trade.Trade\n</code></pre> <p><code>__all__</code> \u2014 controls what <code>from package import *</code> exports:</p> <pre><code>__all__ = ['Trade', 'Portfolio']\n</code></pre> <p>Practical rules: - Empty <code>__init__.py</code> for internal subpackages \u2014 just to make them importable. - Use re-exports in the top-level <code>__init__.py</code> to define your public API. - Don't import heavy dependencies at the top of <code>__init__.py</code> \u2014 it runs on every <code>import finlib</code>.</p> <p>Imports in init.py run on every import of the package</p> <p><code>from finlib.models import Trade</code> triggers <code>finlib/models/__init__.py</code> to execute. Any slow import (numpy, pandas, a DB driver) placed there adds latency to every module that imports from your package. Keep <code>__init__.py</code> thin \u2014 only re-export symbols; let submodules do heavy lifting lazily.</p> <p>Python 3.3+ also supports namespace packages (directories without <code>__init__.py</code>) for splitting a package across multiple directories. This is a niche feature; for normal projects always use <code>__init__.py</code>.</p>"},{"location":"python/language/runtime/logging/","title":"Logging","text":"<p>Python's <code>logging</code> stdlib module routes diagnostic messages through a configurable pipeline. Prefer it over <code>print</code> for anything beyond a throwaway script: you get severity filtering, multiple output destinations, structured metadata, and zero-code reconfiguration.</p>"},{"location":"python/language/runtime/logging/#pipeline","title":"Pipeline","text":"<pre><code>Logger \u2192 [Filter] \u2192 Handler \u2192 [Filter] \u2192 Formatter \u2192 output\n</code></pre> <ul> <li>Logger \u2014 the object your code calls; created via <code>logging.getLogger(__name__)</code>.</li> <li>Handler \u2014 sends a <code>LogRecord</code> somewhere (stream, file, queue, SMTP\u2026).</li> <li>Formatter \u2014 converts a record to a string (or JSON).</li> <li>Filter \u2014 optional accept/reject gate on a logger or handler.</li> </ul>"},{"location":"python/language/runtime/logging/#logger-hierarchy","title":"Logger hierarchy","text":"<p>Loggers are named with dotted paths that mirror Python packages:</p> <pre><code>root \u2192 myapp \u2192 myapp.services \u2192 myapp.services.orders\n</code></pre> <p><code>propagate=True</code> (default) makes records bubble up to the parent. One <code>StreamHandler</code> on root catches everything.</p>"},{"location":"python/language/runtime/logging/#levels","title":"Levels","text":"Level Value Use DEBUG 10 Verbose trace; off in production INFO 20 Normal milestones WARNING 30 Unexpected but recoverable (default) ERROR 40 Failed operation; program continues CRITICAL 50 Fatal <p>A record is emitted only when <code>record.level &gt;= logger.effectiveLevel</code> and <code>record.level &gt;= handler.level</code>. Effective level is inherited from the nearest ancestor that has one set.</p>"},{"location":"python/language/runtime/logging/#standard-module-pattern","title":"Standard module pattern","text":"<p>Every module does only this:</p> <pre><code>import logging\nlog = logging.getLogger(__name__)\n</code></pre> <p>No <code>basicConfig</code>, no <code>addHandler</code> \u2014 that's the application's job.</p>"},{"location":"python/language/runtime/logging/#configuration","title":"Configuration","text":""},{"location":"python/language/runtime/logging/#basicconfig-scripts","title":"<code>basicConfig</code> \u2014 scripts","text":"<pre><code>logging.basicConfig(\n    level=logging.DEBUG,\n    format=\"%(asctime)s [%(levelname)s] %(name)s: %(message)s\",\n    handlers=[logging.StreamHandler(), logging.FileHandler(\"app.log\")],\n)\n</code></pre> <p>No-op if any handler is already on the root logger. Call once, early.</p>"},{"location":"python/language/runtime/logging/#dictconfig-applications","title":"<code>dictConfig</code> \u2014 applications","text":"<pre><code>import logging.config\n\nlogging.config.dictConfig({\n    \"version\": 1,\n    \"disable_existing_loggers\": False,   # never set True \u2014 silences imported loggers\n    \"formatters\": {\n        \"std\": {\"format\": \"%(asctime)s [%(levelname)s] %(name)s: %(message)s\"},\n    },\n    \"handlers\": {\n        \"console\": {\"class\": \"logging.StreamHandler\", \"formatter\": \"std\"},\n        \"file\": {\n            \"class\": \"logging.handlers.RotatingFileHandler\",\n            \"formatter\": \"std\",\n            \"filename\": \"app.log\",\n            \"maxBytes\": 10_000_000,\n            \"backupCount\": 5,\n        },\n    },\n    \"root\": {\"level\": \"INFO\", \"handlers\": [\"console\", \"file\"]},\n    \"loggers\": {\n        \"myapp.db\": {\"level\": \"DEBUG\"},\n        \"httpx\": {\"level\": \"WARNING\"},\n    },\n})\n</code></pre>"},{"location":"python/language/runtime/logging/#library-rule","title":"Library rule","text":"<p>Libraries must never call basicConfig or add handlers</p> <p>If a library calls <code>basicConfig</code>, it silently configures the root logger for every application that imports it \u2014 hijacking the application's logging setup. Libraries should only add a <code>NullHandler</code> to their own logger. Configuration belongs exclusively to the application entry point.</p> <p>Libraries never call <code>basicConfig</code> or add handlers. Add only a <code>NullHandler</code> to prevent \"No handler found\" warnings:</p> <pre><code># in library __init__.py\nlogging.getLogger(__name__).addHandler(logging.NullHandler())\n</code></pre>"},{"location":"python/language/runtime/logging/#key-practices","title":"Key practices","text":"<p><code>%</code>-style formatting \u2014 deferred until the record is actually emitted: <pre><code>log.debug(\"parsed %d records from %s\", n, fname)   # good\nlog.debug(f\"parsed {n} records from {fname}\")       # always evaluated\n</code></pre></p> <p>Exceptions with traceback: <pre><code>try:\n    risky()\nexcept Exception:\n    log.exception(\"risky() failed\")   # = log.error(..., exc_info=True)\n</code></pre></p> <p>Structured context via <code>extra</code>: <pre><code>log.info(\"order placed\", extra={\"order_id\": 123})\n</code></pre></p> <p>Per-request context with <code>LoggerAdapter</code>: <pre><code>class ReqAdapter(logging.LoggerAdapter):\n    def process(self, msg, kw):\n        kw.setdefault(\"extra\", {})[\"req_id\"] = self.extra[\"req_id\"]\n        return msg, kw\n\nlog = ReqAdapter(logging.getLogger(__name__), {\"req_id\": req_id})\n</code></pre></p> <p>Async context propagation with <code>contextvars</code> (see asyncio.md): <pre><code>from contextvars import ContextVar\nreq_id: ContextVar[str] = ContextVar(\"req_id\", default=\"-\")\n\nclass CtxFilter(logging.Filter):\n    def filter(self, record):\n        record.req_id = req_id.get()\n        return True\n</code></pre></p>"},{"location":"python/language/runtime/logging/#json-structured-logging","title":"JSON / structured logging","text":"<p>For log aggregators (Datadog, Loki, CloudWatch):</p> <pre><code>pip install python-json-logger\n</code></pre> <pre><code>from pythonjsonlogger import jsonlogger\nhandler.setFormatter(jsonlogger.JsonFormatter(\n    \"%(asctime)s %(name)s %(levelname)s %(message)s\"\n))\n# extra fields are merged into the JSON object\n</code></pre> <p>Alternative: <code>structlog</code> \u2014 richer API, keyword-argument style, pluggable renderers.</p>"},{"location":"python/language/runtime/logging/#async-io-queuehandler-queuelistener","title":"Async I/O: QueueHandler + QueueListener","text":"<p>Standard <code>log.info()</code> is synchronous \u2014 it briefly blocks the event loop. For high-throughput async apps, offload I/O to a thread:</p> <pre><code>import queue, logging.handlers\n\nq = queue.Queue(-1)\nlistener = logging.handlers.QueueListener(q, file_handler, respect_handler_level=True)\nlistener.start()\nlogging.getLogger().addHandler(logging.handlers.QueueHandler(q))\n# listener.stop() at shutdown\n</code></pre> <p>See concurrency.md for threading context.</p>"},{"location":"python/language/runtime/logging/#rotating-files","title":"Rotating files","text":"<pre><code>from logging.handlers import RotatingFileHandler, TimedRotatingFileHandler\n\nRotatingFileHandler(\"app.log\", maxBytes=10_000_000, backupCount=5)\nTimedRotatingFileHandler(\"app.log\", when=\"midnight\", backupCount=30)\n</code></pre>"},{"location":"python/language/runtime/logging/#setup-checklist","title":"Setup checklist","text":"<ol> <li>Entry point only: call <code>dictConfig</code> or <code>basicConfig</code>.</li> <li>Every module: <code>log = logging.getLogger(__name__)</code>.</li> <li>Libraries: <code>NullHandler</code> only.</li> <li><code>%</code>-style args in log calls.</li> <li><code>log.exception()</code> inside <code>except</code> blocks.</li> <li>Silence noisy deps: <code>logging.getLogger(\"httpx\").setLevel(logging.WARNING)</code>.</li> <li>Production: JSON formatter + rotating file or sink.</li> <li>Async + file I/O: <code>QueueHandler</code> + <code>QueueListener</code>.</li> </ol>"},{"location":"python/language/runtime/scopes/","title":"Scopes and Namespaces","text":"<p>A namespace is a dict mapping names to objects. Every module, every function call, and every class body gets its own. Name resolution searches them in LEGB order.</p>"},{"location":"python/language/runtime/scopes/#legb-rule","title":"LEGB rule","text":"<p>When Python sees a bare name, it searches four scopes in order and stops at the first hit:</p> Level Scope Example L \u2014 Local current function's namespace <code>x = 1</code> inside <code>def f()</code> E \u2014 Enclosing any enclosing function's namespace closure variables G \u2014 Global module top-level namespace <code>x = 1</code> at module level B \u2014 Built-in <code>builtins</code> module <code>len</code>, <code>print</code>, <code>range</code> <pre><code>x = \"global\"\n\ndef outer():\n    x = \"enclosing\"\n    def inner():\n        x = \"local\"\n        print(x)   # \"local\"\n    inner()\n    print(x)       # \"enclosing\"\n\nouter()\nprint(x)           # \"global\"\n</code></pre>"},{"location":"python/language/runtime/scopes/#local-variables","title":"Local variables","text":"<p>Any name assigned anywhere inside a function is local \u2014 Python decides at compile time, not runtime.</p> <pre><code>x = 10\n\ndef f():\n    print(x)   # UnboundLocalError \u2014 x is assigned below, so Python\n    x = 20     # classifies it as local for the whole function\n</code></pre> <p>This is the most common scope bug: Python sees an assignment for <code>x</code> in the function body and makes it local everywhere in that function, including lines above the assignment.</p>"},{"location":"python/language/runtime/scopes/#global-write-to-module-level-from-inside-a-function","title":"<code>global</code> \u2014 write to module-level from inside a function","text":"<p>Reading a global works without any declaration. Assigning requires <code>global</code>:</p> <pre><code>counter = 0\n\ndef increment():\n    global counter   # without this: UnboundLocalError\n    counter += 1\n</code></pre> <p>Only needed when rebinding the name. Mutating a mutable global (<code>.append</code>, <code>.update</code>) doesn't need <code>global</code> \u2014 you're not reassigning the name, just modifying the object.</p>"},{"location":"python/language/runtime/scopes/#nonlocal-write-to-an-enclosing-functions-variable","title":"<code>nonlocal</code> \u2014 write to an enclosing function's variable","text":"<p>Same problem one scope up \u2014 assignment creates a local instead of updating the closure variable:</p> <pre><code>def make_counter():\n    count = 0\n    def increment():\n        nonlocal count   # without this: UnboundLocalError\n        count += 1\n    return increment\n</code></pre> <p><code>nonlocal</code> searches outward through enclosing function scopes (never global or built-in).</p>"},{"location":"python/language/runtime/scopes/#closures","title":"Closures","text":"<p>A function defined inside another function captures the enclosing variable, not its value:</p> <pre><code>def make_multiplier(n):\n    def multiply(x):\n        return x * n   # n is a free variable \u2014 captured from enclosing scope\n    return multiply\n\ndouble = make_multiplier(2)\ndouble(5)  # \u2192 10\n</code></pre> <p>Closures capture the variable, not its value \u2014 the late-binding gotcha</p> <p>All three lambdas in <code>[lambda: i for i in range(3)]</code> share the same <code>i</code> variable. By the time any of them is called, the loop has finished and <code>i == 2</code>. The fix is the default-argument trick: <code>lambda i=i: i</code> captures the current value at definition time because default arguments are evaluated once when the <code>lambda</code> statement executes.</p>"},{"location":"python/language/runtime/scopes/#late-binding-gotcha","title":"Late-binding gotcha","text":"<p>Closures capture the variable, not its value at capture time:</p> <pre><code>funcs = [lambda: i for i in range(3)]\nfuncs[0]()  # \u2192 2, not 0 \u2014 all three see the final value of i\n</code></pre> <p>Fix: bind the value as a default argument (evaluated at definition time):</p> <pre><code>funcs = [lambda i=i: i for i in range(3)]\nfuncs[0]()  # \u2192 0\n</code></pre> <p>Class scope is not part of the LEGB chain for methods</p> <p>A bare name inside a method skips the class body entirely, jumping straight from Local to Enclosing (outer functions) to Global. Class attributes must always be accessed as <code>self.x</code> or <code>ClassName.x</code> \u2014 never as a bare <code>x</code> inside a method.</p>"},{"location":"python/language/runtime/scopes/#class-scope-is-not-enclosing-scope","title":"Class scope is not enclosing scope","text":"<p>Class bodies create a namespace, but methods defined inside them cannot access it via bare names:</p> <pre><code>class C:\n    x = 10\n    def f(self):\n        return x        # NameError \u2014 class scope is not in LEGB chain\n        return self.x   # correct\n</code></pre>"},{"location":"python/language/runtime/scopes/#quick-reference","title":"Quick reference","text":"Goal Syntax Read a global just use the name \u2014 LEGB finds it Write to a global <code>global x</code> inside the function Write to an enclosing variable <code>nonlocal x</code> inside the inner function Capture value (not variable) in a closure <code>lambda x=x: x</code> default-argument trick"},{"location":"python/language/runtime/scopes/#related","title":"Related","text":"<ul> <li>import-system.md \u2014 module namespaces (<code>__dict__</code>, <code>sys.modules</code>)</li> <li>../functional/iterators-generators.md \u2014 generator functions and their frame state</li> <li>../functional/lazy-evaluation.md \u2014 closures as lazy pipelines</li> </ul>"},{"location":"python/language/runtime/threading/","title":"Threading \u2014 <code>concurrent.futures</code> API","text":"<p><code>ThreadPoolExecutor</code> is the high-level interface for running I/O-bound work across threads. See concurrency.md for when to use threads vs multiprocessing vs asyncio.</p>"},{"location":"python/language/runtime/threading/#basic-usage","title":"Basic usage","text":"<pre><code>from concurrent.futures import ThreadPoolExecutor\n\nwith ThreadPoolExecutor(max_workers=10) as pool:\n    futures = [pool.submit(fetch, url) for url in urls]\n    results = [f.result() for f in futures]  # .result() blocks; re-raises exceptions\n</code></pre> <p>Manual thread control:</p> <pre><code>import threading\n\nt = threading.Thread(target=fn, args=(x,), daemon=True)\nt.start()\nt.join()   # wait for completion\n</code></pre> <p><code>daemon=True</code> \u2014 thread is killed when the main program exits without waiting for it.</p>"},{"location":"python/language/runtime/threading/#future-mechanics","title":"Future mechanics","text":"<p><code>submit()</code> returns a <code>Future</code> immediately \u2014 the task is scheduled and the caller moves on. <code>.result()</code> is where blocking actually happens:</p> <pre><code>f1 = pool.submit(fetch, url_a)   # returns at once\nf2 = pool.submit(fetch, url_b)   # returns at once\nr1 = f1.result()                 # blocks until f1 done\nr2 = f2.result()                 # then blocks until f2 done\n</code></pre> <p>Trap: iterating <code>.result()</code> in submission order means you wait on <code>f1</code> even if <code>f2</code> finished first.</p>"},{"location":"python/language/runtime/threading/#as_completed-process-results-as-they-arrive","title":"<code>as_completed</code> \u2014 process results as they arrive","text":"<pre><code>from concurrent.futures import as_completed\n\nfutures = {pool.submit(fetch, url): url for url in urls}\nfor f in as_completed(futures):\n    url = futures[f]   # recover original arg from the dict\n    try:\n        data = f.result()\n    except Exception as e:\n        print(f\"{url} failed: {e}\")\n</code></pre> <p><code>as_completed()</code> yields futures in completion order, not submission order. The <code>{submit(fn, x): x}</code> dict is the idiomatic way to map a future back to its input.</p>"},{"location":"python/language/runtime/threading/#exception-handling","title":"Exception handling","text":"<p>Exceptions raised inside a thread are captured in the <code>Future</code> \u2014 they don't propagate until you call <code>.result()</code>:</p> <pre><code>f = pool.submit(bad_fn, 42)\n# no error in the main thread yet\nf.result()           # raises here\n\nf.exception()        # returns the exception object without raising, or None\nf.done()             # True if finished (success or error)\n</code></pre> <p><code>as_completed()</code> yields failed futures too \u2014 use <code>f.exception()</code> to inspect without aborting the batch.</p>"},{"location":"python/language/runtime/threading/#shared-state","title":"Shared state","text":"<p>counter += 1 is not atomic \u2014 concurrent threads will corrupt it</p> <p><code>counter += 1</code> compiles to three bytecode instructions: LOAD, ADD, STORE. The GIL can be released between any two of them, letting another thread read the pre-increment value. The result: updates are silently lost. Use <code>threading.Lock()</code> around any read-modify-write cycle, or <code>queue.Queue</code> to communicate between threads without shared mutable state.</p> <p>Threads share memory. <code>counter += 1</code> is not atomic (read \u2192 add \u2192 write), so concurrent threads can corrupt it:</p> <pre><code>lock = threading.Lock()\nwith lock:\n    counter += 1   # only one thread at a time\n</code></pre> <p><code>queue.Queue</code> is thread-safe by design \u2014 the idiomatic way to pass work items between threads without explicit locking.</p>"},{"location":"python/language/runtime/threading/#related-notes","title":"Related notes","text":"<ul> <li>concurrency.md \u2014 GIL, when to use threads vs processes vs asyncio, decision guide</li> <li>asyncio.md \u2014 cooperative concurrency alternative for high-scale I/O</li> </ul>"},{"location":"python/tooling/","title":"Python \u2014 Tooling","text":"File Type Description aiohttp.md note Async HTTP client: ClientSession, concurrency, streaming, error handling mypy.md note Static type checking: concepts, configuration, mental model poetry.md note Dependency and virtual environment management pydantic.md note Data validation and parsing at application boundaries pydantic-settings.md note Configuration via BaseSettings: env vars, .env files, type coercion pyenv.md ref Python version management commands ruff.md ref Linter and formatter replacing flake8/black/isort structlog.md note Structured logging: processor pipeline, context binding, JSON output terminal-tables.md note Print tables in the terminal: f-strings, tabulate, rich testing/ \u2014 pytest, testing strategy, testing patterns"},{"location":"python/tooling/aiohttp/","title":"aiohttp","text":"<p>Async HTTP client (and server) library for asyncio. The standard replacement for <code>requests</code> in async code \u2014 suspends the coroutine during network waits instead of blocking the thread.</p>"},{"location":"python/tooling/aiohttp/#clientsession-the-core-abstraction","title":"ClientSession \u2014 the core abstraction","text":"<p>Create one ClientSession per application, not one per request</p> <p>Each <code>ClientSession()</code> creates a new TCP connection pool. Creating and destroying a session per request throws away those connections, adding TLS handshake overhead on every call and defeating HTTP keep-alive. Create one session at startup (or in the top-level <code>async with</code> block) and share it across all requests.</p> <p>Manages a connection pool, cookie jar, and request defaults. Create one session per application, not one per request.</p> <pre><code># BAD \u2014 new TCP connection on every call\nasync def fetch(url):\n    async with aiohttp.ClientSession() as session:  # \u2190 don't do this in a loop\n        async with session.get(url) as resp:\n            return await resp.text()\n\n# GOOD \u2014 one session, many requests\nasync def main(urls):\n    async with aiohttp.ClientSession() as session:\n        results = await asyncio.gather(*[fetch(session, url) for url in urls])\n</code></pre>"},{"location":"python/tooling/aiohttp/#basic-request","title":"Basic request","text":"<pre><code>import aiohttp, asyncio\n\nasync def main():\n    async with aiohttp.ClientSession() as session:\n        async with session.get(\"https://api.frankfurter.app/latest\") as resp:\n            resp.raise_for_status()       # raises ClientResponseError on 4xx/5xx\n            data = await resp.json()      # body is read lazily here\n    return data\n\nresult = asyncio.run(main())\n</code></pre> <p>Body reading methods (all are coroutines \u2014 must <code>await</code>):</p> Method Returns <code>await resp.json()</code> dict/list <code>await resp.text()</code> str <code>await resp.read()</code> bytes <code>resp.status</code> int (attribute, not coroutine)"},{"location":"python/tooling/aiohttp/#requests","title":"Requests","text":"<pre><code>session.get(url, params={\"page\": 2})           # query string\nsession.post(url, json={\"key\": \"val\"})          # JSON body\nsession.post(url, data={\"key\": \"val\"})          # form-encoded body\nsession.put(url, headers={\"X-Token\": \"abc\"})    # per-request headers\n</code></pre> <p>Default session headers / base URL: <pre><code>aiohttp.ClientSession(\n    base_url=\"https://api.example.com\",\n    headers={\"Authorization\": f\"Bearer {token}\"},\n)\n# then: session.get(\"/v1/data\")\n</code></pre></p>"},{"location":"python/tooling/aiohttp/#timeouts","title":"Timeouts","text":"<p>No default \u2014 always set one:</p> <pre><code>timeout = aiohttp.ClientTimeout(total=30, connect=5)\naiohttp.ClientSession(timeout=timeout)\n</code></pre> <p>Raises <code>asyncio.TimeoutError</code> when exceeded.</p>"},{"location":"python/tooling/aiohttp/#error-handling","title":"Error handling","text":"<pre><code>try:\n    async with session.get(url) as resp:\n        resp.raise_for_status()\n        data = await resp.json()\nexcept aiohttp.ClientResponseError as e:    # 4xx/5xx\n    print(f\"HTTP {e.status}: {e.message}\")\nexcept aiohttp.ClientConnectorError:        # DNS/connection failure\n    ...\nexcept asyncio.TimeoutError:\n    ...\n</code></pre>"},{"location":"python/tooling/aiohttp/#concurrent-requests","title":"Concurrent requests","text":"<pre><code>async def fetch_one(session, url):\n    async with session.get(url) as resp:\n        resp.raise_for_status()\n        return await resp.json()\n\nasync def fetch_all(urls):\n    async with aiohttp.ClientSession(timeout=aiohttp.ClientTimeout(total=10)) as session:\n        return await asyncio.gather(\n            *[fetch_one(session, url) for url in urls],\n            return_exceptions=True,\n        )\n</code></pre> <p>Cap concurrency with a semaphore for rate-limited APIs: <pre><code>sem = asyncio.Semaphore(5)\n\nasync def fetch_one(session, url):\n    async with sem:\n        async with session.get(url) as resp:\n            return await resp.json()\n</code></pre></p>"},{"location":"python/tooling/aiohttp/#connection-pool","title":"Connection pool","text":"<pre><code>aiohttp.ClientSession(\n    connector=aiohttp.TCPConnector(limit=100, limit_per_host=10)\n)\n</code></pre>"},{"location":"python/tooling/aiohttp/#streaming","title":"Streaming","text":"<p>For large downloads or streaming APIs (SSE, NDJSON), don't buffer the whole body:</p> <pre><code>async with session.get(url) as resp:\n    async for chunk in resp.content.iter_chunked(65536):\n        file.write(chunk)\n\n# line-by-line (SSE / NDJSON)\nasync for line in resp.content:\n    process(line.strip())\n</code></pre>"},{"location":"python/tooling/aiohttp/#gotchas","title":"Gotchas","text":"<ul> <li><code>resp.json()</code> raises if <code>Content-Type</code> is not JSON \u2014 use <code>await resp.json(content_type=None)</code> to skip the check.</li> <li><code>time.sleep()</code> and <code>requests.get()</code> inside an async function freeze the whole event loop \u2014 use <code>await asyncio.sleep()</code> and async libraries.</li> <li>Always <code>async with</code> the session, or call <code>await session.close()</code> manually.</li> </ul>"},{"location":"python/tooling/aiohttp/#see-also","title":"See also","text":"<ul> <li>asyncio.md \u2014 event loop, gather, create_task</li> <li>market-data-apis.md \u2014 free public endpoints to call with aiohttp</li> </ul>"},{"location":"python/tooling/mypy/","title":"Mypy","text":""},{"location":"python/tooling/mypy/#what-mypy-is","title":"What mypy is","text":"<ul> <li>mypy is a static type checker for Python.</li> <li>It analyses your code before runtime and checks whether your type hints are used consistently.</li> <li>Python itself remains dynamically typed; mypy is an external development tool.</li> </ul>"},{"location":"python/tooling/mypy/#why-it-is-useful","title":"Why it is useful","text":"<ul> <li>Mypy helps catch mistakes such as passing a <code>str</code> where a <code>float</code> is expected.</li> <li>It also improves readability because function signatures document expected inputs and outputs.</li> <li>This is especially useful in larger projects where functions, classes, and modules interact.</li> </ul>"},{"location":"python/tooling/mypy/#what-mypy-checks","title":"What mypy checks","text":"<ul> <li>Mypy checks type consistency across variables, functions, classes, and modules.</li> <li>Type hints such as <code>x: int</code>, <code>list[str]</code>, or <code>float | None</code> give mypy information to reason about.</li> <li>The special type <code>Any</code> disables most checking for a value and should be used carefully.</li> </ul>"},{"location":"python/tooling/mypy/#what-mypy-does-not-check","title":"What mypy does not check","text":"<ul> <li>Mypy does not prove that a program is logically correct.</li> <li>It checks type consistency, not numerical accuracy, performance, or business logic.</li> </ul>"},{"location":"python/tooling/mypy/#using-mypy-with-poetry","title":"Using mypy with Poetry","text":"<ul> <li>In a Poetry project, mypy is usually added as a development dependency:</li> </ul> <pre><code>poetry add --group dev mypy\n</code></pre> <p>Run it against your source folder with:</p> <pre><code>poetry run mypy src\n</code></pre> <p>For stricter checking, use:</p> <pre><code>poetry run mypy src --strict\n</code></pre>"},{"location":"python/tooling/mypy/#configuration","title":"Configuration","text":"<p>Common configuration can be placed in <code>pyproject.toml</code> under <code>[tool.mypy]</code>:</p> <pre><code>[tool.mypy]\npython_version = \"3.12\"\nstrict = true\n</code></pre>"},{"location":"python/tooling/mypy/#good-project-structure","title":"Good project structure","text":"<ul> <li>A typical typed project keeps messy external data at the boundaries.</li> <li>Validation or parsing code converts raw data into clean typed objects.</li> <li>The core domain logic can then use precise types and benefit most from mypy.</li> </ul>"},{"location":"python/tooling/mypy/#mental-model","title":"Mental model","text":"<p>mypy checks type consistency, not correctness \u2014 it's a design aid, not a proof</p> <p>mypy can verify that a <code>float</code> is passed where a <code>float</code> is expected, but it cannot verify that the float is a valid price, that a calculation is numerically correct, or that business logic is sound. Think of it as enforcing the contracts between functions. Pair it with tests (which check behaviour) \u2014 they're complementary, not redundant.</p> <ul> <li>Mypy is best viewed as a design aid.</li> <li>It makes code contracts explicit and helps reveal unclear assumptions in your program.</li> </ul>"},{"location":"python/tooling/poetry/","title":"Poetry","text":""},{"location":"python/tooling/poetry/#what-is-poetry","title":"What is Poetry?","text":"<p>Poetry is a modern Python tool for:</p> <ul> <li>dependency management</li> <li>virtual environment management</li> <li>packaging Python projects</li> </ul> <p>It replaces the traditional combination of:</p> <ul> <li><code>pip</code></li> <li><code>requirements.txt</code></li> <li><code>virtualenv</code></li> <li><code>setup.py</code></li> </ul> <p>with a single workflow centered around:</p> <ul> <li><code>pyproject.toml</code> (requirements)</li> <li><code>poetry.lock</code> (what's installed)</li> </ul> <p>It provides a unified workflow for managing Python projects.</p>"},{"location":"python/tooling/poetry/#why-use-poetry","title":"Why use Poetry?","text":"<p>Main advantages:</p> <ul> <li>reproducible environments</li> <li>deterministic dependency versions</li> <li>automatic virtual environments</li> <li>cleaner project structure</li> <li>easier collaboration</li> </ul> <p>Poetry helps avoid:</p> <p>\"works on my machine\" problems.</p>"},{"location":"python/tooling/poetry/#installation-macos","title":"Installation (macOS)","text":"<p>Install Poetry:</p> <pre><code>curl -sSL https://install.python-poetry.org | python3 -\n</code></pre>"},{"location":"python/tooling/poetry/#project-scaffold","title":"Project scaffold","text":"<pre><code>poetry new finlib\ncd finlib\npoetry add pydantic\npoetry add --group dev mypy ruff black pytest\n</code></pre> <pre><code>finlib/\n|-- pyproject.toml\n|-- src/finlib/__init__.py\n|-- tests/\n</code></pre>"},{"location":"python/tooling/poetry/#poetry-add","title":"<code>poetry add</code>","text":"<p><code>poetry add &lt;package&gt;</code> declares a new dependency. It does four things atomically:</p> <ol> <li>Resolves the package against existing constraints</li> <li>Updates <code>pyproject.toml</code> with a version constraint</li> <li>Updates <code>poetry.lock</code> with exact pinned versions for all packages</li> <li>Installs into the virtual environment</li> </ol> <p>Two files, two jobs: pyproject.toml is the intention; poetry.lock is the truth</p> <p><code>pyproject.toml</code> records human-readable constraints (<code>^2.31.0</code>). <code>poetry.lock</code> records the exact resolved version of every dependency and transitive dependency. Commit both: <code>pyproject.toml</code> so humans can read the constraints; <code>poetry.lock</code> so <code>poetry install</code> on another machine produces an identical environment. Never hand-edit <code>poetry.lock</code>.</p> <p>The two files:</p> <pre><code>pyproject.toml   \u2190 human-edited constraints (\"what I want\")\npoetry.lock      \u2190 machine-generated exact pins (\"what's installed\")\n</code></pre> <p>Commit both. Never hand-edit <code>poetry.lock</code>.</p>"},{"location":"python/tooling/poetry/#version-constraints","title":"Version constraints","text":"<p>By default, Poetry writes a caret constraint (<code>^</code>):</p> <pre><code>poetry add requests    # writes requests = \"^2.31.0\" in pyproject.toml\n</code></pre> <p>Caret semantics follow semver:</p> Constraint Resolves to Reasoning <code>^1.2.3</code> <code>&gt;=1.2.3, &lt;2.0.0</code> Major locked <code>^0.2.3</code> <code>&gt;=0.2.3, &lt;0.3.0</code> Major=0 \u2192 minor locked <code>^0.0.3</code> <code>&gt;=0.0.3, &lt;0.0.4</code> Major=minor=0 \u2192 patch locked <p>Other operators: <code>~</code> (lock major+minor), <code>&gt;=</code>, <code>==</code> (exact pin), <code>!=</code> (exclude).</p>"},{"location":"python/tooling/poetry/#dependency-groups","title":"Dependency groups","text":"<p>Groups separate runtime from dev/test dependencies:</p> <pre><code>poetry add --group dev pytest ruff mypy      # excluded from prod installs\npoetry add --group test factory-boy faker\n</code></pre> <p><code>poetry install --without dev</code> skips dev groups. The old <code>--dev</code> flag is deprecated.</p>"},{"location":"python/tooling/poetry/#extras","title":"Extras","text":"<pre><code>poetry add \"uvicorn[standard]\"    # optional sub-deps bundled by the package\n</code></pre>"},{"location":"python/tooling/poetry/#useful-flags","title":"Useful flags","text":"<pre><code>poetry add --dry-run requests     # preview changes without acting\npoetry add --lock requests        # update files only, skip install\npoetry add git+https://github.com/org/repo.git#tag   # git source\npoetry add ../local-pkg --editable                   # local editable path\n</code></pre>"},{"location":"python/tooling/poetry/#add-vs-install-vs-update","title":"add vs install vs update","text":"Command pyproject.toml? poetry.lock? Installs? <code>poetry add</code> Yes (new dep) Yes (re-resolves) Yes <code>poetry install</code> No No (reads it) Yes <code>poetry update</code> No Yes (re-resolves) Yes <code>poetry remove</code> Yes (removes) Yes Yes (removes) <p><code>poetry install</code> is idempotent \u2014 makes the environment match the lock file exactly.</p>"},{"location":"python/tooling/poetry/#running-code","title":"Running code","text":"<p>If you <code>python ...</code> you use the default environment on your machine. Instead,  you want to run it using the poetry environment.</p> <pre><code>poetry run python -m finlib.module\n</code></pre>"},{"location":"python/tooling/poetry/#sync-environment-to-lock-file","title":"Sync environment to lock file","text":"<pre><code>poetry install\n</code></pre>"},{"location":"python/tooling/pydantic-settings/","title":"pydantic-settings","text":"<p><code>pydantic-settings</code> extends Pydantic with <code>BaseSettings</code>: a model that reads field values from environment variables and <code>.env</code> files automatically. Use it for all configuration \u2014 API keys, file paths, feature flags, timeouts.</p> <pre><code>poetry add pydantic-settings python-dotenv  # python-dotenv enables .env file reading\n</code></pre>"},{"location":"python/tooling/pydantic-settings/#basesettings-minimal-example","title":"BaseSettings minimal example","text":"<pre><code>from pydantic_settings import BaseSettings, SettingsConfigDict\n\nclass Settings(BaseSettings):\n    model_config = SettingsConfigDict(env_file=\".env\", env_file_encoding=\"utf-8\")\n\n    api_key: str           # required \u2014 ValidationError at startup if missing\n    timeout: int = 30      # optional with default\n    debug: bool = False\n</code></pre> <p>Instantiate once; <code>Settings()</code> reads env and <code>.env</code>, coerces types, and validates immediately.</p>"},{"location":"python/tooling/pydantic-settings/#realistic-project-settings","title":"Realistic project Settings","text":"<pre><code>from pathlib import Path\nfrom pydantic import SecretStr\nfrom pydantic_settings import BaseSettings, SettingsConfigDict\n\nclass Settings(BaseSettings):\n    model_config = SettingsConfigDict(\n        env_file=\".env\",\n        env_file_encoding=\"utf-8\",\n        case_sensitive=False,\n    )\n\n    api_key: SecretStr          # hides value in repr/logs\n    data_dir: Path = Path(\"./data\")\n    output_dir: Path = Path(\"./output\")\n    request_timeout: int = 30\n    max_retries: int = 3\n    log_level: str = \"INFO\"\n    debug: bool = False\n</code></pre> <p><code>.env</code> file: <pre><code>API_KEY=sk-abc123\nDATA_DIR=/home/user/project/data\nLOG_LEVEL=DEBUG\n</code></pre></p> <pre><code>settings = Settings()\nsettings.api_key.get_secret_value()  # retrieve SecretStr value\nsettings.data_dir                    # PosixPath('./data')\nsettings.request_timeout             # 30  (int, not \"30\")\n</code></pre>"},{"location":"python/tooling/pydantic-settings/#singleton-pattern","title":"Singleton pattern","text":"<pre><code># config.py\nsettings = Settings()   # module-level \u2014 instantiate once\n\n# everywhere else\nfrom myproject.config import settings\n</code></pre>"},{"location":"python/tooling/pydantic-settings/#nested-settings","title":"Nested settings","text":"<p><code>.env</code> files are flat (<code>KEY=value</code> only \u2014 no native nesting). Pydantic-settings reconstructs structure in Python via three strategies.</p>"},{"location":"python/tooling/pydantic-settings/#nested-basemodel-env_nested_delimiter-recommended","title":"Nested BaseModel + <code>env_nested_delimiter</code> (recommended)","text":"<pre><code>from pydantic import BaseModel, SecretStr\nfrom pydantic_settings import BaseSettings, SettingsConfigDict\n\nclass DatabaseSettings(BaseModel):   # plain BaseModel, not BaseSettings\n    host: str = \"localhost\"\n    port: int = 5432\n    name: str\n    password: SecretStr\n\nclass Settings(BaseSettings):\n    model_config = SettingsConfigDict(env_file=\".env\", env_nested_delimiter=\"__\")\n\n    database: DatabaseSettings\n    debug: bool = False\n</code></pre> <p><code>.env</code>: <pre><code>DATABASE__HOST=db.prod.internal\nDATABASE__NAME=myapp\nDATABASE__PASSWORD=s3cr3t\n</code></pre></p> <pre><code>settings.database.host   # \"db.prod.internal\"\nsettings.database.port   # 5432 (int, not \"5432\")\n</code></pre> <p><code>__</code> in the env var name means \"go one level deeper\". <code>env_nested_delimiter</code> is not set by default \u2014 you must add it.</p>"},{"location":"python/tooling/pydantic-settings/#json-encoded-value-one-var-per-section","title":"JSON-encoded value (one var per section)","text":"<pre><code>DATABASE={\"host\": \"localhost\", \"port\": 5432, \"name\": \"myapp\", \"password\": \"s3cr3t\"}\n</code></pre> <p>Pydantic detects the model field type and JSON-parses the string automatically. Useful for K8s secrets or CI env blobs; fragile for human-edited <code>.env</code> files.</p>"},{"location":"python/tooling/pydantic-settings/#dict-field-dynamic-keys","title":"<code>dict</code> field (dynamic keys)","text":"<p><pre><code>class Settings(BaseSettings):\n    model_config = SettingsConfigDict(env_nested_delimiter=\"__\")\n    feature_flags: dict[str, bool] = {}\n</code></pre> <pre><code>FEATURE_FLAGS__dark_mode=true\nFEATURE_FLAGS__new_onboarding=false\n</code></pre></p> <p>Good for feature flags or any open-ended key set.</p>"},{"location":"python/tooling/pydantic-settings/#nested-settings-gotchas","title":"Nested settings gotchas","text":"<ul> <li>Sub-models must be <code>BaseModel</code>, not <code>BaseSettings</code> \u2014 nesting two <code>BaseSettings</code> doesn't compose as expected.</li> <li>With <code>env_prefix=\"MYAPP_\"</code> the full key becomes <code>MYAPP_DATABASE__HOST</code> (prefix first, then delimiter).</li> </ul>"},{"location":"python/tooling/pydantic-settings/#env-prefix","title":"Env prefix","text":"<p>Namespace all keys to avoid collision with system env vars:</p> <pre><code>class Settings(BaseSettings):\n    model_config = SettingsConfigDict(env_prefix=\"MYAPP_\")\n\n    api_key: str    # reads MYAPP_API_KEY\n    debug: bool     # reads MYAPP_DEBUG\n</code></pre>"},{"location":"python/tooling/pydantic-settings/#priority-order-highest-lowest","title":"Priority order (highest \u2192 lowest)","text":"<ol> <li>Constructor arguments: <code>Settings(api_key=\"override\")</code></li> <li>Actual environment variables</li> <li><code>.env</code> file values</li> <li>Field defaults</li> </ol>"},{"location":"python/tooling/pydantic-settings/#type-coercion","title":"Type coercion","text":"Env string Field type Result <code>\"30\"</code> <code>int</code> <code>30</code> <code>\"true\"</code> / <code>\"1\"</code> <code>bool</code> <code>True</code> <code>\"/tmp/d\"</code> <code>Path</code> <code>PosixPath('/tmp/d')</code> <code>'[\"a\",\"b\"]'</code> or <code>\"a,b\"</code> <code>list[str]</code> <code>['a', 'b']</code>"},{"location":"python/tooling/pydantic-settings/#testing","title":"Testing","text":"<p>Constructor args override env/file \u2014 inject test values directly:</p> <pre><code>def test_something():\n    s = Settings(api_key=\"test-key\", debug=True)\n</code></pre>"},{"location":"python/tooling/pydantic-settings/#refactoring-away-from-global-config-reads","title":"Refactoring away from global config reads","text":"<p>The singleton <code>settings</code> global in <code>config.py</code> is fine \u2014 it's pure data. The anti-pattern is every other module calling <code>os.environ.get()</code> directly, or instantiating stateful dependencies (DB engines, HTTP clients) at module level.</p> <p>Before (bad): <pre><code># db.py \u2014 two problems: direct env read + instantiation at import time\nimport os, sqlalchemy\nengine = sqlalchemy.create_engine(os.environ.get(\"DATABASE_URL\"))\n</code></pre></p> <p>After (good): <pre><code># db.py\nfrom functools import lru_cache\nimport sqlalchemy\nfrom .config import settings   # single source of truth\n\n@lru_cache(maxsize=1)\ndef get_engine() -&gt; sqlalchemy.Engine:\n    return sqlalchemy.create_engine(settings.database_url)\n</code></pre></p> <p><code>@lru_cache</code> makes <code>get_engine()</code> behave like a singleton (creates once, returns cached) while keeping instantiation out of import time. In tests, call <code>get_engine.cache_clear()</code> to force a rebuild with different settings.</p> <p>FastAPI pattern \u2014 override the settings dependency instead of patching: <pre><code># dependencies.py\nfrom functools import lru_cache\nfrom .config import Settings\n\n@lru_cache\ndef get_settings() -&gt; Settings:\n    return Settings()\n\n# in tests\napp.dependency_overrides[get_settings] = lambda: Settings(api_key=\"test\")\n</code></pre></p> <p>Rule of thumb:</p> What Pattern Config values One <code>settings = Settings()</code> in <code>config.py</code> only DB engines, HTTP clients, API wrappers <code>@lru_cache</code> factory \u2014 never module-level instantiation FastAPI <code>Depends(get_settings)</code> + <code>dependency_overrides</code> in tests"},{"location":"python/tooling/pydantic-settings/#gotchas","title":"Gotchas","text":"<p>python-dotenv not installed \u2192 env_file is silently ignored</p> <p>If <code>python-dotenv</code> is not in the environment, <code>model_config = SettingsConfigDict(env_file=\".env\")</code> does nothing \u2014 no error, no warning. Fields that should come from <code>.env</code> get their defaults or raise <code>ValidationError</code> for required fields. Add <code>python-dotenv</code> alongside <code>pydantic-settings</code> and pin it in your dependencies.</p> <ul> <li><code>python-dotenv</code> not installed \u2192 <code>env_file</code> silently ignored</li> <li>Relative <code>Path</code> defaults resolve from the process working directory, not the project root</li> <li><code>SecretStr</code> requires <code>.get_secret_value()</code> to access the real string</li> </ul>"},{"location":"python/tooling/pydantic/","title":"Pydantic","text":"<p>Pydantic is a Python library used for data validation and parsing using type hints.</p>"},{"location":"python/tooling/pydantic/#main-idea","title":"Main Idea","text":"<p>You define the structure of your data as Python classes, and Pydantic: - validates incoming data - converts compatible types automatically - raises clear errors for invalid data</p>"},{"location":"python/tooling/pydantic/#basic-example","title":"Basic Example","text":"<pre><code>from pydantic import BaseModel, Field, field_validator\nfrom decimal import Decimal\nfrom typing import Literal\n\nclass Trade(BaseModel):\n    model_config = {'frozen': True, 'str_strip_whitespace': True}\n\n    symbol: str = Field(..., min_length=1, max_length=10)\n    price: Decimal = Field(..., gt=0)\n    qty: int\n    side: Literal['BUY', 'SELL']\n\n    @field_validator('symbol')  # for custom validation logic\n    @classmethod\n    def symbol_uppercase(cls, v: str) -&gt; str:\n        return v.upper()\n\n    @property\n    def notional(self) -&gt; Decimal:\n        return self.quantity * self.price\n\ntrade = Trade(symbol=\"ES\", price=\"5230.5\", qty=\"10\", side=\"BUY\")\n</code></pre> <p><code>Literal</code> restricts <code>side</code> to the two valid strings; Pydantic raises <code>ValidationError</code> at parse time for any other value.</p>"},{"location":"python/tooling/pydantic/#typical-uses","title":"Typical Uses","text":"<ul> <li>API request/response validation</li> <li>Parsing JSON data</li> <li>Data pipelines and ETL</li> <li>LLM structured outputs</li> <li>Configuration management and env vars \u2192 see pydantic-settings.md</li> </ul>"},{"location":"python/tooling/pydantic/#common-features","title":"Common Features","text":"<ul> <li>Runtime type checking</li> <li>Automatic type coercion</li> <li>Clear validation errors</li> <li>Nested models</li> <li>Optional/default fields</li> <li>JSON serialization</li> </ul>"},{"location":"python/tooling/pydantic/#notes","title":"Notes","text":"<ul> <li>BaseModel is the core Pydantic class</li> <li>Widely used with FastAPI</li> <li>Best used at application boundaries, not inside performance-critical loops</li> <li>Current major version is Pydantic v2</li> </ul>"},{"location":"python/tooling/pydantic/#when-to-use-pydantic-vs-dataclass-see-data-modelmd","title":"When to use Pydantic vs dataclass (see data-model.md)?","text":"<p>Pydantic at boundaries, dataclass for internal models</p> <p>Pydantic validates and coerces untrusted data (API responses, user input, config files) at the edges of your system. Once data is inside, plain dataclasses are lighter and don't impose runtime validation overhead. Using Pydantic everywhere adds cost without benefit for objects that are constructed from already-validated data inside the system.</p> <p>Rule: Pydantic at edges (input/output), dataclass for internal models</p>"},{"location":"python/tooling/pydantic/#pydantic_1","title":"Pydantic","text":"<p>For: - validation - serialisation = converting an object in memory into a format that can be: saved to disk; sent over a network; stored in a database; transmitted to another program. Usually this means converting Python objects into: JSON, bytes, text, dictionaries. - for API boundaries</p>"},{"location":"python/tooling/pydantic/#dataclass","title":"dataclass","text":"<ul> <li>stdlib</li> <li>lightweight</li> <li>no runtime validation</li> <li>for internal data</li> </ul>"},{"location":"python/tooling/pyenv/","title":"pyenv","text":"<p>pyenv lets you install and switch between multiple Python versions.</p> <p>Useful because different projects may require different Python versions.</p> <p>Common commands:</p> <pre><code>pyenv install 3.12\npyenv global 3.12\npyenv local 3.11\npyenv versions\n</code></pre> <p>Note: - global: for entire machine - local: for the local folder (i.e. project)</p>"},{"location":"python/tooling/ruff/","title":"Ruff","text":"<p>Ruff is a fast Python linter and formatter written in Rust. It replaces tools like <code>flake8</code>, <code>isort</code>, and <code>black</code> in a single tool.</p>"},{"location":"python/tooling/ruff/#installation","title":"Installation","text":"<pre><code>poetry add --group dev ruff\n</code></pre>"},{"location":"python/tooling/ruff/#usage","title":"Usage","text":"<pre><code>poetry run ruff check src    # lint\npoetry run ruff format src   # format\n</code></pre>"},{"location":"python/tooling/structlog/","title":"structlog","text":"<p>Third-party logging library that makes structured (key=value) logging the natural default. Every log call produces a dictionary; renderers output it as JSON, coloured console text, or anything else. Sits on top of (or beside) stdlib <code>logging</code>.</p>"},{"location":"python/tooling/structlog/#why-over-stdlib","title":"Why over stdlib","text":"<ul> <li>Context binding via <code>log.bind(key=val)</code> instead of <code>LoggerAdapter</code> / <code>extra={}</code></li> <li>Fully composable processor pipeline instead of fixed Filter \u2192 Formatter chain</li> <li>Built-in <code>JSONRenderer</code> and <code>ConsoleRenderer</code></li> <li><code>capture_logs()</code> for zero-config test assertions</li> </ul>"},{"location":"python/tooling/structlog/#basic-usage","title":"Basic usage","text":"<pre><code>import structlog\n\nlog = structlog.get_logger(__name__)\n\nlog.info(\"order_placed\", order_id=123, user=\"alice\")\n# \u2192 {\"event\": \"order_placed\", \"order_id\": 123, \"user\": \"alice\", ...}\n</code></pre>"},{"location":"python/tooling/structlog/#bound-logger","title":"Bound logger","text":"<p><code>log.bind(**kw)</code> returns a new logger with those keys permanently attached:</p> <pre><code>bound = log.bind(service=\"pricing\", symbol=\"BTC\")\nbound.info(\"price_fetched\", price=42.0)\n# every future call carries service= and symbol=\n</code></pre> <p><code>log.unbind(\"symbol\")</code> removes a key. <code>log.new(**kw)</code> resets all context.</p>"},{"location":"python/tooling/structlog/#processor-pipeline","title":"Processor pipeline","text":"<p>Each processor is a callable <code>(logger, method, event_dict) -&gt; event_dict</code>. They chain in order; the last one renders to a string.</p> Processor Adds <code>add_log_level</code> <code>\"level\": \"info\"</code> <code>TimeStamper(fmt=\"iso\")</code> <code>\"timestamp\": \"2026-...\"</code> <code>dict_tracebacks</code> exception \u2192 nested dict (JSON-safe) <code>ExceptionRenderer()</code> pretty exception in console <code>merge_contextvars</code> pull in async context (see below) <code>ConsoleRenderer()</code> coloured dev output <code>JSONRenderer()</code> JSON string for production"},{"location":"python/tooling/structlog/#configuration","title":"Configuration","text":"<p>Call once at startup:</p> <pre><code>structlog.configure(\n    processors=[\n        structlog.contextvars.merge_contextvars,\n        structlog.processors.add_log_level,\n        structlog.processors.TimeStamper(fmt=\"iso\"),\n        structlog.dev.ConsoleRenderer(),          # swap for JSONRenderer() in prod\n    ],\n    wrapper_class=structlog.make_filtering_bound_logger(logging.INFO),\n    logger_factory=structlog.PrintLoggerFactory(),\n)\n</code></pre>"},{"location":"python/tooling/structlog/#async-contextvars-integration","title":"Async: <code>contextvars</code> integration","text":"<p>Attach context to the current async task without threading a bound logger everywhere:</p> <pre><code>from structlog.contextvars import bind_contextvars, clear_contextvars\n\nasync def handle_request(request):\n    clear_contextvars()\n    bind_contextvars(request_id=request.headers[\"X-Request-ID\"])\n    # all log calls in this task (and subtasks) now carry request_id\n    log.info(\"request_received\")\n</code></pre> <p>Requires <code>merge_contextvars</code> in the processor chain.</p>"},{"location":"python/tooling/structlog/#stdlib-integration","title":"stdlib integration","text":"<p>Route structlog through stdlib so structlog and <code>logging.getLogger(...)</code> share one pipeline:</p> <pre><code>structlog.configure(\n    logger_factory=structlog.stdlib.LoggerFactory(),\n    wrapper_class=structlog.stdlib.BoundLogger,\n    processors=[\n        ...\n        structlog.stdlib.ProcessorFormatter.wrap_for_formatter,\n    ],\n)\n\nformatter = structlog.stdlib.ProcessorFormatter(processor=structlog.dev.ConsoleRenderer())\nhandler = logging.StreamHandler()\nhandler.setFormatter(formatter)\nlogging.getLogger().addHandler(handler)\n</code></pre>"},{"location":"python/tooling/structlog/#testing","title":"Testing","text":"<p>capture_logs() makes log assertions trivially easy \u2014 no patching needed</p> <p><code>capture_logs()</code> is a context manager that temporarily replaces the processor chain and collects events as plain dicts. You can assert on event names, log levels, and structured fields directly. Compare this to stdlib <code>logging</code> tests, which require <code>logging.handlers.MemoryHandler</code> or <code>caplog</code> fixtures and string parsing.</p> <p><code>capture_logs()</code> replaces the processor chain temporarily and returns all events as a list of dicts:</p> <pre><code>from structlog.testing import capture_logs\n\ndef test_logs_error_on_failure():\n    with capture_logs() as cap:\n        process_order(order_id=1)\n\n    assert cap[0][\"event\"] == \"processing_failed\"\n    assert cap[0][\"log_level\"] == \"error\"\n    assert cap[0][\"order_id\"] == 1\n</code></pre>"},{"location":"python/tooling/structlog/#install","title":"Install","text":"<pre><code>pip install structlog\n</code></pre>"},{"location":"python/tooling/terminal-tables/","title":"Printing Tables in the Terminal","text":"<p>Three options in increasing power:</p> Need Tool No dependencies, fixed schema f-strings + format spec Quick readable output <code>tabulate</code> Colours, borders, styles <code>rich</code> Already using pandas <code>df.to_string()</code> or <code>tabulate(df, headers=\"keys\")</code>"},{"location":"python/tooling/terminal-tables/#1-f-strings-no-deps","title":"1. f-strings (no deps)","text":"<p>Use the format spec mini-language for alignment and width:</p> <pre><code>rows = [(\"Apple\", 1.25, 100), (\"Dragonfruit\", 4.99, 12)]\n\nprint(f\"{'Name':&lt;15} {'Price':&gt;8} {'Qty':&gt;5}\")\nprint(\"-\" * 30)\nfor name, price, qty in rows:\n    print(f\"{name:&lt;15} {price:&gt;8.2f} {qty:&gt;5d}\")\n</code></pre> <p>For dynamic widths, compute them first: <pre><code>all_rows = [headers] + rows\nwidths = [max(len(str(r[i])) for r in all_rows) for i in range(len(headers))]\nfmt = \"  \".join(f\"{{:&lt;{w}}}\" for w in widths)\n</code></pre></p>"},{"location":"python/tooling/terminal-tables/#2-tabulate","title":"2. tabulate","text":"<pre><code>pip install tabulate\n</code></pre> <pre><code>from tabulate import tabulate\n\nrows = [[\"Apple\", 1.25, 100], [\"Dragonfruit\", 4.99, 12]]\nheaders = [\"Name\", \"Price\", \"Qty\"]\n\nprint(tabulate(rows, headers=headers, tablefmt=\"simple\", floatfmt=\".2f\"))\n</code></pre>"},{"location":"python/tooling/terminal-tables/#input-formats","title":"Input formats","text":"<pre><code>tabulate(rows, headers=headers)                  # list of lists\ntabulate(rows, headers=\"keys\")                   # list of dicts or DataFrame\ntabulate({\"Name\": [...], \"Price\": [...]}, headers=\"keys\")  # dict of columns\n</code></pre>"},{"location":"python/tooling/terminal-tables/#tablefmt-options","title":"<code>tablefmt</code> options","text":"Value Output <code>\"simple\"</code> (default) Dashes under header <code>\"grid\"</code> Full ASCII grid <code>\"pipe\"</code> Markdown <code>\\|</code> table <code>\"html\"</code> <code>&lt;table&gt;</code> <code>\"latex\"</code> LaTeX tabular"},{"location":"python/tooling/terminal-tables/#number-formatting","title":"Number formatting","text":"<pre><code>tabulate(rows, headers=headers, floatfmt=\".2f\", intfmt=\",\")\n</code></pre>"},{"location":"python/tooling/terminal-tables/#3-rich","title":"3. rich","text":"<pre><code>pip install rich\n</code></pre> <pre><code>from rich.console import Console\nfrom rich.table import Table\n\nconsole = Console()\ntable = Table(title=\"Fruit Prices\")\n\ntable.add_column(\"Name\", style=\"cyan\", no_wrap=True)\ntable.add_column(\"Price\", justify=\"right\", style=\"green\")\ntable.add_column(\"Qty\",   justify=\"right\")\n\ntable.add_row(\"Apple\",       \"\u00a31.25\", \"100\")\ntable.add_row(\"Dragonfruit\", \"\u00a34.99\",  \"12\")\n\nconsole.print(table)\n</code></pre>"},{"location":"python/tooling/terminal-tables/#key-options","title":"Key options","text":"<pre><code>table.add_column(\"Col\",\n    style=\"bold\",        # cell style\n    justify=\"right\",     # \"left\" | \"right\" | \"center\"\n    no_wrap=True,\n    min_width=10,\n)\n\nTable(\n    show_lines=True,          # horizontal lines between rows\n    box=box.ROUNDED,          # border style\n)\n</code></pre>"},{"location":"python/tooling/terminal-tables/#box-styles","title":"Box styles","text":"<pre><code>from rich import box\nTable(box=box.SIMPLE)     # minimal\nTable(box=box.ROUNDED)    # rounded corners\nTable(box=box.MARKDOWN)   # Markdown-compatible\nTable(box=box.ASCII)      # safe for non-unicode terminals\n</code></pre>"},{"location":"python/tooling/terminal-tables/#conditional-cell-styling","title":"Conditional cell styling","text":"<pre><code>from rich.text import Text\n\nfor row in data:\n    color = \"red\" if row[\"price\"] &gt; 3 else \"green\"\n    table.add_row(row[\"name\"], Text(f\"{row['price']:.2f}\", style=color))\n</code></pre>"},{"location":"python/tooling/testing/","title":"Python \u2014 Tooling / Testing","text":"File Type Description pytest.md ref Test discovery and command quick-reference testing-patterns.md note Practical testing patterns: external deps, file I/O, DB, stdout, network mocking testing-strategy.md note Testing strategy: pyramid, what to test, mocks, TDD, coverage"},{"location":"python/tooling/testing/pytest/","title":"pytest","text":"<p>pytest is the standard Python testing framework. It automatically discovers files named <code>test_*.py</code> and functions named <code>test_*</code>.</p>"},{"location":"python/tooling/testing/pytest/#installation","title":"Installation","text":"<pre><code>poetry add --group dev pytest\n</code></pre>"},{"location":"python/tooling/testing/pytest/#usage","title":"Usage","text":""},{"location":"python/tooling/testing/pytest/#generate-a-test-file","title":"Generate a test file","text":"<pre><code>def test_rejects_negative_quantity():\n    with pytest.raises(Exception):\n        Trade(symbol='BHP', quantity=-10, price=45.5, side='BUY')\n</code></pre> <p>The <code>Trade</code> model used here follows the pattern in <code>pydantic.md</code>.</p>"},{"location":"python/tooling/testing/pytest/#run","title":"Run","text":"<p>Pytest automatically: - discovers files named test_.py - runs functions named test_ - reports failures clearly</p> <pre><code>poetry run pytest\n</code></pre>"},{"location":"python/tooling/testing/testing-patterns/","title":"Testing \u2014 Patterns","text":"<p>Practical patterns for testing functions with external dependencies or side effects.</p>"},{"location":"python/tooling/testing/testing-patterns/#testing-functions-with-external-dependencies","title":"Testing functions with external dependencies","text":"<p>The problem: a function that opens a CSV or queries a database makes tests slow, fragile, and non-deterministic. The solution is always the same \u2014 create a seam where the test can swap in a substitute.</p>"},{"location":"python/tooling/testing/testing-patterns/#root-cause-hidden-coupling","title":"Root cause: hidden coupling","text":"<pre><code># BAD \u2014 dependency is invisible from the outside\ndef load_prices(symbol: str) -&gt; list[float]:\n    with open(\"/data/prices.csv\") as f:   # impossible to swap in tests\n        ...\n\n# GOOD \u2014 caller decides what file to use (dependency injection)\ndef load_prices(symbol: str, filepath: str | Path) -&gt; list[float]:\n    with open(filepath) as f:\n        ...\n</code></pre> <p>Receive dependencies as arguments rather than creating them internally.</p>"},{"location":"python/tooling/testing/testing-patterns/#strategy-1-real-file-via-tmp_path-best-for-file-io","title":"Strategy 1 \u2014 real file via <code>tmp_path</code> (best for file I/O)","text":"<p>pytest's built-in <code>tmp_path</code> fixture provides a fresh temp directory per test. Deleted automatically after the test.</p> <pre><code>def test_load_prices(tmp_path):\n    csv_file = tmp_path / \"prices.csv\"\n    csv_file.write_text(\"symbol,price\\nBHP,45.5\\nBHP,46.0\\n\")\n\n    result = load_prices(\"BHP\", filepath=csv_file)\n\n    assert result == [45.5, 46.0]\n</code></pre>"},{"location":"python/tooling/testing/testing-patterns/#strategy-2-in-memory-file-object-iostringio","title":"Strategy 2 \u2014 in-memory file object (<code>io.StringIO</code>)","text":"<p>Accept <code>TextIO</code> (any file-like) instead of a path string. Fastest option; no filesystem touched.</p> <pre><code>import io, csv\n\ndef load_prices(symbol: str, file) -&gt; list[float]:\n    return [float(r[\"price\"]) for r in csv.DictReader(file) if r[\"symbol\"] == symbol]\n\ndef test_load_prices():\n    assert load_prices(\"BHP\", io.StringIO(\"symbol,price\\nBHP,45.5\\n\")) == [45.5]\n</code></pre>"},{"location":"python/tooling/testing/testing-patterns/#strategy-3-in-memory-database-sqlite-memory","title":"Strategy 3 \u2014 in-memory database (SQLite <code>:memory:</code>)","text":"<p>A real SQL engine, zero infrastructure. Best for unit-testing SQL logic.</p> <pre><code>import sqlite3\n\ndef get_open_trades(conn) -&gt; list[dict]:\n    conn.row_factory = sqlite3.Row\n    return [dict(r) for r in conn.execute(\"SELECT * FROM trades WHERE status='OPEN'\")]\n\ndef test_returns_only_open_trades():\n    conn = sqlite3.connect(\":memory:\")\n    conn.execute(\"CREATE TABLE trades (id INT, status TEXT)\")\n    conn.execute(\"INSERT INTO trades VALUES (1, 'OPEN'), (2, 'CLOSED')\")\n\n    assert len(get_open_trades(conn)) == 1\n</code></pre> <p>Limitation: SQLite dialect differs from Postgres/MySQL. Use Testcontainers for full dialect fidelity.</p>"},{"location":"python/tooling/testing/testing-patterns/#strategy-4-transaction-rollback-integration-tests-against-a-real-db","title":"Strategy 4 \u2014 transaction rollback (integration tests against a real DB)","text":"<p>Wrap each test in a transaction rolled back on teardown. State never leaks between tests.</p> <pre><code>@pytest.fixture\ndef db_conn():\n    conn = psycopg2.connect(dsn=\"postgresql://localhost/testdb\")\n    conn.autocommit = False\n    yield conn\n    conn.rollback()\n    conn.close()\n</code></pre>"},{"location":"python/tooling/testing/testing-patterns/#strategy-5-patch-last-resort","title":"Strategy 5 \u2014 <code>patch()</code> (last resort)","text":"<p>When you can't inject the dependency. Ties the test to implementation details \u2014 fragile under refactors.</p> <pre><code>from unittest.mock import mock_open, patch\n\ndef test_load_prices_mocked():\n    with patch(\"builtins.open\", mock_open(read_data=\"symbol,price\\nBHP,45.5\\n\")):\n        assert load_prices(\"BHP\", \"/any/path\") == [45.5]\n</code></pre>"},{"location":"python/tooling/testing/testing-patterns/#decision-guide","title":"Decision guide","text":"Situation Strategy File I/O, testing parsing logic <code>tmp_path</code> (real file) File I/O, parsing logic is trivial <code>io.StringIO</code> SQL, SQLite dialect is fine <code>sqlite3.connect(\":memory:\")</code> SQL, need exact dialect Testcontainers or rollback fixture Can't inject the dep <code>patch()</code>"},{"location":"python/tooling/testing/testing-patterns/#testing-functions-that-print-to-stdout","title":"Testing functions that print to stdout","text":"<p>Printing is a side effect \u2014 there's no return value to assert against. pytest's <code>capsys</code> fixture captures <code>sys.stdout</code> / <code>sys.stderr</code> during the test.</p> <pre><code>def greet(name: str) -&gt; None:\n    print(f\"Hello, {name}!\")\n\ndef test_greet(capsys):\n    greet(\"Alice\")\n    captured = capsys.readouterr()   # named tuple: .out, .err\n    assert captured.out == \"Hello, Alice!\\n\"\n</code></pre> <p><code>readouterr()</code> consumes the buffer \u2014 each call returns only what was printed since the last call.</p>"},{"location":"python/tooling/testing/testing-patterns/#checking-stderr-separately","title":"Checking stderr separately","text":"<pre><code>import sys\n\ndef log_warning(msg: str) -&gt; None:\n    print(f\"WARNING: {msg}\", file=sys.stderr)\n\ndef test_warning_goes_to_stderr(capsys):\n    log_warning(\"low balance\")\n    captured = capsys.readouterr()\n    assert captured.out == \"\"\n    assert \"low balance\" in captured.err\n</code></pre>"},{"location":"python/tooling/testing/testing-patterns/#capfd-file-descriptor-level","title":"<code>capfd</code> \u2014 file-descriptor level","text":"<p>Use <code>capfd</code> (same API) when output comes from C extensions or subprocesses that bypass <code>sys.stdout</code>.</p>"},{"location":"python/tooling/testing/testing-patterns/#partial-matching","title":"Partial matching","text":"<p>For long or variable output, use <code>in</code> rather than strict equality:</p> <pre><code>assert \"Total:\" in capsys.readouterr().out\n</code></pre>"},{"location":"python/tooling/testing/testing-patterns/#design-note-prefer-a-pure-function","title":"Design note: prefer a pure function","text":"<p>If you're testing <code>print</code> output heavily, the function is probably conflating computation with display. Split them:</p> <pre><code># Hard to test \u2014 side effect baked in\ndef report_pnl(trades):\n    print(f\"P&amp;L: {sum(t.pnl for t in trades):.2f}\")\n\n# Easy to test \u2014 pure function + thin display layer\ndef compute_pnl(trades) -&gt; float:\n    return sum(t.pnl for t in trades)\n\ndef report_pnl(trades):\n    print(f\"P&amp;L: {compute_pnl(trades):.2f}\")\n</code></pre> <p>Test <code>compute_pnl</code> directly; leave <code>report_pnl</code> to a single smoke test or skip it entirely.</p>"},{"location":"python/tooling/testing/testing-patterns/#testing-functions-that-make-network-calls","title":"Testing functions that make network calls","text":"<p>The goal: test what your code does with the response, not whether the network is up. Never hit a real endpoint in a unit test.</p>"},{"location":"python/tooling/testing/testing-patterns/#patch-at-the-function-boundary","title":"Patch at the function boundary","text":"<p>Mock the outermost function that crosses a boundary you don't own:</p> <ul> <li>Testing <code>fetch_price</code> itself (response parsing)? Mock <code>httpx.get</code>.</li> <li>Testing code that calls <code>fetch_price</code> (orchestration)? Mock <code>fetch_price</code>.</li> </ul>"},{"location":"python/tooling/testing/testing-patterns/#monkeypatch-simpler-pytest-native","title":"<code>monkeypatch</code> \u2014 simpler, pytest-native","text":"<pre><code># mymodule.py\nimport httpx\n\ndef fetch_price(symbol: str) -&gt; float:\n    resp = httpx.get(f\"https://api.example.com/price/{symbol}\")\n    resp.raise_for_status()\n    return resp.json()[\"price\"]\n</code></pre> <pre><code>def test_fetch_price(monkeypatch):\n    class FakeResp:\n        def raise_for_status(self): pass\n        def json(self): return {\"price\": 42.0}\n\n    monkeypatch.setattr(mymodule.httpx, \"get\", lambda url: FakeResp())\n\n    assert mymodule.fetch_price(\"BTC\") == 42.0\n</code></pre> <p><code>monkeypatch.setattr</code> restores the original automatically after the test.</p>"},{"location":"python/tooling/testing/testing-patterns/#unittestmockpatch-when-you-need-call-inspection","title":"<code>unittest.mock.patch</code> \u2014 when you need call inspection","text":"<pre><code>from unittest.mock import patch, MagicMock\n\ndef test_fetch_price():\n    mock_resp = MagicMock()\n    mock_resp.json.return_value = {\"price\": 42.0}\n\n    with patch(\"mymodule.httpx.get\", return_value=mock_resp):\n        assert mymodule.fetch_price(\"BTC\") == 42.0\n</code></pre>"},{"location":"python/tooling/testing/testing-patterns/#happy-path","title":"Happy path","text":"<pre><code>def test_portfolio_value_happy(monkeypatch):\n    prices = {\"BTC\": 30000.0, \"ETH\": 2000.0}\n    monkeypatch.setattr(\"portfolio.fetch_price\", lambda s: prices[s])\n    assert portfolio_value([\"BTC\", \"ETH\"]) == 32000.0\n</code></pre>"},{"location":"python/tooling/testing/testing-patterns/#partial-failure-path-some-symbols-raise-others-succeed","title":"Partial-failure path \u2014 some symbols raise, others succeed","text":"<pre><code>def test_portfolio_partial_failure():\n    def flaky(symbol):\n        if symbol == \"DOGE\":\n            raise httpx.HTTPStatusError(\"404\", request=None, response=None)\n        return {\"BTC\": 30000.0, \"ETH\": 2000.0}[symbol]\n\n    with patch(\"portfolio.fetch_price\", side_effect=flaky):\n        result = portfolio_value([\"BTC\", \"ETH\", \"DOGE\"])\n\n    assert result[\"BTC\"] == 30000.0\n    assert result[\"DOGE\"] is None   # error was caught and mapped to None\n</code></pre> <p><code>side_effect</code> also accepts a list \u2014 each call pops the next item; an exception class is raised, a value is returned:</p> <pre><code>with patch(\"mymodule.httpx.get\", side_effect=[httpx.TimeoutException(\"\"), mock_resp]):\n    assert fetch_price_with_retry(\"BTC\") == 42.0   # retried after timeout\n</code></pre>"},{"location":"python/tooling/testing/testing-patterns/#patch-where-its-used-not-where-its-defined","title":"Patch where it's used, not where it's defined","text":"<p>Patch where the name is used, not where it's defined</p> <p><code>from httpx import get</code> binds <code>get</code> in <code>mymodule</code>'s namespace. Patching <code>httpx.get</code> replaces the original, but <code>mymodule.get</code> still points to the old function. The rule: patch the dotted path of the name as it appears in the module under test, e.g. <code>\"mymodule.get\"</code>. If it uses <code>import httpx</code>, patch <code>\"mymodule.httpx.get\"</code>.</p> <pre><code># mymodule.py uses:  from httpx import get\nwith patch(\"httpx.get\", ...):      # WRONG \u2014 has no effect in mymodule\nwith patch(\"mymodule.get\", ...):   # RIGHT\n</code></pre> <p>If the module uses <code>import httpx</code> and calls <code>httpx.get(...)</code>, patch <code>\"mymodule.httpx.get\"</code>.</p>"},{"location":"python/tooling/testing/testing-patterns/#decision-guide_1","title":"Decision guide","text":"Situation Use Simple replacement, no call inspection <code>monkeypatch.setattr</code> Need to count calls or inspect args <code>patch</code> + <code>MagicMock</code> Sequential call sequence (first fails, second succeeds) <code>side_effect=[...]</code> list Async network call <code>patch</code> + <code>AsyncMock</code>"},{"location":"python/tooling/testing/testing-patterns/#related-notes","title":"Related notes","text":"<ul> <li><code>testing-strategy.md</code> \u2014 philosophy, pyramid, mocks, TDD</li> <li><code>pytest.md</code> \u2014 command quick-reference</li> </ul>"},{"location":"python/tooling/testing/testing-strategy/","title":"Testing \u2014 Strategy","text":"<p>Tests are executable claims about behaviour. They exist to enable change safely \u2014 refactors, upgrades, onboarding \u2014 not to reach a coverage number.</p>"},{"location":"python/tooling/testing/testing-strategy/#the-test-pyramid","title":"The test pyramid","text":"<pre><code>         \u25b2  E2E / smoke tests     (few, slow, high confidence)\n        \u25b2\u25b2\u25b2 Integration tests     (moderate \u2014 real deps wired together)\n       \u25b2\u25b2\u25b2\u25b2\u25b2 Unit tests           (many, fast, pinpoint failures)\n</code></pre> <p>Most tests should be units. Integration tests catch what units can't (schema mismatches, transaction semantics). E2E tests cover only the critical path.</p>"},{"location":"python/tooling/testing/testing-strategy/#what-to-test","title":"What to test","text":"<p>Test behaviour, not implementation. The rule of thumb: risk \u00d7 value.</p> <ul> <li>Test heavily: pure functions, validation/parsing, error paths, business rules</li> <li>Skip: trivial getters, third-party library internals, UI layout</li> </ul> <p>\"Test the things that scare you.\" \u2014 Kent Beck</p>"},{"location":"python/tooling/testing/testing-strategy/#aaa-pattern","title":"AAA pattern","text":"<p>Every test has three acts:</p> <pre><code>def test_trade_rejects_negative_quantity():\n    # Arrange\n    data = {\"symbol\": \"BHP\", \"quantity\": -10, \"price\": 45.5, \"side\": \"BUY\"}\n\n    # Act + Assert\n    with pytest.raises(ValidationError):\n        Trade(**data)\n</code></pre> <p>One test, one behaviour. If a test has 10 assertions, split it into 10 tests.</p>"},{"location":"python/tooling/testing/testing-strategy/#fakes-stubs-mocks","title":"Fakes, stubs, mocks","text":"Term What it does When to use Stub Returns a canned value Drive a code path Mock Stub that records calls Assert a side effect was triggered Fake Lightweight real implementation (e.g. in-memory dict) When a stub is too thin <p>Mock at your own boundary \u2014 don't mock what you don't own</p> <p>Mocking third-party library internals (e.g. <code>httpx.Response</code> internals) lets the mock drift from the real API \u2014 the library updates and your tests keep passing while your code breaks. Instead, mock at the boundary you own: your function that calls the library, or your repository interface. See the testing-patterns.md decision guide.</p> <p>Don't mock what you don't own \u2014 mocking third-party library internals lets the mock drift from reality. Mock at your own boundary.</p> <pre><code>from unittest.mock import MagicMock, patch\n\ndef test_sends_alert_on_breach():\n    with patch(\"myapp.notifier.send_email\") as mock_send:\n        check_risk_limit(position=1_000_000)\n        mock_send.assert_called_once()\n</code></pre>"},{"location":"python/tooling/testing/testing-strategy/#how-much-is-enough","title":"How much is enough?","text":"<p>80\u201390% line coverage is a reasonable floor; beyond that, returns diminish fast. Better questions:</p> <ul> <li>Do the scary parts (validation, error handling, business rules) have tests?</li> <li>Would a new reader understand expected behaviour from the test suite?</li> <li>Does a real bug cause a test to fail? (Use <code>mutmut</code> for mutation testing.)</li> </ul>"},{"location":"python/tooling/testing/testing-strategy/#tdd-red-green-refactor","title":"TDD: Red \u2192 Green \u2192 Refactor","text":"<p>Write the failing test first, then write the minimal code to pass it, then clean up.</p> <pre><code>Red (failing test) \u2192 Green (passes) \u2192 Refactor (clean without breaking)\n</code></pre> <p>Most useful when fixing bugs (write a test that reproduces the bug, then fix it) and when designing new APIs.</p>"},{"location":"python/tooling/testing/testing-strategy/#useful-pytest-ecosystem","title":"Useful pytest ecosystem","text":"Package Role <code>pytest</code> Runner, discovery, assertions <code>pytest-cov</code> Coverage (<code>--cov=myapp --cov-report=term-missing</code>) <code>pytest-mock</code> Cleaner <code>mocker</code> fixture <code>hypothesis</code> Property-based testing \u2014 auto-generates edge cases <code>factory_boy</code> Generates fixture data for models <code>mutmut</code> Mutation testing to verify test quality"},{"location":"python/tooling/testing/testing-strategy/#hypothesis-example","title":"hypothesis example","text":"<pre><code>from hypothesis import given, strategies as st\n\n@given(st.integers(max_value=-1))\ndef test_rejects_any_negative_quantity(qty):\n    with pytest.raises(ValidationError):\n        Trade(symbol=\"BHP\", quantity=qty, price=1.0, side=\"BUY\")\n</code></pre>"},{"location":"python/tooling/testing/testing-strategy/#related-notes","title":"Related notes","text":"<ul> <li><code>pytest.md</code> \u2014 command quick-reference</li> <li><code>pydantic.md</code> \u2014 validation logic concentrates at model boundaries, ideal test target</li> <li><code>mypy.md</code> \u2014 static types + dynamic tests are complementary, not redundant</li> <li><code>testing-patterns.md</code> \u2014 practical patterns: external deps, file I/O, DB, stdout</li> </ul>"},{"location":"tools/","title":"Tools","text":"<p>Language-agnostic tools and notation.</p> File Type Description env-vars.md note Environment variables: what they are, scopes, .env files, security mermaid.md ref Mermaid diagram syntax for class relationships"},{"location":"tools/env-vars/","title":"Environment Variables \u2014 macOS / Unix","text":""},{"location":"tools/env-vars/#what-is-the-environment","title":"What is \"the environment\"?","text":"<p>Every Unix process has a small key-value store called the process environment: a flat list of <code>KEY=value</code> strings held in memory alongside the process. It's separate from the filesystem and from heap memory.</p> <p>Inheritance rule: a child process gets a copy of its parent's environment at spawn time. Changes in the child never propagate back up. Changes in the parent after spawning don't reach existing children.</p> <p>The shell (zsh/bash) is itself a process. When you open a terminal, the shell inherits from whatever launched it (the terminal app, launchd, SSH daemon) and passes copies to every command you run.</p>"},{"location":"tools/env-vars/#environment-variables","title":"Environment variables","text":"<p>A variable is one <code>KEY=value</code> entry. Convention: UPPERCASE_WITH_UNDERSCORES keys; values are always strings \u2014 programs interpret them as paths, integers, booleans, JSON, etc.</p> <p>Why they matter: - Configure behaviour without code changes (dev vs. prod, debug vs. quiet) - Keep secrets (API keys, tokens) out of source code - Interop: many tools look for well-known names (<code>HOME</code>, <code>PATH</code>, <code>EDITOR</code>, <code>AWS_ACCESS_KEY_ID</code>, <code>PYTHONPATH</code>)</p> <pre><code>env               # print entire environment\nprintenv HOME     # print one variable\necho $HOME        # shell expands inline\n</code></pre>"},{"location":"tools/env-vars/#setting-variables-three-scopes","title":"Setting variables \u2014 three scopes","text":"<p>Current session (gone when terminal closes): <pre><code>export API_KEY=\"sk-abc123\"   # export = visible to child processes\n</code></pre></p> <p>Without <code>export</code>, the variable exists in the shell but children don't inherit it.</p> <p>One command only (parent shell unchanged): <pre><code>LOG_LEVEL=DEBUG python app.py\n</code></pre></p> <p>Permanent (add to shell startup file): <pre><code># ~/.zshrc  (zsh, macOS default)\nexport EDITOR=\"vim\"\nexport API_KEY=\"sk-abc123\"\nexport PATH=\"$HOME/.local/bin:$PATH\"  # prepend to PATH\n\nsource ~/.zshrc   # reload without reopening terminal\n</code></pre></p> Shell Startup file When loaded zsh <code>~/.zshrc</code> Every interactive shell bash <code>~/.bashrc</code> Every interactive shell bash (login) <code>~/.bash_profile</code> Login shells, SSH"},{"location":"tools/env-vars/#path","title":"PATH","text":"<p><code>PATH</code> is a colon-separated list of directories searched left-to-right for executables:</p> <pre><code>echo $PATH\n# /opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin\n\nwhich python        # which directory won the search\n</code></pre> <p>Prepend to be found first; append to be found last: <pre><code>export PATH=\"/new/dir:$PATH\"\n</code></pre></p>"},{"location":"tools/env-vars/#env-files","title":".env files","text":"<p>A <code>.env</code> file is a plain-text <code>KEY=value</code> file at a project root, gitignored, read by the app at startup \u2014 not by the shell automatically.</p> <pre><code>myproject/\n\u251c\u2500\u2500 .env           \u2190 gitignored; real secrets\n\u251c\u2500\u2500 .env.example   \u2190 committed; fake values, documents what's needed\n\u251c\u2500\u2500 pyproject.toml\n\u2514\u2500\u2500 src/\n</code></pre> <pre><code># .env syntax\nAPI_KEY=sk-abc123\nDATA_DIR=./data\nLOG_LEVEL=DEBUG\n# no spaces around =; no export keyword; comments with #\n</code></pre>"},{"location":"tools/env-vars/#why-env-and-not-zshrc","title":"Why .env and not ~/.zshrc?","text":"<ul> <li>Per-project scope: avoids leaking project A's keys into project B's processes</li> <li>Tooling reads it: <code>pydantic-settings</code>, <code>python-dotenv</code>, Docker Compose, most frameworks all look for <code>.env</code> by default</li> </ul>"},{"location":"tools/env-vars/#reading-env-in-python","title":"Reading .env in Python","text":"<p>Bare: <pre><code>from dotenv import load_dotenv\nimport os\n\nload_dotenv()                        # reads .env from cwd\napi_key = os.environ[\"API_KEY\"]      # KeyError if missing\napi_key = os.getenv(\"API_KEY\", \"\")   # default if missing\n</code></pre></p> <p>With types + validation \u2014 prefer pydantic-settings: <pre><code>from pydantic_settings import BaseSettings, SettingsConfigDict\n\nclass Settings(BaseSettings):\n    model_config = SettingsConfigDict(env_file=\".env\")\n    api_key: str\n\nsettings = Settings()\n</code></pre></p>"},{"location":"tools/env-vars/#scope-summary","title":"Scope summary","text":"Scope Where Who sees it System <code>/etc/environment</code>, <code>/etc/profile</code> All users User session <code>~/.zshrc</code> Your terminal processes Project <code>.env</code> at project root Only when explicitly loaded Single command <code>KEY=val cmd</code> That command only"},{"location":"tools/env-vars/#debugging","title":"Debugging","text":"<pre><code>env | grep MYAPP                       # filter for your app's vars\nprintenv MYAPP_KEY                     # empty = not set\necho ${MYAPP_KEY:-\"not set\"}           # show fallback if unset\n</code></pre>"},{"location":"tools/env-vars/#security","title":"Security","text":"<p>Env vars are not a security boundary between processes on the same host</p> <p>Any process running as the same OS user can read another process's environment via <code>/proc/&lt;pid&gt;/environ</code> or <code>ps e</code>. Env vars are safe for keeping secrets out of source code and out of logs, but they don't protect against a compromised co-tenant process. For strong isolation, use a secrets manager (Vault, AWS Secrets Manager) that issues scoped credentials per service.</p> <ul> <li>Never commit <code>.env</code> with real secrets \u2014 always gitignore it</li> <li>Never hardcode secrets in source \u2014 once in git history, assume compromised</li> <li>Env vars are visible to all processes on the host running as the same user (<code>ps e</code>) \u2014 not a security boundary between processes on the same machine</li> <li>In CI/CD: use the platform's secret manager (GitHub Actions secrets, etc.); they're injected as env vars at runtime without appearing in logs</li> </ul>"},{"location":"tools/mermaid/","title":"Mermaid","text":"<p>Mermaid is a framework to depict class strucutre in a markdown file.</p>"},{"location":"tools/mermaid/#common-mermaid-relationships","title":"Common Mermaid relationships:","text":"<pre><code>A &lt;|-- B     inheritance: B extends A\nA &lt;|.. B     implementation: B implements A / protocol\nA --&gt; B      association: A uses B\nA *-- B      composition: A owns B strongly\nA o-- B      aggregation: A contains B weakly\n</code></pre>"}]}
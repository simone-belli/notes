# Python — Language / Stdlib / Text

Producing, formatting, and matching strings from the standard library.

:material-card-bulleted-outline: **[Format Spec Mini-Language](format-spec.md){ .lvl-advanced }**
:   Format spec mini-language: alignment, width, precision, type codes, `__format__`

:material-card-bulleted-outline: **[`re` — Regular Expressions](re.md){ .lvl-intermediate }**
:   Choosing a matcher (`str` methods, `fnmatch`, `pathlib`, `re`), then the `re` module itself: match/search/fullmatch, Match objects, compile, flags, sub, escape

:material-card-bulleted-outline: **[String Formatting in Python](string-formatting.md){ .lvl-basic }**
:   String ops: f-strings, str.format(), Template, split/join

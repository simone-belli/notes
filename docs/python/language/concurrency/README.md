# Python — Language / Concurrency

:material-text-box-outline: **[asyncio — Core Mechanics](asyncio.md){ .lvl-intermediate }**
:   async def, await, asyncio.run(), gather(), create_task() — core mechanics

:material-text-box-outline: **[Concurrency in Python](concurrency.md){ .lvl-basic }**
:   GIL, threading, multiprocessing, asyncio — overview and decision guide

:material-text-box-outline: **[Threading — `concurrent.futures` API](threading.md){ .lvl-intermediate }**
:   `ThreadPoolExecutor`, `Future` mechanics, `as_completed`, exception handling, shared state
